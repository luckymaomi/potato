# potato Agent 工作规约

本文件是仓库地图和最高协作约束。当前产品事实见 `spec.md` 或 `spec/`，具体开发方法见 `.agents/skills/`，用户入口见 `README.md`，探索与取舍见 `history.md`。

## 基础约束

- 始终使用简体中文与项目 owner 协作；仓库文本统一使用 UTF-8 无 BOM。
- 显示异常时先验证编码，不把终端乱码误判为文件损坏。
- 保留 owner 已有改动；脏工作区只处理当前任务相关文件，不回滚、不覆盖、不顺手整理无关内容。
- 修改采用范围明确、可审查的补丁；禁止无审查机械替换、大小写不受控的命名迁移和跨文件盲目替换。

## 事实纪律

- 判断、计划、代码和回复必须基于 owner 明确输入、当前仓库、命令输出、真实运行结果或已核验资料。
- 区分已确认事实、owner 提供但未验证的事实、推测、计划事项和已完成并验证事项。
- 文档、页面提示、HTTP 200、Mock、测试名称、文件存在或一次绿灯都不能单独证明真实业务完成。
- 事实不清楚时先调查；无法调查时明确写出未知、所缺证据和影响，不能用自信语气替代确认。
- 用户提出的原因和修法是线索，不是结论。发现过度抽象、错误职责、不可验证方案或无必要复杂度时，应基于证据说明并拒绝照做。

## 文档 owner

- `AGENTS.md`：跨任务长期有效的协作、安全、提交和验证纪律。
- `spec.md` 或 `spec/`：当前产品目标、边界、输入、输出、状态和验收标准。
- `.agents/skills/<name>/SKILL.md`：对应模块或工作流的可复用方法。
- `history.md`：问题、假设、尝试、证据、失败、成功层级和最终取舍。
- `README.md`：产品介绍、徽章、当前能力和快速入口。
- `plan.md`：当前任务合同；只有跨模块、迁移、外部副作用、恢复任务或 owner 明确要求时维护。

同一事实只在唯一 owner 中演进。其他文档只链接，不复制另一层的完整内容。

## 工程与断裂式变更

- 先建立输入、核心规则、状态、持久化、输出、失败、恢复、测试和文档的影响地图，再修改事实 owner。
- 单一职责按变化原因判断，不按文件行数机械拆分；文件过大只触发职责审查。
- 当前事实推翻旧 owner、schema、状态、接口或入口，且不存在正式生产数据或外部公共合同约束时，断裂式重建唯一主干；同一次交付替换定义、消费者、持久结构、配置、测试、文档、生成源和生成物。
- 若 owner 明确存在必须保留的正式生产数据、用户文件或外部公共合同，必须设计一次性、可验证、可回滚的前向迁移。

## 测试与交付

- 回归修复先建立可复现失败测试或等价证据，再修实现。
- 失败测试的名称和断言表达修复后应当成立的行为；成功、明确拒绝、错误可见和恢复完成都属于正向契约。
- 长期测试保护用户可观察结果和公共契约；旧名称、旧文件、旧字符串与内部目录形状交给差异审查和源码检查。
- 分开报告确定性自动测试、真实外部验收、人工验收和未验证项。
- 外部系统、真实凭据、人工登录或生产写操作使用证据闸门；证据未闭合时停在取证阶段。
- 完成职责完整模块并通过必要验证后再提交，不改一点推送一点。
- 提交信息使用简体中文的一句话；commit、push、部署、发布和生产写入遵循 owner 当次明确授权，默认分支为 `master`。
- 收尾说明完成事实、实际验证、未验证项、剩余风险和外部操作状态。
