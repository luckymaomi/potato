# AI 漫画工作流

> **定位**：土豆漫画（由原「土豆短剧 / potato」断裂转向）的**理念工作流**说明。  
> **不是**仓库实现合同（实现以日后更新的 `spec.md` 为准）。  
> **继承关系**：主干纪律直接继承桌面文档《AI 短剧工作流》与本仓 `reference/` 中关于**项目资产、定妆、按 ID 引用、双通道组装、单格返工**的共识；**删除**短剧主链中的「分镜图 → 镜头视频 → 预览成片」整段，改为「分格底板 → 字层 / 页模板排版 / 可选声层 → 阅读序与话数包」。  
> **吸收补充**：wind-comic 等样本中的分格/页序与单环节重跑；不照搬其全局素材池、无限画布节点动物园或强 AI 编剧闭环。  
> **owner 已定方向**：与**画像生成**有关的主路径只有**资产定妆图**和**分格底板图**；对白/旁白/气泡与四格等页合成为**非生图编排层**；声层走 PearAPI 通用文本配音 `freedub`（TTS），成功后下载归档；改字与改版式不得逼着重出底板；对用户生产单元称「话」；旧短剧数据已清空，实施按**断裂式重建**，不做迁移。

---

## 文档怎么读

本文分三层，不要混读：

| 层 | 含义 | 例子 |
| :--- | :--- | :--- |
| **生产合同** | 流水线必须成立的规则 | 项目资产隔离、按 ID 引用、双通道组装、底板与字/页/声分层 |
| **创作规范** | 提高一致性与可读性的推荐做法 | 角色三视图、干净底板、少烧字 |
| **明确不做** | 本流水线内核不承担 | 图生视频、AI 一次画四格、口型/I2V 冒充有声、自由排版画布、精排出版引擎 |

**与短剧工作流的读法差异**：短剧文档里「图片 / 视频双配方」「首帧 I2V」「预览成片」均为生产合同；在本文中这些属于**已删除的短剧合同**，不得再当作漫画内核必经步骤。

---

## 四层模型（全文总纲）

制作与返工必须按层思考，禁止揉成「一张带字四格有声成品图」当作唯一工程真相：

| 层 | 名称 | 是什么 | 改它时 | 与 AI |
| :--- | :--- | :--- | :--- | :--- |
| **A** | 分格底板 | 一格一张**干净**画面 | 不满意 → **重出这一张底板** | **是（主生成）** |
| **B** | 字层 | 对白 / 旁白 / 气泡类型与位置 | 改字、拖气泡、换样式 | **否（编辑）** |
| **C** | 页合成 | 把已有底板**引用**进版式槽位（单格 / 四格等） | 换模板、换槽位引用、调序 | **否（排版）** |
| **D** | 声层 | 对白文本 → PearAPI `freedub` TTS → 本地音频，挂在格上 | 改文案重配、换音色/风格、改绑定 | **TTS 接口（非生图）** |

另有更早的**资产定妆**（标准资产图）也属于 AI 生图，但是「定妆」而不是「做一话画面」。

**三条分层铁律（owner 确认）**

1. **一格 = 一张干净底板**；画面不对只重出这一张。  
2. **对白、旁白、气泡位置常改 → 绝不为此重出底板。**  
3. **单格页 / 四格页 = 已有分格的模板排版 → 绝不重新生成整页，更禁止 AI 一次画四格。**

**生产时分层；导出时可扁平化。** 「制成带字页图」或「静帧有声封装」是**导出动作**，不是把 B/C/D 焊回 A 的唯一存档形态。

---

## 主链

```
人工故事 / 本话剧本
  → 项目级资产库（角色卡 / 场景卡 / 道具卡 + 标准资产图）     【AI：定妆】
  → 分格台按 ID 编译（文本通道 + 图片通道 → 仅一份图片配方）
  → 分格底板图（干净主画面；默认无字无气泡）                 【AI：唯一话内主生成】
  →【B·字层】对白 / 旁白气泡贴字（可拖、可改；非 AI）
  →【C·页合成】选版式模板，引用已有底板填槽（非 AI）
  →【D·声层·可选】字层对白文本经 PearAPI freedub TTS，归档后挂阅读序
  → 阅读序预览 / 话数包 / **必须支持导出合成页图（含四格页）** / 可选静帧有声封装
  →【外置】精排、网纹、出版裁切、BGM 精修、复杂听书包装
```

**资产与引用铁律（继承短剧）**

1. **资产图是产出物**；用户上传的参考图是**生成输入**。二者不要混用。分格台只引用产出层标准图。  
2. **一个造型 / 一种光影设定 / 一种道具状态 = 一张独立资产卡 = 一个 ID。**  
3. **内核主媒体是静帧分格底板，不是视频。** 有声是 D 层，不是图生视频。

制作始终落在「某一个项目里的某一话」上。资产卡挂在**项目**上，供该项目下各话复用。

**命名对照（避免和短剧词混用）**

| 短剧用语 | 漫画用语 | 说明 |
| :--- | :--- | :--- |
| 集 | 话 | 生产单元；表名可用 `episodes`；**用户可见（含错误提示）禁止「集」**；产品路由用 `/comic/`，**禁止**产品主路径保留 `/film/` |
| 分镜 / 镜头 | 分格 / 格子 | 最小视觉制作单元 |
| 分镜台 | 分格台 | 底板配方的唯一编译器 |
| 分镜图 | 分格底板 | A 层主画面 |
| 镜头视频 / 预览成片 | （删除） | 不再进入主链 |
| 镜头包 | 话数包 | 交接导出物 |

---

## 成熟流水线六阶段总览

| 阶段 | 目标 | 主要产出 | 与 AI |
| :---: | :--- | :--- | :--- |
| 0 前期叙事 | 把故事落成可画的本话剧本 | 故事总览、本话剧本、场次与节拍 | 否 |
| 1 资产锁定 | 先定妆，再逐格制作 | 三类资产卡 + 标准资产图 | **是** |
| 2 分格编译 | 把一格信息编译成生图请求 | 有序分格规格 + **一份**图片配方 | 组装非生成 |
| 3 媒体生成 | 只出分格底板，可单格重跑 | 干净底板图 | **是** |
| 3b 编排层 | 字层 / 页模板 / 声层 TTS | 气泡、版式页、本地音频 | 字/页非生图；声=TTS |
| 4 验收返工 | 按层定点修，不整话重来 | 通过 / 标脏 / 重跑清单 | 仅重出 A 时 |
| 5 成话交接 | 交出可读（可听）交付物 | 阅读序、话数包、可选合成页 | 否 |

**成本纪律**

- 主链 AI 成本按「张」计量（定妆图 + 底板图），不按「秒」计量。  
- 改对白、改四格槽位、改配音 = 零生图成本。  
- 任何把视频生成或「AI 一次出四格」接回主链的提案，视为对本文的断裂式推翻。

---

## 〇、前期叙事

### 目标

得到**人工编辑、人工定稿**的故事与本话剧本。本流水线不提供 AI 写剧本、润色、自动拆格、提取资产或生成分格描述入口。

### 输入

- 故事想法、大纲、小说章节、已有剧本、参考设定（任选）

### 产出

- **项目故事总览**：世界观、主线、基调、画风倾向（可选，创作规范）  
- **本话剧本**：本话情节、场次、对白草稿、情绪节拍  
- （建议）本话出场角色 / 场景 / 关键道具清单草稿，供下一步建卡，**不自动当成资产卡**

### 规则

1. 文本创作以人工编辑为准。  
2. 换话只换本话剧本与后续分格/编排结果；不自动复制上一话。  
3. **剧本改动不自动一刀切标脏全话底板。** 剧本中的对白/旁白草稿与 B 字层应对齐靠人工；改草稿本身**不**把 A 底板标待复核。出场人物/场次大变时，由用户在分格台改规格或资产绑定后再走 A 层标脏。  
4. 剧本对白是**叙事草稿**；不自动变成气泡，也不自动触发 TTS。

---

## 一、容器层级：项目 · 话 · 项目资产库

```
项目（一部漫画 / 一个故事世界）
├── 项目级资产库（角色卡 / 场景卡 / 道具卡）← 本项目各话共用
├── 第 1 话：
│     剧本 → 分格列表（底板 A）→ 字层 B / 页模板 C / 声层 D → 预览与导出
├── 第 2 话：…
└── 第 N 话：…
```

### 1.1 项目

- 最外层容器：一部漫画、一个故事世界。  
- **项目级资产库**挂在项目上，不属于某一话。  
- 项目与项目之间资产库**完全隔离**。  
- 项目可记录统一画风关键词（创作规范），但不替代单卡文本与单格提示词。

### 1.2 话（生产单元）

- 每话有自己的剧本、分格清单、底板、字层、页模板实例、可选声层与导出物。  
- 换话不换资产库。  
- 话与话之间：**共享资产卡**；**不共享**本话剧本、分格、编排与导出包。

### 1.3 复用规则

| 范围 | 是否共享 | 说明 |
| :--- | :--- | :--- |
| 同一项目的各话 | **共享** | 同 ID 资产跨话引用 |
| 不同项目 | **隔离** | 目标项目内新建卡 |
| 同一项目内不同造型 / 光影 / 状态 | **独立卡** | 一卡一设定 |

> 本流水线**不设跨项目全局资产中心**。

---

## 二、资产锁定（三类资产卡）

资产卡不是「一张图」，而是一个**生成器**：

```
输入层（结构化文本 + 用户上传参考图）
  → 生成（文生图 / 图生图 / 文+图）
  → 产出层（标准资产图）
  → 供分格台按 ID 引用
```

**锁定门闸**

1. 建卡并填好本卡唯一造型/状态的文本。  
2. 需要时上传生成输入参考图。  
3. 生成或上传**标准资产图**，人工确认「选用这张」。  
4. 未确认标准图的资产，分格可引用文本，但生成底板前应优先补齐视觉锚点。

generation 历史只是「选用哪次生成结果」，**不是**资产卡版本树。

产出层提供独立的完整提示词编辑区；布局/类型下拉只选预设，不自动覆盖文本；只有用户点击「组装提示词」后后端才组装进编辑区。生成直接消费用户可见已保存文本；输入参考图走独立图片通道。

资产标准图逐卡提交；完成须以本地归档成功为准。

### 2.1 角色卡

#### 输入层 · 文本（也用于分格组装）

| 层级 | 字段 | 说明 |
| :--- | :--- | :--- |
| 身份 | 姓名（卡名）、年龄、性别、职业、阵营 | 叙事定位 |
| 物理 | 脸型、五官、发型、体型、肤色 | 锁定「脸」 |
| 服装 | **本卡默认穿搭** | 不同造型分别建独立卡 |
| 神态 | 性格、常见表情、气场 | 底板表情应符合人设 |
| 声音 | 音色倾向、语速、口音、标志性语气 | 供 D 层表演参考；**不自动 TTS** |

#### 输入层 · 参考图

- 只用于生成标准资产图，**不**直接进入分格 references。

#### 产出层 · 标准参考图册（创作规范）

每张角色卡至少 1 张标准图。推荐布局（实现可选其一）：

- **布局 A · 三栏三视图（推荐）**  
- **布局 B · 左脸右身**  
- **布局 C · 4+3 双层**  
- **布局 D · 7 图锚点组**

**一致性铁律**：特写与全身同一张脸、同一发型、同一服装。  
**漫画补充**：定妆图避免复杂背景文字与水印；定妆不合格不要进入分格量产。

### 2.2 场景卡

| 层级 | 字段 |
| :--- | :--- |
| 空间 | 地点类型、布局、建筑风格、尺寸比例 |
| 光影 | 时间段、光源、色温、明暗对比 |
| 陈设 | 关键家具、道具、装饰、植被 |
| 氛围 | 色调、情绪、天气 |

产出（创作规范）：空间全景（至少 1）；局部特写可选。不同光影 = 独立变体卡。

### 2.3 道具卡

| 层级 | 字段 |
| :--- | :--- |
| 物理 | 名称、类别、尺寸、材质、颜色、形状 |
| 细节 | 新旧程度、特殊标记、独特设计 |
| 状态 | **本卡当前状态**、互动与绑定说明 |

产出（创作规范）：多角度图。不同状态 = 独立变体卡。

### 2.4 结构关系

| 资产卡 | 输入：文本 | 输入：参考图 | 产出：标准图 |
| :--- | :--- | :--- | :--- |
| 角色卡 | 身份 + 物理 + 穿搭 + 神态 + 声音 | 用户上传 | 三视图等布局之一 |
| 场景卡 | 空间 + 光影 + 陈设 + 氛围 | 用户上传 | 全景 / 特写 / 光影变体卡 |
| 道具卡 | 物理 + 细节 + 状态 | 用户上传 | 多角度 / 状态变体卡 |

### 2.5 分格台如何取用资产

- **文本结构** → 进入 `panelPrompt`  
- **产出层标准资产图** → 进入 `panelReferences`（**有标准图的出场资产必须按 ID 显式带入**，不能只靠文本碰运气）  
- 文本与图片双通道；不注入资产生成输入参考图。  
- **降级**：当前图片模型参考图上限不足以容纳本格全部标准图时，**明确失败或要求用户减少出场资产/更换模型**，禁止静默丢弃 references 还当作双通道成功。

---

## 三、分格台：底板配方编译器

分格台是 **A 层底板** 的唯一编译器。它不负责字层排版引擎、页模板填槽或配音；那些是 B/C/D。

> 继承短剧「显式绑定资产 ID + 后端唯一组装 owner + 显式按钮才组装」。  
> **删除**「一次组装图片/视频双配方」。只组装**一份图片配方**。

### 3.1 分格在问什么

| 维度 | 短剧镜头问 | 漫画分格问 |
| :--- | :--- | :--- |
| 叙事 | 这几秒发生什么 | 这一格落在哪个节拍 |
| 视觉 | 景别、机位、运镜 | 谁在画里、表情/动作定格、构图 |
| 时间 | 时长、运动 | 阅读序中的位置；无「秒」 |
| 对白 | 常进视频提示词 | **B 层**；默认不进生图 |
| 声音 | 常进视频提示词 | **D 层**；freedub TTS 挂载 |

### 3.2 输入

#### （1）本项目资产库（按 ID）

只连入本格实际出场的资产卡 ID。

#### （2）本话人工分格规格（均可空）

| 字段 | 用途 | 层 |
| :--- | :--- | :--- |
| 标题 / 节拍说明 | 回退主干 | A |
| 出场资产 ID | 绑定角色/场景/道具 | A |
| 取景 / 构图 / 视角 | 静帧构图 | A |
| 动作定格 / 表情 | 定格姿态 | A |
| 光线 / 氛围 | 光影情绪 | A |
| 图像提示词 | 生图主干 | A |
| 本格额外参考图 | 仅本格 | A |
| 对白 / 旁白及气泡几何 | 字层数据 | B |
| 音源指针 | 本格可选音频 | D |

页模板选槽、跨格排版属于 **C 层**，不是单格规格里的「再生成字段」。

**生产合同**：运镜、视频时长、视频提示词等短剧字段不进入漫画主路径。

### 3.3 组装逻辑（生产合同）

用户点击「组装图片配方」后才编译。选择资产、改规格或打开页面本身不触发组装。

#### 通道一：文本 → `panelPrompt`（空值跳过）

1. 图像提示词（空则回退节拍 / 标题）  
2. 所选资产卡结构化文本  
3. 取景、构图、视角、动作定格、表情、光线、氛围

规则：

- 有值才组装。  
- **对白 / 旁白默认不进入生图提示词。**  
- 声音字段不进入图片配方。  
- 创作规范：提示词宜含「干净画面、无字幕、无气泡、无水印」。  
- 后端唯一 owner。

#### 通道二：图片 → `panelReferences`

- 本格资产标准图 + 本格额外参考图  
- 不注入资产生成输入参考图  

### 3.4 输出

```
{
  panelRecipe: {
    panelPrompt: string,
    panelReferences: string[]
  }
}
```

可见、可编辑、可重写；保存后为生图唯一快照。改规格不自动覆盖已保存配方，而是**配方待重装** + 底板**待复核**。

---

## 三 B、字层（对白 / 气泡）——非 AI

### 生产合同

1. 字层是底板之上的**独立图层数据**（文案、气泡类型、位置、缩放、可选说话人），**不是**底板文件的一部分。  
2. **默认路径 = 工具内贴气泡**：选样式 → 打字 → 拖到本格上 → 可改可删。  
3. 改对白 / 旁白 / 气泡位置 → **禁止**要求重出底板。  
4. 无对白的格子合法。  
5. **不要求** OCR、AI 自动排气泡、AI 把字画进底板为必经步骤。  
6. 「制成带字图」仅允许作为**导出时的扁平化合成**；工程内存档仍保留 A/B 分离。

### 首期范围（P0）

| 做 | 不做（首期） |
| :--- | :--- |
| 对白挂在「某一格」上 | AI 生成带字整格图当默认 |
| 2～3 种气泡 + 旁白框 | 全功能字体/出版排版 |
| 拖动、改字、删除 | AI 自动气泡位置 |
| 导出可选「合成带字」或「字与底板分装」 | 把字焊死进唯一底板存档 |

### 创作规范

- 声效字（「砰！」）优先走字层。  
- 若二期提供「生成空气泡框素材」，也只当贴纸底图，**字仍是真字**；不得默认烧文案进框图。

---

## 三 C、页合成（版式模板）——非 AI

### 生产合同

1. **有序分格列表的 `order` = 本话故事阅读序的唯一真相。**  
2. **页合成 = 人选版式模板 + 向槽位引用已有底板（分格 ID）**，零生图。  
3. **禁止**「选四格预设后让模型一次画满四格」。  
4. **禁止**把页合成做成与分格列表平行的第二套「再生生产线」。  
5. 单格页与四格页是同一批底板的**不同包装**，不是两套互不相干的图。  
6. 页上引用的是分格 ID；底板更换后，页预览跟随当前选用底板（可提示已变）。  
7. **槽位与阅读序**：允许手填槽位；「按序填槽」必须按分格 `order` 填充。导出 `reading_order.json`（或等价）须同时记录**分格序**与**页序（layout 列表 + 每槽 panel id）**。  
8. **模板注册表数据驱动**：模板 id、槽位数、栅格几何存为数据；首期只注册下表三个，二期加模板不改拼图核心。首期**不做**自由画布与一大两小等复杂版式。

### 首期版式预设（人为定义，够用即止）

| 模板 | 槽位数 | 用途 |
| :--- | :---: | :--- |
| 单格 | 1 | 单页重点格、封面感、只做一格 |
| 四格 2×2 | 4 | 经典四格；**必须可导出一张本地合成页图** |
| 竖条四格 | 4 | 条漫一屏 |

导出硬门闸：槽位与底板齐全才允许导出；缺一则明确失败，禁止静默残页。  
**拼页比例**：只按各槽底板本地文件真实宽高拼接；例如目录手选 4:3 生成的底板，四格页即为 4:3 格的 2×2，不得用伪造 1:1 通用格硬拉。同一页内四张比例必须一致（允许 1px 误差），否则明确拒绝。上传底板同样以实际像素为准。

### 使用方式

```
同一套分格底板列表
  ├─ 单格模式：一次看 / 导一格（或一格一页模板）
  └─ 多格模式：选模板 → 把已有分格填进槽位 → 预览 / 导出合成页
```

内容制作永远在分格列表里出底板、改字；交差时再选「单格序列」或「四格页」等导出包装。

---

## 三 D、声层（有声，可选）——PearAPI 文本配音

有声漫画 = **静帧阅读序 + 可选音轨**，不是短剧。音源合同为 PearAPI 通用文本配音接口，**不是**图生视频，也不是口型驱动。

### 接口合同（生产合同）

| 项 | 值 |
| :--- | :--- |
| 地址 | `https://api.pearapi.ai/api/freedub` |
| 方法 | `GET`（拉 role/style 列表）或 `POST`（生成；JSON body） |
| 权限 | 文档称免费接口；凭据与其它 PearAPI 调用一致走现有配置，不另造密钥体系 |
| 必填 | `text` |
| 可选 | `role`（音色）、`style`（语录意境，默认如 `cheerful`） |
| 成功 | `code === 200`，取 `data.audio_url` |
| 归档 | `audio_url` 为临时链；**必须下载到本地归档**后，业务对象才指向本地可用音频；供应商 URL 不得冒充完成 |

### 规则

| 规则 | 说明 |
| :--- | :--- |
| 非必经 | 无声层时静帧主链完整 |
| 文本来源 | 默认用本格 **B 层对白/旁白文案** 作为 `text`；用户可改后再生成 |
| 音色/风格 | 从接口列表选择并校验；未选可用接口默认 |
| 绑定 | 首期**一格一段音**，按阅读序播放；四格页按槽位顺序播 |
| 与气泡 | 不强制「一条气泡几何 = 一段音频」；首期按格配音即可 |
| 角色卡「声音」字段 | 仅作选 role 时的人工参考，**不**自动映射为接口 role（除非日后显式做映射表） |
| 导出 | 试听；话数包带本地音频；可选静帧有声封装（**非** I2V） |
| 禁止 | 口型驱动、为有声引入图生视频、首期逐气泡高亮听书引擎 |

### 与「非生图」的关系

D 层**不调用图片模型**。TTS 是独立文本转音频能力；完成标准与图片一样：任务明确成功/失败 + **本地归档成功** 才算完成。

---

## 三 E、生图前就绪检查

| 检查 | 建议行为 |
| :--- | :--- |
| 无出场资产且主体依赖角色 | 警告或阻断 |
| 出场角色/场景无标准图 | 强警告或阻断 |
| 无图像提示词且无节拍/标题 | 警告 |
| 提示词要求九宫格/多格同图 | **警告并引导改走 C 层页模板** |
| 提示词要求把完整对白烧进画面 | **警告并引导改走 B 层** |

### 分格台原则

1. 唯一组装 owner 在后端。  
2. 有就组装，空就跳过。  
3. 文本 / 图片双通道。  
4. 按资产 ID 引用。  
5. 只出图片配方；B/C/D 不参与默认生图组装。  
6. 一格一底板；阅读序由分格列表表达；页模板只引用不重生。

---

## 四、媒体生成（仅 A 层 + 定妆）

### 4.1 分格底板

- 消费已保存 `panelRecipe`。  
- **可 AI 生成，可本地上传**，可清除当前图指针（历史保留）；二者完成标准相同：本地归档成功后才能选用。  
- **画幅只使用当前手选图片模型目录声明的能力**；无预设或无手选画幅则不得生成；不伪造通用比例表，不把多模型能力合并成假目录（纪律同 2026-09-21 制作页能力投影与全手选门闸）。  
- 产品主路径**只需要图片 Provider**；视频能力从主路径移除。  
- 真实全链验收优先用 **Agnes 图片模型**（免费）跑通定妆与底板。

### 4.2 明确禁止的「生成」

- 图生视频 / 视频队列 / 整话视频合成  
- AI 一次生成四格同图  
- AI 因改对白而重出底板  
- AI 因换页模板而重出各槽画面  

### 4.3 单次出图

- 定妆图与底板均单次提交；同一资产或同一分格同时仅一项进行中图片任务。  
- 失败可见，单格重跑。

### 4.4 何谓「制成」

| 制成物 | 含义 | 首期 |
| :--- | :--- | :--- |
| 工程态 | A/B/C/D 分离保存，可继续改 | 必须 |
| **合成页图** | 导出时把槽位底板 +（可选）字层扁平化成一张交付图 | **必须**（含四格 2×2、竖四格、单格） |
| 话数包 | 分格底板 + 字层数据 + 可选音频 + 可选合成页 | 必须 |
| 静帧有声封装 | 顺序静帧 + 音轨封装播放件（非 I2V） | 可选/可二期 |

用户说「导出一张四格页」= **C 层模板合成导出**，不是再调文生图画四格。

---

## 五、验收与返工

### 5.1 按层验收

**A 底板**：脸/服是否仍是所引资产；场景是否漂；构图表情是否落节拍；是否误烧字/水印/多格。  
**B 字层**：文案是否对；气泡是否挡关键脸（人工判断）。  
**C 页**：槽位引用是否正确；阅读序是否对。  
**D 声**：是否指错格、是否缺失（若启用）。

### 5.2 标脏与定点重跑

| 上游变更 | 下游 |
| :--- | :--- |
| 改资产或换标准图 | 引用该卡的**底板**待复核 |
| 改分格规格或出场资产 | 配方待重装，需重出**底板** |
| 只改 B 字层（对白/气泡位置） | **不**重出底板；不标 A 待复核 |
| 只改剧本对白/旁白**草稿** | **不**自动脏 A；应改 B 或人工决定是否改分格 |
| 只改 C 槽位 / 模板 | **不**重出底板 |
| 只改声层（换 TTS 结果） | **不**重出底板 |
| 重出底板 | 字层位置可能需微调；页引用与声绑定通常保留 |

原则：**能改 B/C/D 就不动 A；能单格重跑 A 就不要整话重跑；禁止「剧本一改全话底板待复核」。**

### 5.3 选用当前媒体

「选用这张」只改底板指针；generation 历史保留。

---

## 六、成话交接

### 6.1 内核交付物

1. 按分格序收集本话底板（当前选用）。  
2. 导出范围内缺底板则**明确失败**。  
3. **阅读序预览**（可叠加字层；有声则顺序播）。  
4. **页模板预览 / 导出合成页图（首期必做）**：至少支持单格页、四格 2×2、竖四格；槽位填齐且底板齐全才允许导出对应页；可选是否烧入字层。  
5. **话数包**（日常交接；本地数据为工程真相）：

```
{项目名}_第N话_话数包/
  panels.csv
  pages/                         # 合成页导出物（含四格页）
    page_01_fourgrid.jpg
  01_{safeTitle}/
    panel.jpg                    # A 底板（必有）
    assets/...
    meta.txt                     # panelPrompt、资产 ID 等
    captions.json                # B 字层（文案+几何，可选）
    audio.wav                    # D（可选）
  reading_order.json
  page_layouts.json              # C 模板与槽位引用（可选）
```

6. 支持整话导出与选中分格导出。  
7. 每一话各自导出。

### 6.2 明确外置

- 出版级精排、网点、装帧、平台专用包  
- 复杂多轨听书工程、BGM 精修  
- 任意 I2V / 动态漫运动镜头生产线  
- 首期不做的自由画布与 AI 自动排字  

内核保证：分格序正确、底板可选中、资产可追溯、A/B/C/D 可分离携带、预览可读（可听可选）。

### 6.3 与短剧对照

| 短剧 | 漫画 |
| :--- | :--- |
| 预览成片 MP4 | 阅读序 / 页模板预览；可选静帧有声封装 |
| 镜头包 | 话数包（底板+字层数据+可选音频+可选合成页） |
| 工程 ZIP 往返 | 不做日常交接 |

---

## 七、总原则（浓缩）

1. **项目隔离，话内复用资产。**  
2. **一卡一造型；需要新造型就新建卡。**  
3. **先定妆，再出分格底板。**  
4. **分格台只编译底板图片配方；按 ID 引用，双通道组装。**  
5. **画像主生成只有定妆 + 底板；字/页为编辑排版；声为 freedub TTS。**  
6. **改字、改气泡、改四格槽位，不重出底板；改对白后可重跑 TTS。**  
7. **页合成是模板引用，不是重新生成；禁止 AI 一次画四格；四格合成页必须可导出。**  
8. **生产分层，导出可扁平化。**  
9. **有声可选；走 PearAPI freedub 并本地归档；不是短剧视频。**  
10. **断裂式转向：空库重建漫画主链；不迁移旧短剧视频合同。**  
11. **不伪造模型能力；不照搬全局资产中心、强 AI 编剧、I2V 主链。**

---

## 八、从《AI 短剧工作流》断裂删除清单

| 短剧合同 | 漫画处置 |
| :--- | :--- |
| 图片 + 视频双配方 | 仅图片配方 |
| 运镜 / 视频提示词 / 视频参考 | 删除主路径 |
| 分镜图作视频首帧 | 删除 |
| 镜头视频与视频队列 | 删除 |
| 视频待复核 | 删除；保留底板待复核 |
| 整集视频合成 | 改为阅读序 / 页模板预览；可选静帧有声封装 |
| Provider 视频主能力 | 从主路径移除 |
| 对白进视频提示词 | 改为 B 字层 |
| 声音进视频提示词 | 改为 D 层 PearAPI freedub TTS |

**保留并改名**

| 保留纪律 | 漫画中的名字 |
| :--- | :--- |
| 项目隔离与三类资产 | 同左 |
| 定妆标准图 vs 输入参考图 | 同左 |
| 一卡一造型 | 同左 |
| 显式组装、后端唯一 owner | 分格台组装底板配方 |
| 单镜定点返工 | 单格定点返工（仅 A） |
| 当前选用指针 + generation 历史 | 同左 |
| 不设全局资产中心、不做强 AI 编剧 | 同左 |

---

## 附录 A · 吸收与拒绝

| 吸收 | 拒绝照搬 |
| :--- | :--- |
| 项目级资产 + 分格显式绑定 ID | 跨项目全局 Asset Hub |
| 定妆后再逐格出底板 | 无门闸一键出整话 |
| 字图分离、页模板引用底板；**四格页必须可导出合成图** | AI 一次画四格；烧字进唯一存档 |
| 单格失败可重跑 | 无限画布节点动物园当唯一合同 |
| 声层走 PearAPI `freedub` TTS 并本地归档 | 口型视频；I2V 冒充有声；仅外链不归档 |
| 话数包交接 | 工程 ZIP 当日常交接 |
| — | 强 AI 写剧本为默认必经 |

## 附录 B · 与实现文档的关系

| 文档 | 职责 |
| :--- | :--- |
| 本文《AI 漫画工作流》 | 理念与生产纪律（owner 方向已写入） |
| 桌面《AI 短剧工作流》 | 旧短剧理念；仅作对照 |
| 仓库 `spec.md` | 断裂实施后应改写为漫画主链事实 |
| 仓库 `README.md` | 用户入口与能力摘要 |
| 仓库 `history.md` | 探索与取舍证据 |
| 仓库 `plan.md` | 断裂改造执行合同（另写） |

## 附录 C · 静帧 vs 有声（同内核）

| | 静帧 | 有声 |
| :--- | :--- | :--- |
| 主媒体 | 分格底板 | 分格底板 |
| 字层 / 页模板 | 同左 | 同左 |
| 声层 | 关 | 开（PearAPI freedub TTS → 本地归档） |
| 是否需要 I2V | 否 | 否 |

## 附录 D · 开放点（已关闭）

| 原开放点 | owner 裁定 |
| :--- | :--- |
| 声层分期 | **首期做**；音源 = PearAPI `https://api.pearapi.ai/api/freedub` |
| 称「话」或「集」 | **已定**：用户可见一律「话」；路由本轮改 `/comic/`，不留 `/film/` |
| 分格表名 | **已定**：本轮用 `panels`（或等价），禁止长期 `storyboards` 挂羊头 |
| 旧短剧数据 | **已清空**；断裂重建，不迁移 |
| 合成页图 | **已定**：首期必导出，含四格；缺底板失败 |
| 模板扩展 | 首期三模板；**注册表数据驱动**便于二期加模板，首期不做自由画布 |
| 标脏 | **已定**：按层；剧本对白草稿不自动脏底板 |
| TTS 完成 | **已定**：业务指针必须本地文件，禁止 https 冒充完成 |
| references | **已定**：有标准图必进 references；上限不足则明确失败/换模，禁止静默丢图 |

## 附录 E · 主链一句话

**定妆与底板才生图；字可拖、页用模板引用并必须能导出四格页；声走 freedub TTS；生产分层；不烧视频，不 AI 画四格；空库断裂不迁移。**
