# tomato-ai-drama 前端

React 18 + TypeScript 工作台，包含项目列表、AI 配置和项目制作区。

```powershell
npm.cmd install
npm.cmd run dev
npm.cmd run build
npm.cmd run lint
npm.cmd test
```

开发服务器默认地址为 `http://localhost:3012`，`/api` 与 `/static` 代理到 `http://localhost:5679`。
