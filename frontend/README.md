# mini-video 前端

这是 `mini-video` 的 React 18 + TypeScript 工作台前端，包含项目列表、AI 配置和 React Flow 画布。

开发服务器默认运行在 `http://localhost:3012`，并将 `/api` 与 `/static` 代理到本项目后端 `http://localhost:5679`。

```powershell
npm.cmd install
npm.cmd run dev
npm.cmd run build
npm.cmd run lint
```
