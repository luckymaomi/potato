# potato 变更记录

> 硬规则：本文件严格按时间从前到后排列，最新历史只能追加到最下面；不得把新记录插入文件顶部，也不得删除既有历史。

## 2026-09-16：工作台可管理性与运行体验重构


- 默认模板固定为 10 个节点、12 条依赖连线和 1 个可保存工作流；新项目首次初始化，已有画布不覆盖，虚拟 Demo 按版本幂等打开。
- 项目列表补齐搜索、新建、打开、编辑、删除、导入和导出；节点补齐新增、编辑、复制、确认删除；工作流组补齐保存、改名、运行和删除。
- 所有运行入口收敛到同一执行器：单节点、所选节点、当前节点下游、整个画布和工作流组按拓扑顺序运行，循环连线和上游失败均明确反馈。
- 连接会传递必要的上游文本与图片；图生图/图生视频无参考图时明确拒绝；文本提取和分镜结果会回写节点内容供后续节点使用。
- 全面重做项目页、画布、节点、检查器和配置页视觉层级，移除所有单边强调色、状态 rail、`border-left/right` 和伪元素色条。
- 新增 Vitest 编排测试 6 项；前端构建、lint、测试与依赖审计通过。Chrome/Edge 无头截图在当前环境未产出，真实 Agnes/PearAPI 生成未因缺少凭据而宣称通过。

## 2026-09-16：前后端目录标准化


- owner 确认项目不需要路径兼容，因此将两个应用目录断裂式统一为 `frontend/` 与 `backend/`，没有保留副本、软链接或旧路径探测。
- 同步更新根启动脚本、后端静态前端产物定位、当前产品事实、安装运行命令和 FFmpeg 说明；API、数据库 schema、Provider 合同和页面功能均未改变。
- 验证：后端迁移、完整严格类型检查、21 个测试与构建通过；前端 lint 与构建通过；根启动脚本 Python 语法检查通过；旧目录名静态扫描为 0。
- 运行探针：从 `backend/` 启动正式构建产物后，`GET /health` 返回 `0.0.1`，根路径成功返回 `frontend/dist` 页面；探针结束后服务已关闭。
- 外部边界：没有真实第三方凭据，本次未复验 Agnes/PearAPI 生成；未执行 commit、push 或部署。

## 2026-09-16：仅 Agnes 与 PearAPI 的严格 TypeScript 后端


- owner 明确本项目没有生产数据或旧版本兼容要求，因此采用断裂式重建，不迁移历史供应商和旧业务模块。
- 修改前证据：全后端完整严格检查有 2950 条诊断、涉及 79 个旧文件，其中 2253 条为隐式参数类型，463 条为未收窄异常值。
- 取舍：删除历史供应商实现、协议推断、供应商专用路由、旧迁移、旧测试与无用依赖；项目、配置、任务、实体、分镜、文本、图片、视频、归档和整集合成按变化原因拆为独立服务。
- Provider 核心只按明确身份注册和选择适配器，当前注册 Agnes 与 PearAPI。Agnes 补齐文本生成；PearAPI 保持图片与视频能力。业务服务不读取供应商响应结构。
- 数据：旧开发数据库及 WAL/SHM 已删除，新 SQLite schema 只保留工作台当前使用的表；这次操作不可恢复，符合 owner 明确的无兼容边界。
- 验证：全后端完整严格 TypeScript 检查 0 诊断；迁移、构建、21 个测试全部通过。测试覆盖两家供应商的文本/图片/视频合同、未知供应商拒绝、项目和画布持久化、归档往返、文本与四种媒体模式，以及使用仓库内 FFmpeg 的真实本地整集合成。
- 运行探针：构建产物启动和健康接口通过；Provider 目录只返回 Agnes/PearAPI；项目、集数、画布、归档导出通过；未知供应商返回 400，不存在项目返回 404，缺少图片配置返回 400；前端入口和 Vite API 代理通过。
- 外部边界：没有真实第三方凭据，不能宣称 Agnes/PearAPI 的生产生成已验证。前端构建仍报告单个产物块超过 500 kB 的性能警告。
- 收尾：HTTP 验收产生的项目数据已删除并重建空库；前后端服务已关闭；未执行 commit、push 或部署。

## 2026-09-17：Agnes 真实 Demo 全链路验收


- 完整 Demo 升级为 `agnes-short-drama-demo` v4：11 个节点、13 条曲线连线；除手工梗概外所有节点默认选择 Agnes，模型仍从实时目录读取。
- 删除虚拟成功结果后真实生成《雾港来信》：完成剧本、角色/场景/道具提取、角色/场景/道具参考图、六个分镜、首张分镜图、文生视频与图生视频；真实结果写回本地项目 2、剧集 3 和画布节点。
- 修复提取节点不传实体 ID、分镜图误取角色 ID 两个编排缺陷，并增加对应前端测试。旧 Agnes Demo 点击入口时原位升级，不重复创建项目。
- Agnes 文本并发曾返回 429，道具图曾发生一次网络失败；采用串行调用和明确重试后完成。失败任务保留失败事实，没有改写成成功。
- 实时目录的 `agnes-video-2.5` 对当前凭据返回 403，`agnes-video-2.5-flash` 实测可用；当时曾新增根配置 `default_models`，后续已由 AI 配置页全局预设替代，当前实现不再读取该字段。
- 首次整集合成因 FFmpeg concat 拒绝 HTTPS 失败；加入最小协议白名单并新增远程 HTTP 视频回归测试后成功生成本地 MP4，FFmpeg 解码验证通过。
- 验证：后端迁移、严格类型检查、27 个测试、构建通过；前端 11 个测试、lint 0 诊断、构建、0 漏洞审计通过；禁用单边强调色的静态扫描命中 0。
- 未验证：PearAPI 真实付费生成与浏览器 GUI 人工视觉/拖拽验收。完成验证后经 owner 明确授权提交并推送 `origin/master`；未执行部署或发布。

## 2026-09-17：Demo 画布编码回归修复


- 截图与数据库核验表明：项目、剧本、实体和分镜均为正常 UTF-8，只有 `dramas.metadata.canvas_layout` 中文变成了字面量 `?`；不是字体缺失或前端渲染问题。
- 根因是验收阶段通过 Windows PowerShell 回写整份画布 JSON 时，请求 Body 未按 UTF-8 发送，中文在进入后端前已不可逆丢失。
- 修复前使用 SQLite 在线备份保留恢复点；随后以正常剧本、角色、场景、道具和分镜记录为事实源重建画布，保留节点坐标、媒体 URL、任务 ID、完成状态和整集结果。
- Demo 升级到 v5：模板可从已有项目事实水合，并识别连续问号型损坏画布后原位修复。回归测试覆盖结果水合和损坏识别。
- 数据复验：11 个节点标题全部恢复，连续问号字段 0、Unicode 替换字符 0、完成节点 10、媒体节点 5、任务节点 10，第 1 集仍为 completed 且保留整集视频。
- 本轮修复未获得新的 commit/push 授权，远程 `origin/master` 暂未包含此修复。

## 2026-09-17：本地参考图 Base64 与缩略图修复


- 真实失败证据：项目 3 的图片记录 6、7 使用本地 `/static/uploads/*.jpg` 和 `agnes-image-2.5-flash`，均返回 HTTP 400。文件本身存在且可读，根因是旧实现把本地路径拼成 `http://localhost:5679/static/...` 交给远端 Agnes。
- 新增统一媒体引用解析服务：公网 URL 原样保留；本地静态图片在 storage 根目录内完成真实路径、文件存在、16MB 上限和 JPEG/PNG/GIF/WebP 魔数校验后，临时转换为 `data:image/...;base64,...`。路径穿越、缺失文件和伪造图片均明确拒绝。
- Agnes/PearAPI 图片适配器统一在提交前解析参考图，业务服务不增加供应商分支；视频接口仍只把实际公网 URL 当作公网引用，不再伪装 localhost 地址。
- Provider 传输层会从 `error.message`、`detail`、`message`、`msg` 等常见字段提取不超过 300 字的上游原因，使节点能显示比“HTTP 400”更具体的错误。
- 画布检查器用独立参考图组件替代 URL 标签：保留 URL 添加和本地多图上传，下方显示缩略图，支持点击预览和单张删除；所有新增样式使用完整边框，无单边强调色。
- 生成结果不再只显示文字链接：右侧检查器新增当前结果缩略图，图片可点开大图，视频可直接播放，同时保留新窗口打开入口；画布节点与检查器共用媒体类型判断。
- 真实 Agnes 复验：使用原失败节点的本地 JPEG、原提示词和 `agnes-image-2.5-flash` 创建图片记录 8，任务完成；返回 PNG 可下载，HTTP 200，大小 4,260,720 字节。结果已写回项目 3 的角色参考图节点。
- PearAPI 密钥已写入被 Git 忽略的根 `config.yaml` 并完成脱敏读取确认；遵照 owner 要求没有向 PearAPI 发起请求。
- 验证：后端 strict typecheck、32/32 测试和 build 通过；前端 17/17 测试、lint 和 build 通过；单边强调色扫描无命中。未做浏览器人工视觉验收，构建仍有单包超过 500kB 的既有性能警告。
- 本轮未执行 commit、push、部署或发布。

## 2026-09-17：PearAPI 双凭据适配修复


- 真实失败证据：项目 3 的 PearAPI 图片记录 10、12、13（图生图）和 14（文生图）均返回“余额不足或用户不存在”；无参考图仍失败，排除了 Base64 与图生图参数。
- 根因经 PearAPI 人员回复和真实调用共同确认：默认渠道 `sk-` 令牌只供 `/v1` 接口并放在请求头，普通分发 Key 供 `/api/image_generate`、`/api/video_generate` 并放在请求体；旧适配器错误地把前者当作后者。
- 根配置现在分别保存 `api_key` 与 `settings.generation_key`；PearAPI 媒体适配器只接受普通分发 Key，缺失时明确拒绝，不再回退到 `sk-` 令牌。示例配置与用户文档同步说明两者用途。
- 修复后的真实图片记录 15 使用 `gpt-image-2` 完成，返回 `image/png`，大小 2,065,056 字节，结果主机为 `cdn.pearapi.ai`；这证明普通分发 Key 的媒体调用和账户余额可用。按 owner 后续要求不再发起新的生图请求，也未把该验证结果写入 Demo 画布。
- 实时模型目录返回 80 个模型，包含 `gpt-image-2`、`gpt-image-2-2k` 和 `gpt-image-2-4k`；目录不返回价格字段，因此只能把基础型号作为最低价候选，不能声称已通过价格数据验证。
- PearAPI 控制台展示的余额 URL 以普通 Key 查询，但实测仍返回 HTTP 404 与“key参数不能为空”；前端页面只展示该示例文本，没有可复用的真实请求。该异常需要 PearAPI 确认实际路由或参数，不影响已完成的生成验证。
- 验证：后端 strict typecheck、33/33 测试和 build 通过；前端 17/17 测试、lint 和 build 通过；差异、敏感信息、UTF-8 无 BOM 与单边强调色扫描通过。前端构建仍有单包超过 500 kB 的既有性能警告。
- 本轮未执行 commit、push、部署或发布；临时开发服务已停止。

## 2026-09-17：模型画幅能力、空库 schema 与 Agnes 复杂 Demo


- owner 明确当前为快速开发期，数据库不保留。服务停止后已删除整个 `backend/data`，其中 SQLite、WAL/SHM、媒体 storage 与旧备份均不可恢复；删除旧 `migrate.ts`、`runMigrations.ts` 和 `migrate` 命令，`src/db/schema.ts` 成为唯一结构 owner。后端测试通过 `pretest -> clean:data` 每次先删除数据目录。
- 模型能力合同新增画幅比例。PearAPI 公开丰富目录实测返回模型级 `aspect_ratio`，适配器将它与 `/v1/models` 的凭据可用目录按模型和渠道求交集；Agnes `/v1/models` 不返回比例，改由 Agnes 适配器复用真实协议中的图片、Video 2.5 和 Video 2.0 比例集合。
- 图片请求拆出独立 `aspectRatio`，修复过去把 `9:16` 塞进 `size` 后被 Agnes 回退成 `1:1` 的缺陷。前端按具体模型展示比例，切换模型清理无效值；后端在创建生成记录和收费任务前校验模式、参考图数量与比例，未知能力不会静默放行。
- 参考 `reference/aicon` 的服务端模型能力目录与模型切换规则、ComfyUI 的节点参数归属；拒绝 Huobao 的项目级固定比例，因为同一无限画布允许不同模型和媒体节点。没有复制第三方代码、品牌或视觉。
- Agnes Demo 断裂式升级为《雾港回声》：18 个节点、27 条曲线连线、5 个工作流组，包含 2 角色、2 场景、2 道具、3 个分镜图与 3 个分镜视频分支。提取节点保存全部记录 ID，分支通过显式序号写回正确记录；分镜数量成为显式节点参数并由后端限制。
- 真实 Agnes 验收从空库开始：同步到 6 个文本、3 个图片、3 个视频模型，图片模型 8 种比例、Video 2.5 五种比例、Video 2.0 六种比例。免费文本接口两次返回 429，按节点串行重试后成功；最终得到 2 个角色、2 个场景、3 个道具、3 张分镜图、3 段 `9:16` 分镜视频，整集合成任务 completed 且剧集有真实最终视频。PearAPI 未发起任何媒体生成。
- 本轮所有新增界面仍使用完整边框；没有单边强调色、状态 rail 或伪元素侧边条。未执行 commit、push、部署或发布。

## 2026-09-17：画布保存合同断裂式重构


- 调研 `C:\Users\Administrator\Desktop\repository\mo` 的 `StorySaveCoordinator`、`WorkspaceRepository`、Web Locks、BroadcastChannel 和编辑器接线：确认成熟保存必须同时处理不可变快照、防抖合并、写入串行、expected revision、失败保留、冲突停止与离开保护。potato 的持久化 owner 是 Node/SQLite，因此没有照搬 IndexedDB、浏览器锁或本地广播。
- 对照 AICON 的节点级 300ms 防抖和生成前 flush、tldraw 的 document/session 分离、ComfyUI 的 revision dirty 语义；拒绝 AICON 仅记录控制台错误且无冲突保护的做法，也没有引入超出单机产品需求的 CRDT。
- 删除“900ms effect 直接 PUT”的旧保存主干，新增独立 `CanvasSaveCoordinator`：规范快照指纹去重、700ms 防抖、单 Promise 串行写入、保存中继续排队、失败快照保留和显式 idle/dirty/saving/error/conflict 状态。生成、手动保存和画布内部导航统一 flush，待保存内容触发浏览器关闭提示。
- 画布快照成为唯一 owner：`canvas_layout` 内含节点、坐标、连线、工作流组和模板；删除重复的顶层 `workflow_groups` 写入。React Flow 的节点/连线 selected、dragging 等瞬时 UI 状态不入库。
- 当前唯一 schema 新增 `dramas.canvas_revision`。保存接口必须提交 `expected_revision`，ProjectService 在 SQLite 事务内 compare-and-set 并递增版本；旧页面收到 HTTP 409，不能覆盖较新画布。没有迁移、兼容参数或双写路径。
- 回归测试先证明旧实现缺口，再验证连续快照只保存最新值、写入严格串行并递增 revision、网络失败可重试、冲突停止后续覆盖、选择态不持久化和服务端旧 revision 不改写数据。前端 24/24、lint、build 与后端 strict typecheck、36/36、build 均通过；前端仍有单包超过 500 kB 的构建警告。
- 官方 npm 审计发现旧版 `adm-zip`、Express 依赖链、`js-yaml` 与 `sharp` 存在高/中危告警；分别升级到 0.6.1、4.22.3、5.4.2 与 0.35.4，移除多余的 `@types/js-yaml`，并按 sharp 要求把 Node 下限收敛为 20.9。升级后前后端审计均为 0 漏洞，完整类型、测试和构建重新验证通过。
- 本轮保存改造未重新调用任何付费媒体模型；此前同一交付中的 Agnes 复杂 Demo 真实全链验收仍有效。未执行 commit、push、部署或发布。

## 2026-09-17：数据清理触发规则纠正


- owner 澄清“需要时直接删除 data”是对执行者的显式操作要求，不是每次测试或启动都清库。移除 `dev:fresh`、`clean:data` 与 `pretest`，普通启动、测试和构建不再删除 `backend/data`。
- 根 `start.py` 移除已经不存在的 `migrate` 调用，只分别启动前后端开发服务。迁移体系仍保持删除状态，不增加兼容命令。
- 后端测试使用内存 SQLite 与系统临时目录，不依赖自动删除日常数据库；今后只有 owner 明确要求清空时，执行者才在停止服务并确认目标后手工删除 `backend/data`。
- 验证：启动器进程参数仅包含前后端 `npm run dev`；后端 36/36 测试、严格类型检查与构建通过，测试后 `backend/data` 仍保持原先的不存在状态；活动入口静态扫描未发现迁移或自动清库调用。

## 2026-09-17：短剧生产语义与可停止运行断裂式重构


- 删除画布旧的混合节点合同，改用稳定生产角色和统一生产目录派生材料、阶段与资产分类，不保留旧字段兼容或双写。
- 默认模板升级为 11 节点、13 条曲线连线的完整生产链；Agnes Demo 升级为 v7 并使用相同合同。节点创建入口收敛为单一加号菜单，按生产链与通用材料分组，真实浏览器截图确认不再遮挡最左侧故事节点。
- 新增后端文本提示词目录和 `GET /api/v1/production/text-prompts`。六类系统提示词在检查器完整可见、可编辑、可恢复默认，用户覆盖通过 `system_prompt` 到达 Provider；默认正文仍只由后端目录拥有。手动剧本运行会保存到关联剧集，提取和分镜可通过 `source_text` 直接消费连线上游文本。
- 新增单一 `CanvasRunSession`：记录停止信号和当前任务 ID，停止时调用任务取消接口、结束轮询并阻止后续节点提交。取消信号传播到文本、图片、视频、Provider 重试等待和 FFmpeg；后端在结果落库前再次检查取消，迟到响应不能把已取消任务改成成功。第三方不提供撤销协议时仍无法承诺其远端计算物理停止。
- 修复两条审查中发现的真实链路缺口：AI 文本节点重跑时重新读取上游内容，不把上次输出当输入；角色/场景/道具资产图的专用接口现在接收节点提示词覆盖和参考图，图生图不再退化为文生图。检查器允许明确选择关联资产或分镜记录，手动选择会覆盖自动序号匹配。
- 新增或更新节点目录、默认模板、Demo、拓扑、连线输入、手动剧本、资产图参考图、保存快照、媒体展示、运行停止、提示词覆盖、取消后不落库和 Provider 重试取消测试。未调用真实供应商模型。
- 空库启动验收：后端返回健康状态和当前中文提示词目录，前端首页 HTTP 200；临时项目由浏览器初始化出 11 节点、13 连线和 1 个工作流组，节点均使用当时的当前合同。临时项目随后删除，前后端服务已停止；启动产生的空 SQLite 结构保留。
- 本轮未执行 commit、push、部署或发布。浏览器验证覆盖 1440×1000 桌面画布；移动端和真实供应商生成未在本轮复验。

## 2026-09-17：唯一生产主干、模块化错误与产品身份收口


- 物理删除旧画布节点目录、旧编排器、旧工作台生成服务和旧生成 API；节点快照只保存稳定生产角色 `role`，材料、生产阶段、资产分类、文本动作与默认生成方式全部由 `features/production/catalog.ts` 派生。读取画布不识别、不升级也不报错提示旧节点结构；空数据库只生成当前合同。
- 前端执行器把手动文本、AI 文本、图片、视频和整集合成翻译成统一生产命令；后端 `ProductionWorkflowService` 成为唯一编排入口，HTTP 字段解析、领域编排、底层生成服务、Provider 适配、任务和持久化各自独立。核心测试确认文本、文生图、图生图、文生视频和图生视频全部经过同一生产主干。
- 建立前后端统一错误合同：保留机器错误码、HTTP 状态、供应商和可重试事实，集中展示 400、401、403、404、408、409、413、422、429、500、502、503、504、网络、超时、配置和能力错误。批量运行首次失败即终止当前范围，未开始节点不提交；429 只做短暂有界重试，耗尽后由用户手动重跑。
- 产品身份从旧名断裂式统一为 `potato`：npm 包、页面标题、应用品牌、根配置、SQLite 文件名、导出文件、启动日志、文档、测试临时目录和仓库 skill 均已同步；磁盘根目录按 owner 要求暂不改名。Git 远端切换到 `https://github.com/luckymaomi/potato.git`。
- 确定性验证：前端 25/25 测试、lint、build 通过；后端 44/44 测试、全局 strict typecheck、build 通过；前后端依赖审计均为 0 漏洞。实际启动返回新应用名和 0.0.1，页面标题、六类提示词、项目 CRUD 与 Vite API 代理通过；临时项目删除，服务停止。
- 主实现提交 `9f24838` 已推送新远端 `origin/master`；私有配置、运行数据库和媒体目录均未进入提交。
- 本轮未调用真实供应商、未部署、未发布。前端构建仍有约 1.47 MB 单块警告；第三方不支持撤销时，本地停止不能物理撤回已经到达供应商的计算。

## 2026-09-17：节点插件、自治上下文与资产仓库重构前调研


- 问题：当前生产目录只拥有部分角色元信息，执行器仍按直接上游材料和角色猜文本、图片及记录 ID；角色、场景、道具和分镜虽已落库，却没有统一的稳定引用与上下文解析合同。
- 假设：成熟节点工作台会让节点类型自己声明输入输出与执行能力，短剧领域则需要在端口数据流之外增加按稳定 ID 查询的资产仓库。
- 尝试：在固定本地提交上核验 tldraw Image Pipeline、ComfyUI frontend、AICON 和 Huobao Drama 的节点定义、注册、模型目录、资产关系、执行状态、错误与恢复实现，并与当前 `catalog.ts`、`executor.ts`、实体/分镜服务及统一生产入口交叉对照。
- 证据：tldraw 与 ComfyUI 均由集中节点定义投影创建、控件和执行；AICON 的模型能力由服务端 catalog 拥有且生成历史绑定资源 ID；Huobao 以项目级资产、分镜关联表和显式业务 ID 读取本镜头上下文。当前仓库的 `prepareNodeForExecution` 只看直接上游，而实体与分镜 replace 服务会先删后建，可能破坏媒体和引用。
- 结果：研究证据已足以进入断裂式实现；生产插件目录、节点输入合同和资产仓库分别成为唯一 owner，连线只保留数据流方向。真实 Agnes 可用性仍须在实现后通过证据闸门核验。
- 淘汰路线：不采用纯端口上下文、按祖先角色猜测、旧字段兼容、双写、ComfyUI 迁移层、tldraw 并行策略、Huobao 全量替换和进程内任务状态。
- 留下边界：保留现有统一后端生产主干、SQLite 任务、错误合同、首次失败终止与取消；owner 随后明确把本地媒体归档、生成历史、带媒体导入导出与《雨夜外卖》全链真实验收纳入同一交付，仍不扩张到配音、字幕或完整剪辑。详细来源与采用边界见 `reference/README.md`。

## 2026-09-17：从单链纠偏为资产复用 DAG，并收口本地媒体历史


- 失败证据：首版《雨夜外卖》把 37 个节点放在同一水平线并以 36 条相邻边串成一条长链。owner 的实际截图证明这种结构看不出资产一对多、镜头多输入和合成汇聚，且把数据依赖误画成时间轴。
- 取舍：删除线性工作区构造器和“边数 = 节点数 − 1/同一 y/中间节点一进一出”合同，改为分层 DAG。当前 Demo 仍为 37 个节点，但有 80 条真实依赖边和 1 个工作流组；镜头 6/7/9/10 的资产入边、10 路视频汇入合成及整图无环由结构测试保护。
- 执行：上下文解析只读取插件声明和直接入边；资产图节点的结果携带稳定资产 ID 与本地图片，分镜图按实际连线取得参考图。全图继续拓扑排序，任何直接上游未完成时明确拒绝，不把未满足依赖当作可运行。
- 资产生成：同一角色/场景/道具插件支持文生图与图生图；图生图命令可同时携带文本提示词和多张参考图，数量由实时 Agnes 模型能力再次校验。
- 媒体历史：图片、镜头视频和 FFmpeg 整集合成都建立不可变 generation；角色/场景/道具、分镜和剧集只更新当前版本指针，检查器可选择旧版本。供应商 URL 只作来源记录，本地归档或本地合成失败不会标为完成。
- 项目归档：确认 `multer.memoryStorage()` 与 50MB 限制没有业务依据，且 `AdmZip(path)` 内部仍会整包 `readFileSync`。导入现改为上传流直写临时磁盘、逐 ZIP 条目流式解压；导出也改为媒体文件流式写 ZIP。两端均无人工 ZIP 大小上限并在成功或失败后清理传输文件。
- 确定性验证：前端 30 项测试、lint、build 通过；后端 51 项测试、strict typecheck、build 通过。真实《雨夜外卖》Agnes 全链、隔离空库启动和浏览器视觉验收仍待收尾，不能用历史成功或 Mock 替代。

# 2026-09-17：节点自治、显式数据流、Everything 日志与可恢复 PearAPI 验收


- 真实失败证据表明项目只写入 `demo_version=12`、项目和剧集，却没有角色、场景、道具、分镜与媒体；初始化脚本只看版本号提前返回，错误地把半成品当完成。回归测试现保护“同版本半成品原位补齐、重复初始化不创建第二个项目、不覆盖完整画布”。
- owner 最终明确节点自治合同：面板参数是完整输入，连线只追加已经完成的直接上游数据；单节点不补跑祖先，未完成上游不阻断，后端也不得从资产仓库补提示词或参考图。图片、视频和合成服务已删除隐式仓库取数，整集合成只接受显式视频列表。
- Demo 升级为 v13：46 个节点、66 条边、1 个工作流组。10 个逐镜文本节点和实际入画资产图共同进入对应分镜图，分镜图进入视频，10 路视频汇入合成；资产图节点不再连接提取节点。媒体固定为 PearAPI `gpt-image-2` 与 `grok-imagine-video`。
- 新增默认位于 `backend/logs/everything.log` 的 JSONL Everything 日志，覆盖 HTTP、项目/剧集/画布 CRUD、生产命令、任务、供应商、本地归档、合成、初始化与验收。Key、Authorization、token、secret、查询串、Data URL、Buffer、循环对象和超长值统一脱敏，日志不放在 `backend/data`。
- 新增 `npm.cmd run accept:rainy-night-demo`：可恢复初始化、刷新 PearAPI 模型目录、跳过本地有效 generation、逐节点运行和保存，对 429/暂时性错误做有界重试，最终只验证任务与本地媒体可用，不断言随机生成内容。
- 确定性验证：前端 35/35、lint、build 通过；后端 58/58、strict typecheck、build 通过。owner 最后要求保持 `backend/data` 不存在并亲自执行真实验收，因此本轮没有初始化真实库、没有启动服务、没有调用 PearAPI；真实外部结果仍未知。

## 2026-09-17：运行进度、检查器收口与 Demo 画布降噪


- 后端任务本来已经拥有真实 `progress/message`，但前端只读取状态。本轮把百分比和“提交生成、供应商处理、本地归档、整集合成”等阶段投影到节点与检查器，并用运行范围的拓扑序显示顶部“完成数/总数、当前节点、综合进度”。
- 修复检查器的两个直接问题：模型 option 不再拼接整段能力，能力标签移到可换行区域；历史改为“本地时间 - 图片/视频短编号”并带缩略内容。检查器横向裁剪和网格最小宽度同时收口。
- 查明节点编辑时的可证明刷新来源：表单同步 effect 依赖完整 node/project 对象，字段修改和自动保存响应都会重新整表灌值。现在只在切换选中节点时灌值，自动保存不再打断正在进行的编辑。
- 删除 `generic-text` 画布插件和《雨夜外卖》的 10 个重复镜头文本节点。Demo 升级到 v14，共 36 个节点、56 条显式边和 1 个工作流组；镜头描述由分镜图和镜头视频各自面板持有。
- 资产节点增加角色/场景/道具三组视觉折叠，默认收起以降低蛛网连线噪声；折叠不写快照、不改变原始 DAG，也不形成组级隐式输入，新增资产或选择完整工作流时会展开相关节点。
- Provider 默认请求、PearAPI 图片/视频请求和 Agnes 视频轮询的单次 HTTP 超时统一为 10 分钟；模型目录仍为 30 秒，异步视频整体轮询继续使用独立配置。
- 确定性验证：前端 12 个测试文件、40/40、lint、build 通过；后端 59/59、strict typecheck、build 通过。前端仍有约 1.48 MB 单包警告。未启动服务、未创建 `backend/data`、未调用真实供应商，浏览器人工验收由 owner 接手。

## 2026-09-17：画布白屏根因删除、节点参数隔离与运行范围收口


- 真实浏览器栈显示点击节点后在 React Flow `StoreUpdater -> setNodes` 触发 `Maximum update depth exceeded`。根因不是节点点击或运行规则，而是默认折叠时 `visibleAssetGraph` 每次渲染都产生新 `nodes/edges`，内部 store 同步又触发下一轮渲染；白屏卸载页面后，运行会话 cleanup 才连带显示“所有节点已停止”。
- 对照本地浅克隆的 React Flow 官方源码 `0a1f957`：受控图按引用同步，内置贝塞尔类型是 `default`，官方自定义边使用 `BaseEdge + getBezierPath`，类型映射必须保持稳定。对照 ComfyUI frontend `31d1d3d`：真实选择 `selectedItems` 与显示用 `highlighted_links` 分离。owner 要求不再等待 n8n，因此没有把未完成下载包装成调研结论。
- 断裂式删除资产折叠模块、按钮、状态和测试；React Flow 直接消费 Zustand 原图。所有活动边改为 `default`，旧快照读取时也统一规范。直接关联高亮只计算 ID 集合，由节点和边渲染层显示，不进入多选、运行范围或快照。
- 参考图数组改为节点私有值：创建、恢复、更新和表单灌值均复制列表；切换节点会显示当前节点自己的列表，已卸载节点的异步上传结果不会写入新节点。回归测试用同一输入数组创建两个节点并验证增删互不污染。
- `accept:rainy-night-demo` 删除真实生成执行器并直接复用 Demo 初始化入口，不刷新模型目录、不提交供应商任务。真实 Agnes 工作流只由 owner 在画布中主动启动。
- 三种运行范围共用 `runWorkflow`：单节点只跑自己；运行全部按 DAG 拓扑执行所有节点，并把刚完成的直接上游结果传给下游；运行未完成只筛选无有效结果的节点。新增回归测试保护同一执行主干中的拓扑顺序和上下文传递。

## 2026-09-18：更新理解——短剧制作主链与模块边界


- 问题：owner 感觉页面组织奇怪。产品口头链是「故事 → 剧本 → 资产 → 分镜 → 成片」，但打开项目后几乎只有一张 DAG 画布；领域阶段和工作流节点被画成同一种东西，看起来像在搭生成管线，而不像在做短剧。
- 取证：对照当前 `spec.md`、路由（项目列表 / AI 配置 / 画布）、生产插件目录、SQLite 里的角色/场景/道具/分镜及关联表，以及 `reference/README.md` 中“拒绝 Huobao 阶段表单、坚持纯画布”的既有取舍。起步模板把提取节点连到资产图并保留分镜清单；《雨夜外卖》Demo 的资产图不连提取节点，也没有分镜清单节点。两条图在表达同一主链时并不一致。
- 本次只沉淀 owner 对齐后的正确理解，不改代码、不改 spec。理解如下。

### 过程（正确主链）

1. **项目 / 剧集是容器，不是文本。** 它们回答“这是哪一部、哪一集、什么规格”。真正的文本从故事和剧本开始。
2. **故事与剧本是文本来源。** 一句话描述先定“讲什么”；剧本再写成场次、动作和对白，回答“整集怎么演”。后面的人物、场景、道具都从这里提取，不能跳过文本直接凭空出资产。
3. **人物、场景、道具提取出来后，做成可复用的资产图。** 资产图是标准图加对应描述，供后面很多镜头反复引用。同一角色不能在不同镜头里换脸换装；一致性先在资产库锁定，而不是等到成片里碰运气。资产库的核心是“可复用”，不是“画布上的一个生成节点”。
4. **分镜台按镜头把前面的东西捆成一包规格。** 每一镜至少捆住四样：这一镜的剧情说明与镜头语言（景别、动作、对白）、场景、要引用的资产（既有文字身份，也有标准图）、以及生成这一镜时用的提示词。分镜台是“这一镜是什么、用什么”，提示词组装是它服务 AI 生成时的做法，不是分镜台的全部身份。
5. **分镜图是这一包规格生成出来的单镜画面，不是分镜台本身。** 分镜台是输入包；分镜图是这一镜的静帧/关键画面结果。一镜对应一张分镜图。不是先生成一张大图，再把大图切成很多镜头。
6. **分镜图再生成镜头视频，多段镜头视频按序合成成片。** 中间不是“分镜图直接变成成片”，而是：第 N 镜规格 → 第 N 张分镜图 → 第 N 段镜头视频 → 第 1 到 N 段按序合成。

### 例子：《雨夜外卖》镜头 6

- 故事（文本）：暴雨夜，外卖员小林送错外卖，闯进女总裁苏晴的办公室，两人因此认识。
- 剧本（文本）：第四场总裁办公室。门被推开，小林拎着外卖箱进来；所有人愣住；苏晴抬头。
- 从全剧提取并做成资产图（可复用，不专属于这一镜）：人物有小林、苏晴、股东甲、保安；场景有雨夜街道、写字楼大厅、总裁办公室；道具有外卖箱、合同、电动车。办公室和外卖箱会在多个镜头重复出现，所以它们属于资产库，不属于某一个分镜图节点。
- 分镜台把镜头 6 捆成一包：剧情是“意外闯入、门口全景、众人愣住”；镜头语言是办公室门口全景；引用的资产托盘是小林、苏晴、股东甲、总裁办公室、外卖箱；再加上这一镜自己的生成提示词。这一包同时有文字和图片引用。
- 用这一包生成 **分镜图 6**（这一镜长什么样）。
- 再用分镜图 6 生成 **镜头视频 6**。
- 镜头 1 到 10 各自走完“分镜台 → 分镜图 → 镜头视频”后，按顺序合成《雨夜外卖》成片。镜头 7、9 只引用苏晴和办公室，镜头 10 再带回小林与外卖箱；复用的是同一套资产图，不是为每一镜重新发明角色。

### 模块对照与和当前实现的差别

- 项目 / 剧集：容器。剧本台：故事与剧本等文本。资产库：可复用的人物/场景/道具标准图。分镜台：按镜头打包剧情、资产托盘和生成提示。生产：产出分镜图与镜头视频。成片台：多镜合成。
- 成熟短剧是先组织“项目里有什么”（剧本、资产、镜头规格），再决定“怎么生成”。当前 potato 更像先组织“怎么生成”的 DAG，领域对象藏在节点结果和 SQLite 里。后端已有角色、场景、道具、分镜及分镜–资产关联表，数据上能支撑按模块拆视图；页面上仍是纯画布。
- 与实现的关系：上述是信息架构共识，**本轮未改代码**。是否拆出剧本、资产库、分镜台等独立视图，仍待 owner 明确授权后再做。
- 验证：仅文档沉淀；未跑测试、未调用供应商、未修改 `backend/data`。

## 2026-09-18：未知模型能力放行与固定模型选择


- 修复 `gpt-image-2` 因目录没有声明画幅比例而在本地终止的问题。模型的空模式列表、未知参考图上限和未知画幅列表现在都表示“目录未说明”，请求参数原样交给供应商验证；只有目录明确声明的模式、上限或比例与请求冲突时才提前拒绝。
- 节点显式模型和 AI 配置页全局预设改为固定选择，不再经过兼容性过滤后静默切到其他模型。仅在两者都未设置时自动择模，自动路径优先已确认兼容模型，没有已确认候选时允许使用能力未知模型。
- 检查器不再隐藏模式未知的媒体模型；选择画幅能力未知的模型时保留节点当前比例，并提供通用比例选项，不再禁用画幅控件。
- 回归测试真实穿过图片生成服务，确认显式 `gpt-image-2` 与 `9:16` 原样到达 Provider 请求。确定性验证：后端 66/66、strict typecheck、build 通过；前端 17 个测试文件 60/60、lint、build 通过。前端构建仍有既有的单包超过 500 kB 提示。未调用真实供应商、未运行 Demo 初始化、未修改 `backend/data`。

## 2026-09-18：PearAPI 新协议断裂式收口与 15 秒视频默认值


- 以 owner 提供的 PearAPI 对接文档为当前协议事实源。Grok Imagine Video 1.5 固定通过 `POST /v1/video/generations` 创建、`GET /v1/video/generations/{task_id}` 查询，Bearer 鉴权；请求映射收口为 `mode`、`seconds`、`aspect_ratio`、`images`，删除可配置旧端点以及 `duration`、`reference_contents` 兼容发送路径。
- 模型能力按文档声明 Grok 1.5 支持 4/6/8/10/12/15 秒。《雨夜外卖》Demo 升级为 v19，镜头视频默认值改为 15 秒，十个镜头对应剧集预估时长改为 150 秒；通用视频节点与起步模板不受影响。
- 确定性验证：后端 65/65、strict typecheck、build 通过；前端 17 个测试文件 59/59、lint、build 通过。没有调用真实 PearAPI、没有执行 Demo 初始化，也没有修改 `backend/data`；真实供应商与浏览器人工验收未执行。

## 2026-09-18：PearAPI 长任务续等、手动完成开关与旧结果恢复


- Everything 取证确认图片任务 `a0b648dc-1239-4198-97ec-ca6834409ff1` 在约 5 分钟时因 Undici `HeadersTimeoutError` 失败，而 PearAPI 工作台仍在生成。根因是 PearAPI 图片使用同步 POST，加上 Node 默认响应头超时先于应用的 10 分钟 AbortController 生效，不是供应商明确失败。
- PearAPI 图片改为固定异步提交，取得供应商任务 ID 后由统一图片运行时持续轮询；文本、图片和视频生成请求不再设置本地应用、响应头或响应体超时，图片、视频和前端任务状态也不再按累计时长失败，只接受供应商明确终态或用户主动停止。轮询的暂时性网络错误继续等待，分别写入 `provider.image.poll.retry` 与 `provider.video.poll.retry`；初次生成请求不盲目重发，避免任务已受理但响应丢失时重复扣费。
- 画布顶部、运行中节点和检查器使用同一本次运行起始时间，每秒显示真实经过时间；耗时只作观察信息，不写入画布 Store，也不伪造供应商百分比。
- 节点数据新增持久化 `manuallyCompleted` 开关。开启后单节点、下游、整个画布、工作流组和运行未完成都会跳过该节点，关闭后恢复；它只影响运行范围，不伪造结果，未真实完成的节点也不会向下游注入数据。节点卡片和检查器明确显示“已标记完成”，复制节点会重置该开关。
- 已有可复用结果的节点重跑停止或失败时，恢复旧 `result/history/assetRefs` 和 `completed` 状态，并清除本次临时任务标识；本次错误仍可见，不再把旧剧本或旧媒体误归为未完成。
- 确定性验证：前端 17 个测试文件、56/56、lint、build 通过；后端 66/66、strict typecheck、build 通过；Provider Skill 校验通过。未调用真实 PearAPI/Agnes，未运行 Demo 初始化，未修改 `backend/data`；真实 PearAPI 长任务、无本地截止线与浏览器计时交互留给 owner 验收。

## 2026-09-18：界面化的文本、图片与视频全局模型预设


- AI 配置页新增同屏的文本、图片、视频三个默认模型选择器；选项来自已启用、已配置供应商的实时模型目录，三项均可清空。失效旧值会明确显示为不可用，用户可以清除，不会伪装成有效模型。
- 新增 `ai_model_presets` 和 `/api/v1/ai-configs/model-presets` 读写合同，只保存供应商 ID 与模型 ID，不接触或回传 API Key。保存采用一次事务替换三类预设，并写入 `ai.model-presets.updated` Everything 审计事件。
- 运行时优先级收口为：节点显式选择 → 界面全局预设 → 首个满足当前生成模式、参考图数量和画幅的实时模型。模型选择不再写入 `config.yaml`；全局预设与当前节点需求不兼容时仍按能力选择，不会强行提交不支持的模型。
- 《雨夜外卖》升级为供应商无关的 Demo v16：移除项目、工作流组及全部 AI 节点中的 Agnes 名称、供应商和模型预设；用户可在 AI 配置页选择 Agnes 或 PearAPI 全局默认，也可留空自动选择。
- 确定性验证覆盖可空值、供应商/模型联合编码、禁用供应商过滤、失效值显示、服务端类型校验、预设持久化、显式节点覆盖及实时目录能力回退。前端 49/49、lint、build 通过；后端 62/62、strict typecheck、build 通过。未刷新真实目录、未调用供应商。

## 2026-09-18：检查器重入崩溃、真实阶段进度与 Skill 职责拆分


- owner 给出稳定复现：选择图片节点打开侧栏 → 点击画布空白取消选择 → 再次选择同一节点时页面崩溃。浏览器原始栈的首发证据是检查器 Form 深比较报告循环引用，随后 Ant Design `EllipsisMeasure` 进入无限 layout 更新；React Flow `StoreUpdater` 和“所有节点已停止”是异常卸载后的连锁结果，不是首发根因。
- 检查器改为按选中节点挂载独立编辑器和独立 Form 实例，取消选择即卸载；Form 只接收复制后的 `title/parameters`，不再接收结果、历史、执行状态或资产对象。任务 ID 和模型名称改用普通 CSS 截断，删除检查器路径中的 Ant Design Ellipsis 测量。重复选择同一节点不再发布无意义 Zustand 更新。
- React Flow 继续直接消费 Zustand 原始 `nodes/edges`。画布和检查器各有局部错误边界，显示模块错误不会直接白屏整个页面；删除运行会话的组件卸载自动停止逻辑，运行只由用户主动停止、任务取消或真实失败结束。React Flow attribution 隐藏配置一并移除。
- 任务进度区分阶段和百分比：提交、排队、供应商生成、本地归档和合成都可以只报告阶段；只有 Provider 明确返回数字时才显示百分比，完成态使用确定的 100%。删除后端固定 5/10/25/85/96 和视频按轮询次数推算进度，也删除前端提交时的伪 0%。
- 仓库 Skill 从 `project-dev/plan` 两个大入口拆出画布 UI、生产管线、Provider 集成、媒体归档、可观测性和验证交付六个职责模块；所有八份 Skill 均通过 UTF-8 模式的 `quick_validate.py`。
- 确定性验证：前端 14 个测试文件、47/47、lint、build 通过；后端 62/62、strict typecheck、build 通过。未启动服务、未运行 Demo、未调用真实供应商；浏览器中的三次点击复现仍交给 owner 人工复验。

## 2026-09-18：视频模型按次/按时长能力收口


- 真实 Everything 日志确认：PearAPI `grok-imagine-video-1.5` 的失败是供应商明确返回“当前模型不支持该时长”，不是本地归档或前端超时；失败节点的 Demo 提示词还带有固定“时长4秒”文字。
- Provider 模型能力新增 `durationMode`：`duration`、`per-request`、`unknown`。Agnes 视频模型声明按时长；PearAPI 目录优先读取明确的计费/时长字段，并对已核验的 Grok 模型声明按次，缺少证据的模型保持未知。
- 画布检查器仅对当前明确支持按时长的模型显示时长输入；PearAPI 适配器继续只发送文档合同中的 `prompt`、`model`、`aspect_ratio`、`key`、`images`。视频服务落库和提交前会丢弃按次/未知模型的通用 `duration`。
- 《雨夜外卖》Demo 视频提示词移除固定时长文字并升级模板版本，避免按次模型被提示词误导。没有调用真实供应商，也没有运行 Demo 初始化或修改 `backend/data`。
- 验证：后端动态目录、PearAPI Provider、视频服务回归测试通过；后端 strict typecheck 通过；前端模型能力测试、TypeScript 构建通过。真实 PearAPI 复验和浏览器人工验收留给 owner。

## 2026-09-18：雨夜外卖分镜图改为九宫格故事板


- 《雨夜外卖》Demo 升级到 v18。初始化的 10 个分镜图节点使用 `1:1` 方形画幅，提示词明确包含九宫格故事板和 `3x3` 九宫格构图；资产图继续使用 `9:16`，镜头视频继续使用 `9:16`。
- 验证：新增 Demo 工作区回归断言覆盖分镜图画幅与提示词，以及资产图/镜头视频画幅不被联动修改；未运行 Demo 初始化、真实供应商或媒体生成。

# 2026-09-19：领域工作台断裂式收口


- 项目主路径收口为剧本、人物/场景/道具、分镜台、生产；项目打开后进入剧本。
- 删除前端节点编排页面、旧关系线、工作流组、React Flow 依赖和旧生产节点目录。生产依赖由领域 ID、资产托盘和有序镜头列表表达。
- 删除后端项目布局保存接口、项目布局版本字段和归档中的布局重写逻辑；当前数据库按新结构直接建立，不提供旧布局兼容。
- 分镜台改为镜头带、中央分镜画面、素材托盘和右侧镜头设置；生产改为镜头卡片、批量按钮和按序合成。
- 资产与参考图控件统一按列表多选处理：可以不选、选一个或选多个，落库和生成始终使用 ID/URL 列表。
- 供应商适配器保持独立协议；阶段页面只提交领域输入，不暴露供应商协议细节。

本轮未调用真实供应商，未执行部署、提交或推送。前端构建、后端类型检查和后端测试已验证；浏览器人工验收仍待执行。

# 2026-09-19：四阶段制作工作台体验收口


- 项目内主导航收成总览与剧本、资产图、分镜台、生产四个阶段；人物、场景、道具改为资产图工作台内的筛选维度，全局资产库改为资产图内的子视图。
- 项目壳统一桌面可视高度和内部滚动；剧本、资产图、分镜台、生产不再各自维护不同的视口公式。移动端改为自然纵向布局。
- 资产图首页改为标准图大图画廊；点击资产进入制作抽屉，保留名称、图片描述、依赖、参考图、生成、锁定、升级、删除和历史。项目资产与全局资产均支持自由输入的多标签，标签去重保存，并随全局资产加入项目及 ZIP 归档往返保留。
- 分镜台改为横向镜头带、画面制作区、三类并排素材托盘和镜头设置区；人物、场景、道具均通过缩略图自由多选。浏览器验收发现未注册的表单字段会导致资产 ID 保存时丢失，随后改为保留观察并读取完整表单存储；第 6 镜绑定小林、苏晴、股东甲、总裁办公室、外卖箱后，刷新仍完整回显。
- 生产区使用稳定比例的分镜图/镜头视频双媒体卡，不再固定为 138px；批量出图、费用确认后出视频和按序合成规则没有改动。
- Demo 验收数据增加了“小林 / 主角”标签，并补齐第 6 镜的五项资产引用；未提交任何图片或视频生成任务。
- 浏览器验收覆盖 1440x1000 桌面和 390x844 移动视口；四个移动页面的文档宽度均为 390px，没有横向溢出。静态扫描确认前端没有单边边框、伪元素侧边条、装饰性标题文案、React Flow、`@xyflow` 或 `canvas-layout` 残留。
- 验证：前端 16/16 测试、构建通过，lint 无错误且保留 6 条 `set-state-in-effect` 警告；后端 strict typecheck、构建和 73/73 测试通过；`git diff --check` 通过。前端构建仍有单包超过 500 kB 的性能警告。
- 本轮未调用真实供应商，未执行 commit、push、部署或发布；本地前后端服务保持运行，供页面验收。

# 2026-09-19：资产制作间与 CRUD 收口


- 资产页由抽屉式详情改为资产浏览区与标准图制作区：标准图、图片描述、依赖、生成输入、参考图和历史版本在同一工作区完成。
- 参考图支持多张上传、逐张移除和再次生成；依赖资产支持跨人物、场景、道具按 ID 列表保存。
- 补齐项目资产删除接口和确认操作，删除会按当前 schema 清理分镜托盘与依赖关系；不改变历史 generation 记录的保留规则。
- 补充删除级联回归测试；前端构建、前端测试、后端类型检查、后端构建和后端测试已再次通过。

本轮仍未调用真实供应商或部署；本地服务健康检查和浏览器人工验收待完成。

本轮收口：本地健康检查已通过；提交与推送按 owner 授权执行。真实供应商和浏览器人工验收仍未执行。

更正：本地提交已完成，向 `origin/master` 的两次推送均因远端连接重置失败；未宣称远端已更新。

# 2026-09-19：首页与剧本编辑区定高收口


- 关闭旧前后端进程和 Edge 页面后，在空库中初始化《雨夜外卖》Demo；项目 ID 为 2，第 1 集 ID 为 2，未调用任何图片或视频供应商。
- 对照本地 `reference/moyin-creator` 的固定外壳与内部滚动方式，项目首页改为固定高度卡片和视口内项目滚动区；项目再多也不会继续撑长整个页面。
- 总览与剧本页改为左右等高编辑区，移除标题下说明句和重复字段标签，增加实时字数；长剧本只在对应编辑器内部滚动，窄屏恢复纵向布局。
- 1440×1000 Edge 实图确认：左右面板同高同底，397 字剧本出现内部滚动条，页面本身无外层滚动；首页 Demo 卡片高度固定。
- 验证：前端构建、16 项测试通过；Lint 无新增诊断，仍有 6 条既有 effect 警告；前后端健康接口均返回 200。

本轮未调用真实供应商，未执行提交、推送、部署或发布。

# 2026-09-19：参考 README 图片取证与窄屏壳修正


- 从 `reference/moyin-creator/README.md` 提取 14 个界面图片外链，并通过可用图片代理下载到 `reference/moyin-creator/readme-images/`；按文件签名将 3 张 JPEG 改为 `.jpg`，其余保留 `.png`。未将第三方源码或品牌素材拷入产品主干。
- 实际查看参考资产页与导演台图片，确认窄侧栏阶段导航、资产目录/画廊/详情、左镜头列表/中间画幅/右属性/底部素材区的结构证据；本项目桌面截图已按同一层级复核。
- 浏览器取证发现 390px 窄屏项目壳仍沿用横向主布局，导致内容被阶段栏挤出视口；新增窄屏纵向壳、单列资产/生产画廊和工具栏换行规则，桌面布局不受影响。
- 验证：前端构建通过；前端 16 项测试、后端 typecheck/build 与 73 项测试通过；`git diff --check` 通过；单边强调色、装饰英文、React Flow/canvas-layout 静态扫描无命中。真实供应商仍未调用。

# 2026-09-19：断裂式移除资产锁定与任意视频时长


- 按 owner 最新决定，资产不再有锁定标准图、升级版本或资产依赖；生成历史保留为独立记录，当前图片通过“选用这张”切换。
- 资产制作抽屉保持标准图、名称/描述/标签、生成图片、参考图和图片历史单列呈现，清理旧双列 CSS 残留。
- 分镜台不再展示或提交任意镜头秒数；视频服务仅采用模型目录明确声明的固定时长，未知能力不伪造时长。
- GPT Image 2 与 Grok Imagine Video 1.5 的文生/图生、参考图数量、画幅和固定时长说明继续由模型目录能力函数展示，不在页面另造规则。
- 本轮尚未调用真实图片/视频供应商；数据库重建和浏览器人工验收将在自动验证通过后执行。

# 2026-09-19：本地数据库重建与 Demo 重启


- 按 owner 明确授权停止本地服务，删除并重建仓库内 `backend/data`，没有触碰其他目录。
- 后端重新创建当前 schema 后运行 `init:rainy-night-demo`，新数据库中项目 ID 为 1，项目资产 10 条、分镜 10 条；未调用真实图片或视频供应商。
- 后端 `http://localhost:5679/health` 与前端 `http://localhost:3012` 均返回 200；浏览器已打开 `http://localhost:3012/film/1/assets?episode_id=1`。
- 自动验证：后端 typecheck/build/73 项测试通过，前端 build/16 项测试通过，lint 仅保留既有 6 条 `set-state-in-effect` 警告，`git diff --check` 通过。

# 2026-09-19：资产图工作台去除自制框中框


- 对照已下载的魔音资产页截图，资产图改为项目阶段栏、标准图画廊、当前资产详情三段结构；移除资产子导航、抽屉式详情和类型 Segmented 容器。
- 人物、场景、道具只作为画廊顶部的平面筛选；标准图、名称/描述/标签、生成图片、参考图和图片历史在右侧同一列，保留 CRUD 与“选用这张”。
- 去掉画廊工具条的装饰性白色框，避免仅换文案而保留原有后台表单层级。
- 前端 build、16 项测试和 lint 已重新执行；lint 仍只有既有 6 条 effect 警告。真实供应商未调用。

# 2026-09-19：番茄短剧体验收口与生成计时恢复


- 产品显示名改为「番茄短剧」，主题改为浅色淡蓝；去掉首页闪电标志与项目壳四格图标、阶段号；左侧底栏改为返回项目列表。
- 首页改为可折叠项目分组，分组内展示各集入口；项目列表接口附带剧集摘要。
- AI 配置默认模型预设改为选择后自动保存，去掉「保存预设」按钮；剧本页显示当前文本模型。
- 资产图删除「从剧本提取」；保留批量生成缺失图、新建资产与「选用这张」。
- 分镜台可直接生成分镜图，去掉生产房间必经引导与画面角标；资产选中只高亮；单画面画幅取自图片模型能力目录；自定义网格拖选改为容器 pointer capture。
- 生产区去掉批量出图；批量出视频接 tasks.cancel，可停止。
- 恢复工作台生成计时：每个图/视频任务显示「已生成 X 秒」，起点用 generation/task 持久化时间，刷新后从 pending/processing 历史恢复轮询；可单项停止。
- 同步 `spec.md` / `README.md`；本轮未调用真实供应商。

# 2026-09-19：硬伤修复（深色壳 / 宫格 / 新建集 / 上传标准图 / 生产按钮）


- owner 否定浅色淡蓝壳，改为全局顶栏、项目侧栏与项目顶栏深色 `#141414`；内容区仍浅底；面包屑统一 13px。
- 分镜台宫格「点不动」根因：模型目录/`aspectOptions` 变化时整表 `setFieldsValue` 把未保存宫格打回 1×1；改为仅切换镜头时灌表，资产 ID 与画幅分 effect。
- 首页分组与项目壳增加「新建集」，复用 `saveEpisodes` 追加下一集号。
- 资产标准图支持本地上传：`images.importLocal` + `POST .../upload-image`，归档后设为当前图并进历史，历史里标「本地上传」。
- 生产页去掉「去分镜台」；单镜主按钮改为大号「生成视频」/「重新生成」，进行中显示计时与停止。
- 验证：前端 19 项测试与 build 通过；后端 typecheck 与 74 项测试通过（含上传标准图单测）。未调用真实供应商；未 commit/push。

# 2026-09-19：剧集 CRUD 补齐（重命名 / 删除）


- 反思：上一轮只加「新建集」，把剧集当成只能追加的列表项，心智模型不完整；用户对象应对增删改查齐备。
- 后端新增 `updateEpisode` / `removeEpisode`（PATCH/DELETE）；`saveEpisodes` 更新时合并已有字段，避免只改标题清空剧本。删除最后一集明确拒绝。
- 首页集行与项目壳增加重命名弹窗与删除确认。
- 验证：后端 75 项测试与 typecheck 通过；前端 19 项测试与 build 通过。未 commit/push。

# 2026-09-19：去掉分镜网格提示 UI


- owner 明确不要单独的六/九/十六宫格区；构图写在图像提示词即可。
- 删除分镜台 GridPicker 与相关说明文案；后端不再根据 grid_rows/grid_columns 追加构图句；画幅选择器保留。
- 库内 grid 字段仍默认 1×1，本轮不做 schema 断裂迁移。

# 2026-09-19：移除资产图批量生成


- owner 只要逐张点进资产详情生成/停止，不要「批量生成缺失图」。
- 去掉资产页批量按钮、前端 `batchGenerateAssets`、后端 `/assets/generate-batch`；生产批量出图接口改为明确拒绝。
- 单项「生成图片 / 停止生成」保留。
- 验证：后端 75 测、前端 19 测与 build 通过。

# 2026-09-19：生成操作友好错误提示


- 根因：工作区多用裸 `Error.message`，未走已有 `presentError`；API Key/模型目录类失败常表现为点一下无说明。
- 增强 `appError`：识别未配置密钥、模型目录未就绪等并给出「打开 AI 配置」下一步；配置类用 Modal，其余用顶部提示。
- 资产/分镜/生产/剧本等统一 `notifyAppError`；任务终态（完成/失败/停止）额外提示一次。
- 验证：前端 21 项测试与 build 通过。

# 2026-09-19：分镜图本地上传与方形预览


- 分镜图对齐资产标准图：`importLocal` 支持 `storyboardId`；新增 `POST .../storyboards/:id/upload-image` 与 `DELETE .../current-image`（只清当前指针，历史保留）。
- 分镜台预览框为正方形，按舞台剩余空间 flex + container query 自适应放大，去掉写死的 320/420 上限；资产条留在舞台底部，不再空出一整行白区。
- 验证：后端 76 项测试与 typecheck 通过（含分镜上传/清除）；前端 21 项测试与 build 通过。未 commit/push。

# 2026-09-19：移除批量出视频


- owner 只要生产页单镜「生成视频」与整集合成，不要批量出视频。
- 删除生产页批量按钮与停止逻辑、前端 `batchProduce`/`BatchSubmission`、后端 `POST .../produce/batch`。
- 验证：后端 76 项测试通过；前端 21 项测试与 build 通过。未 commit/push。

# 2026-09-19：镜头删除入口可见化


- 镜头删除后端本来就有，但 UI 只是右侧底部一个垃圾桶图标，镜头带上没有，用户会以为只能新增。
- 镜头带每项增加删除按钮；右侧改为明确「删除镜头」文案；删除后重排 `storyboard_number`。
- 验证：后端 77 项测试通过（含删除重排镜号）；前端 build 通过。未 commit/push。

# 2026-09-19：生产页模型注明与 Grok 时长档位


- 对照 owner 桌面 Pear 文档与昨日协议：Grok Imagine Video 1.5 时长档为 4/6/8/10/12/15 秒；适配器目录此前已声明，漏的是工作台把 `duration` 写死为 `undefined`，且视频服务未按目录档位校验/默认。
- 前端 `modelDurationOptions` 曾在「支持时长但未声明档位」时伪造 Grok 的 4–15 秒列表，已去掉；只展示模型目录明确声明的档位。
- 生产页展示当前分镜图/视频模型预设，并按 `supportedDurations` 提供时长选择；生成视频把所选秒数传到后端；非法档位明确拒绝，未指定则取目录首档。
- 分镜台标题区同步注明当前图片模型预设，便于成本判断（本轮不接价格数字）。
- 验证：后端 77 项测试通过；前端 catalog 测与 build 通过。未调用真实供应商；未 commit/push。

# 2026-09-19：镜头级视频参数与去掉伪造通用画幅


- owner 要求：每个镜头各自下拉选时长（一镜 4 秒、另一镜 6 秒），不要全局统一时长；图片画幅与视频画幅分开，全部从供应商模型目录读取，禁止前端注入通用比例表。
- 生产页顶部只展示模型与能力摘要；时长/视频画幅下拉下沉到每张镜头卡，提交时带该镜的 `duration` 与 `aspect_ratio`。
- `aspectRatiosFor` 不再拼接写死的 commonAspectRatios；`preferredAspectRatio` 仅保留当前值或目录首项；能力标签区分「图片画幅」与「视频画幅」。
- 分镜台画幅控件改标「图片画幅」。验证：前端 21 测与 build 通过。未调用真实供应商；未 commit/push。

## 2026-09-19：分镜规格编译器与《雾港来信》五镜头 Demo

- owner 最终确认产品链为“人工故事/剧本/资产信息 → AI 资产图 → 分镜台 → AI 分镜图 → AI 镜头视频 → 整集合成”；文本创作不再提供 AI 入口。
- 新增后端唯一组装 owner `storyboardPromptAssembler`：按稳定顺序组合主体提示词、资产文本、资产参考图和镜头语言。图片与视频共用字段映射但按用途分层，声音进入视频提示词，资产图片保持独立 references。
- 分镜图片入口和视频入口均消费 assembler；图片负面提示词透传真实 `ImageProviderRequest`，视频不伪造通用负面提示词能力。
- 分镜图 prompt 明确可表达九宫格；视频 prompt 明确要求单一连续画面，避免九宫格、分屏、网格、拼图和水印污染首帧视频。
- 移除剧本页“AI 生成剧本”、分镜台“AI 拆镜”及对应工作区路由/API 入口；人工编辑故事、剧本、资产和镜头清单。
- Demo 从《雨夜外卖》十镜头改为悬疑短剧《雾港来信》五镜头，主角为女性调查记者林岚，包含 2 角色、3 场景、2 道具和九宫格分镜图提示词。
- 新增 assembler 正向契约测试和图片负面提示词 Provider 契约测试；未调用真实供应商、未执行部署。

## 2026-09-19：参考图驱动的《红女王》Demo 与根目录便捷脚本

- owner 提供一张红色宫廷、王冠、烛光和眼镜女性参考图；Demo 改写为成人向宫廷复仇悬疑短剧《红女王》，成年女性红女王为主角，主线是十年后回到夜城、摊开旧账并夺回王位，不写露骨内容。
- Demo 保持 5 个镜头，资产为 3 位角色、3 个场景和 3 个道具；分镜图提示词明确九宫格，视频提示词明确单一连续画面并排除九宫格、分屏、网格、拼图和水印。
- 新增根目录 `init_demo.py`，调用后端唯一初始化入口；新增无参数 `clear_database.py`，以仓库根目录为基准直接删除 `backend/data` 整个本地数据目录。
- Demo 初始化不再用通用默认镜头语言覆盖定义中的景别、机位、运镜、构图、光线、氛围和声音，镜头规格按 Demo 定义写入。
- README 与 Demo 打开入口同步改为当前 Demo 及根目录 Python 命令；未调用真实供应商，未执行部署。

## 2026-09-19：资产图、分镜图和视频逐项运行未完成

- 三个媒体工作区新增独立的“生成未完成”入口：资产图按资产顺序逐项提交，分镜台按镜头顺序逐项出图，生产页只按“已有分镜图但没有视频”的镜头顺序生成视频。
- 每个队列一次只提交一个任务；当前任务进入完成、失败或停止终态后才进入下一项，提交失败或供应商失败会跳过并继续，用户停止会取消当前任务并阻止后续提交。
- 队列顶部投影当前名称/镜头和已处理数量；原有单项生成、计时和停止入口保留。视频队列与分镜图队列分开，避免昂贵的视频任务混入图片生成。
- 前端生成追踪器新增等待终态能力和任务名称标签，队列复用同一任务轮询与取消合同；未调用真实供应商。

## 2026-09-19：生成结果统一友好投影

- 复核历史后确认 `useAnnounceGenerationOutcomes` 是媒体任务终态的唯一前端投影，`presentError/notifyAppError` 是统一错误展示 owner。
- 生成队列移除页面内散落的成功、失败和停止 toast；任务名称、失败原因、完成和停止提示统一由终态投影处理，页面只展示进行中队列状态和当前项。
- `TrackedGeneration` 增加可选任务标签，终态提示明确对应资产、镜头或视频；未调用真实供应商。

## 2026-09-19：历史记录顺序规则

- 按 owner 要求将既有记录整理为从旧到新的时间顺序；没有删除历史内容。
- 文件顶部新增硬规则：新记录只能追加到最下面，不能再插入顶部。

## 2026-09-19：红女王终场对白写入分镜字段

- 第五镜“扶正王冠”将“这座城，从来不是你的。”写入 `dialogue` 字段，不再只放在剧情描述里；初始化回归断言保护该对白可被分镜规格组装器消费。

## 2026-09-20：按《AI 短剧工作流》断裂式重建唯一主干

- 调研 `reference/moyin-creator`、`reference/ai-short-drama`、`reference/comfyui-frontend` 和本地 Kitty 后，采用稳定字段顺序、空值过滤、项目 ID 隔离、文本/图片分通道、generation 与当前版本分离；不采用共享资产中心、多套提示词 builder、名称匹配或通用节点图。仓库及相邻目录没有 n8n 源码，因此没有补写无证据结论。
- owner 已删除数据库，确认没有正式数据迁移约束后直接重建 schema：`project_assets` 成为唯一资产表，只保存角色卡、场景卡和道具卡；移除跨项目资产库、旧 `characters/scenes/props`、旧分镜关系表和旧 generation 目标外键。
- 资产卡改为按类型校验的 `text_profile`、标准资产图生成输入 `input_reference_images`、标准图当前版本和 generation 历史。分镜新增 `extra_reference_images`，并只按当前项目资产 ID 保存关系。
- 分镜台唯一编译器一次输出 `imageRecipe` 与 `videoRecipe`。资产文本进入 prompt；资产标准图和本镜额外参考图进入 references；资产卡原始输入参考图不向分镜传播。声音和对白只进入视频提示词。
- 图片工作区入口只消费图片配方；视频入口只消费视频配方，并把已选分镜图作为独立首帧。前端生成请求不再发送最终 prompt 或 reference 数组。
- 删除通用 production 命令入口、产品文本 AI 服务/提示词目录、AI 写剧本/提取资产/拆镜执行主干，以及相应前端 API/type。Provider 目录中的 text 能力仍作为供应商客观能力保留。
- 项目 ZIP 升级为不兼容的 `format: 4`，只保存当前项目资产、分镜、generation、本地媒体和本地参考图；导入重建 ID、关系和本地路径。
- 测试先建立 5 个旧实现失败证据，再以正向合同覆盖三类卡、项目隔离、双配方、文本/图片通道、独立首帧、媒体版本和 ZIP 往返。实现阶段未调用真实供应商。
- 最终验证为后端 61 项测试、typecheck、build、lint 与前端 21 项测试、独立 typecheck、build、lint 全部退出码 0；本地无供应商烟测确认健康检查、项目列表、9 张三类资产卡、5 个分镜及前端深链可读取。保留前端 5 条 effect lint warning 和 Vite 大包体 warning；真实供应商、多参考图能力及浏览器人工视觉验收未执行。

## 2026-09-20：资产卡单 ID 语义与标准图产出层

- 对照工作流差距报告复核后确认：角色换装列表和道具状态列表会让一个资产 ID 混入多套语义；改为一个造型、一种场景光影、一种道具状态各自建立独立项目资产卡。
- `project_assets` 新增按资产类型约束的 `output_type`；角色支持 A-D 四种标准图册布局，场景支持空间全景、局部特写和独立光影变体卡，道具支持多角度图和独立状态变体卡。
- 新增资产产出提示词唯一 owner，角色布局统一约束脸、发型、服装、身高比例、五官和中性背景；资产生图仍只把用户上传图作为生成输入，分镜继续只消费选定的标准资产图。
- 项目 ZIP 随产出规格断裂升级为 `format: 5`；工作流原文同步删除跨项目素材池和单卡多变体措辞，并明确对白只进入视频配方。
- 理念收口又发现 AI 配置、模型预设和 Provider 适配器仍暴露独立文本生成能力；断裂删除该合同后，目录和配置页只保留图片/视频模型，文本创作入口归零。
- 验证：后端 62 项测试、typecheck、build、lint 和前端 21 项测试、独立 typecheck、build、lint 均退出码 0；静态检查旧资产字段、独立文本生成链、全局资产实现和前端配方组装均为 0。保留前端 5 条 effect lint warning 与 Vite 大包体 warning。
- 外部边界：未调用真实供应商，未做浏览器人工视觉验收，未清理 `backend/data`。只读检查确认当前本地数据库仍是旧 schema，启动新主干前需显式重建。

## 2026-09-20：独立产出提示词、单卡状态与移除批量出图

- owner 要求删除批量出图、公开最终提示词并显示单卡实时状态；随后明确纠正交互：直接提供独立提示词区域，去掉下拉覆盖文本的设计。删除资产页与分镜台的批量出图按钮、循环提交、队列和批量停止控制，保留单项生成。
- 调查确认原资产生成路由点击时临时编译模板，用户无法编辑最终文本。先建立持久化、生成消费和 ZIP 往返的失败证据：后端 6 处目标失败，前端缺失资产状态映射模块；再新增 `output_prompt`，由后端原样读取用户已保存的完整文本，标准资产图生成输入仍走独立参考图通道。
- 初始模板仅在建卡时填充。资产页独立编辑产出提示词，保存/生成验证非空，保留换行与空白；结构化资料变化需要用户自行同步到提示词。按卡保存页面内草稿，切卡与任务完成后的媒体刷新保留编辑内容；异步上传参考图回填原卡草稿。
- 缩略图和详情标准图覆盖显示正在提交、排队中、正在生成、归档中、已完成、失败或已停止；跟踪器以实际初始状态开始，失败提示优先使用错误原因。页面重开从历史恢复最新结果及进行中任务。
- schema 新增非空提示词列，ZIP 断裂升级至 `format: 6` 并保留用户提示词；Demo 经建卡链得到可编辑初始文本。同步 spec、前后端 README 和桌面理念文档；媒体归档规约用于保留输入图/标准图边界和按原卡回填上传结果。
- 自动验证：后端 64/64、前端 23/23 测试通过，两端 lint、独立 typecheck、build 退出码均为 0。HTTP 契约覆盖保存提示词到单次生成的真实路由；本地文件签名、可用性、阶段消息和 ZIP 往返已验证。
- 浏览器验证：使用 Chrome 无头浏览器与本地模拟 API，验证独立编辑区、资料更新保留文本、切卡草稿隔离、保存后提交、排队/生成/归档/完成/失败、任务完成刷新保留草稿、重开恢复失败结果与已保存文本。此证据不代表真实供应商出图成功，人工审美未验收。
- 静态扫描：源码中资产/分镜批量图片入口和队列、模板覆盖接口为 0；前端没有资产模板正文，后端生图路由消费 `output_prompt`。差异审查保留原有无内容差异文件，跟踪器仅提交任务状态相关改动。
- 本地数据库只读检查：当前已有 `output_type`，仍缺 `output_prompt`；未清理数据，运行新版前需重建。保留 5 条既有前端 effect lint warning 与 Vite 大包体 warning；秒级轮询可能跳过非常短的中间阶段。真实供应商未调用，测试服务已退出，本轮只做本地提交，不 push。

## 2026-09-20：资产与分镜统一改为显式点击组装

- owner 最终要求所有组装逻辑统一成可见动作：资产卡选择产出预设后点击“组装提示词”，分镜台点击“组装两份配方”；选择预设、修改资料或打开页面本身都不改写已有结果。此前“建卡自动填充资产提示词”和“生成媒体时现场编译分镜配方”的两条例外被删除，Demo 初始化也不再自动调用 assembler。
- 资产卡恢复按类型限制的预设下拉，预设与最终文本位于独立产出区域。角色删除“身份标签”；三类卡所有非枚举资料字段统一为普通输入并按字符串保存。组装结果包含角色、场景或道具产出标准，填入独立可编辑文本域；新卡提示词为空，用户也可跳过预设直接填写。
- 分镜人工主干提示词继续属于镜头规格，另增图片/视频最终提示词和各自 reference 快照，避免把上次编译结果再次当输入。显式接口一次返回两份配方；界面展示并允许修改两份最终文本及查看引用。图片与视频生成分别读取已保存快照，视频继续把当前分镜图作为独立首帧。
- schema 与领域类型新增四个分镜配方字段，资产提示词允许在组装前为空；ZIP 断裂升级为 `format: 7`，导入时重建资产 ID、媒体 URL 与两组配方引用。`spec.md`、前后端 README 和桌面《AI短剧工作流.md》同步为显式按钮合同。
- 先建立 7 个目标失败证据，覆盖新卡等待组装、显式 HTTP 接口、两份配方持久化、生成消费用户编辑结果和 ZIP 往返；修复后后端全量 64/64、前端 23/23 测试通过，两端 lint、typecheck、build 均退出码 0。
- 浏览器使用本地 Vite 与拦截 API 验证：改变资产预设不覆盖原文本，点击后标准约束可见可改；改变分镜主干不覆盖旧配方，点击后图片/视频结果同时可见可改；桌面与 390px 手机宽度无内容遮挡。验收未调用真实供应商，临时服务与文件已清理。
- 静态扫描确认运行时两个 assembler 只由显式组装接口调用，三条生成入口只消费已保存提示词；`identity_tags`、tags 输入器、资产/分镜批量出图入口、全局资产库与旧版本字段在源码均为 0。保留既有 5 条前端 effect lint warning 与 Vite 大包体提示；真实供应商效果和人工审美仍未验证。本轮按授权创建本地提交，不 push。

## 2026-09-20：整站用户界面文案收口

- owner 要求页面标题只保留短名称，删除英文装饰词、宣传副标题和向用户解释内部实现的技术文案。逐页复核项目列表、AI 配置、项目顶栏、剧本、资产、分镜、生产、弹窗、空状态与错误提示，没有用另一句宣传语替换旧文案。
- 资产卡删除“预设何时读取、文本如何保存”的两段说明，将“产出层提示词”“产出预设”“文本结构”等改为直接的“生成提示词”“预设模板”“资料”；显式“组装提示词”按钮和可编辑文本域保持不变。
- 分镜台删除“只消费保存文本”“不会伪造比例”等实现说明，把界面上的配方术语收成“提示词素材”“图片与视频提示词”“参考图”；生产页删除底部架构说明，剧本页移除已经不存在的“从故事总览生成”暗示。
- AI 配置把“根配置”“Key”“实时接口”等改成配置状态、凭据和刷新动作；首页把可见的 Demo 改为“示例”；生成历史状态统一显示中文。错误匹配仍能识别供应商返回的 `API Key`，但用户看到的是“供应商密钥”。
- Chrome 使用本地拦截数据覆盖项目页、AI 配置和四个项目工作区的 1440×900 与 390×844 视口；禁用文案命中为 0。复核时发现移动端剧集选择器挤压操作按钮，改为两行布局后按钮完整显示；阶段导航与配置表格保留可横向滚动。
- 前端 23/23 测试、lint、独立 typecheck 和 build 均退出码 0；保留既有 5 条 effect lint warning 与 Vite 大包体提示。未调用真实供应商，浏览器数据不代表后端或供应商验收。

## 2026-09-20：总览与剧本首页及项目工作区断裂式重做

- 用户要求进入项目后的首屏就是“总览与剧本”，并参考 `reference/` 重做项目入口和工作区；调研后借鉴剧集/项目层级、阶段导航和当前对象上下文，不引入参考项目中的 AI 写作、自动拆镜、批量生成或营销化界面。
- `dramas` 新增核心钩子、世界观、主线、基调和参考设定；`episodes` 新增本集目标、冲突、转折、结尾钩子和场次节拍。项目创建、剧本接口、Demo、ZIP 往返和项目隔离均同步读写这些字段；数据库已由 owner 清空，本轮直接使用新 schema，不设计迁移。
- 首页项目列表改为信息卡片网格；项目工作区改为深色侧栏加浅色内容区；“总览与剧本”页面采用单列编辑，分开故事总览、本集结构和人工剧本，保留四阶段入口及当前集上下文。
- 后端测试 65/65、typecheck、build 通过；前端测试 23/23、lint、独立 typecheck、build 通过。后端没有 lint script；前端保留 5 条既有 effect warning，构建保留 Vite 大包体 warning。
- 未调用真实供应商，未进行本轮浏览器人工视觉验收，也未执行 commit/push；owner 既有生成计时改动保持不变。

## 2026-09-20：分镜标脏、就绪门闸与项目首页进入交互

- 分镜新增持久化的图片待复核、视频待复核和配方待重装状态。资产文本/产出提示词变更、资产标准图选用、镜头规格/出场资产/额外参考图变更、分镜图选用或清除，均通过仓储与媒体服务更新受影响镜头；generation 历史不删除。新分镜图选用清除图片待复核并标记视频待复核，新视频选用清除视频待复核；用户也可按镜确认。
- 取舍：生成提交不重新组装配方。配方待重装只能由用户显式点击组装后保存清除，不能因保存旧文本而绕过；视频九宫格、分屏、多宫格污染词集中在小纯函数并由单测锁定。
- 后端生成门闸要求：分镜图需保存且未过期的配方、至少一项出场资产、出场角色/场景标准图；视频需分镜图、未过期视频配方且不得含污染词；缺少视频或存在待复核视频不能合成。没有视频主干、运镜、动作时只返回风险提示。
- 项目首页移除封面播放视觉和“打开总览”按钮，整卡点击进入；新建集只创建并留在首页，用户再显式点击剧集进入工作区。
- ZIP 断裂升级为 `format: 8`，往返保留复核状态；不读取旧格式。真实供应商未调用，未执行 commit/push。

## 2026-09-21：制作页能力投影与适配器来源审计

- 资产页读取图片模型预设并显示目录能力摘要、图片画幅选择；生成请求开始传递用户选择的 `aspect_ratio`，不再固定发送空参数。
- 分镜台显示当前图片/视频模型能力摘要和已组装参考图相对模型上限；无唯一预设时明确显示自动选择，不把多模型能力合并成确定事实。
- Provider 能力来源增加 `adapter-override`；PearAPI 的 GPT Image 2、Grok Imagine Video 1.5 覆盖字段不再伪装为纯供应商目录能力。
- Agnes Video 2.5 未指定时长时不再本地生成默认 `seconds`，也不再把请求时长静默截断到 4–12 秒；交由供应商协议处理。
- 自动验证：后端 74 项测试、typecheck、build；前端 24 项测试和 lint 通过。前端 build/typecheck 仍被既有 `ProjectShell.tsx` 三个未使用变量阻断。未调用真实供应商，未执行浏览器人工验收。

## 2026-09-21：外部媒体协议能力校正

- 复核 owner 提供的 Pear 文档后，确认 `gpt-image-2`、`gpt-image-2-2k`、`gpt-image-2-4k` 共享最多 16 张参考图和 13 种图片画幅；PearAPI 适配器统一标记为 `adapter-override`。
- Grok Imagine Video 1.5/preview 保持单张参考图、4/6/8/10/12/15 秒固定档位和 `per-request` 计费。计费维度与时长档位是独立能力，视频请求继续发送 `seconds`；比例只保留已有 16:9、9:16 核验证据，不把通用 schema 示例扩展为模型事实。
- 资产页显示输入参考图当前数量与目录上限，达到上限时禁用上传并在上传处理器再次拒绝；Agnes 能力仍按适配器事实，不添加未经协议确认的固定时长档位。
- 本轮未调用真实供应商，未执行 commit/push。

## 2026-09-21：PearAPI 协议标准化与专用 Skill

- 基于桌面 `生图生视频API/PearAPI` 三份源文档登记 SHA-256 和行数，新增 `.agents/skills/provider-api-contract`，将传输路径、鉴权、异步轮询、模型能力、Grok 专用字段和验证闸门集中到标准化参考文档。
- 主题与生成入口完成断裂式切换：前端改用新的唯一主题样式入口，支持日间/夜间模式并持久化；生成服务取消自动模型、首项画幅和首项视频时长兜底，缺少显式选择时拒绝提交。协议 Skill 参考拆分为通用、生图、生视频三份，不再依赖会被删除的桌面源文件。
- 首版新皮肤验收发现布局结构缺失，已补齐项目模式顶栏隐藏、资产库双栏与资产网格、分镜台镜头轨道/媒体区/检查器三段布局及响应式断点；主题存储键升级为 v2，首次使用新皮肤默认日间模式。
- PearAPI 适配器补全 `gpt-image-1.5`（16 张、3 种画幅）以及 Nano Banana 六个模型族（基础版 6 张/10 种画幅，其余 14 张/14 种画幅），全部标记 `adapter-override`。
- Grok 1.5 继续按专栏发送 `mode`、`seconds`、`aspect_ratio`、`images`，并保持按次计费与固定时长档位分离。

## 2026-09-21：生成媒体原文件下载

- 资产标准图、资产生成历史和分镜图的点开预览工具栏增加“下载原图”；分镜视频打开预览弹窗后提供“下载原视频”。
- 下载均使用本地 `/static/` 归档地址和带扩展名的文件名，不再占用历史卡片操作区。
- 下载入口不改变预览、选用或生成历史，未调用真实供应商。

## 2026-09-21：主题布局重建 + 生成全手选 + 刷新分项计数

- 以已删除的 `HEAD:frontend/src/index.css` 为几何事实源，把剧本/资产/分镜/项目列表的布局与子滚动规则迁入唯一样式入口 `frontend/src/theme/app.css`；不恢复 `index.css` 双入口。
- 资产详情与分镜台闭合手选门闸：无图片/视频预设、无画幅、无（声明档位时）视频时长时禁用生成并显示原因；切镜保留合法画幅；批量「生成未完成视频」在缺手选参数时提前警告。
- AI 配置刷新成功文案改为「图片 X / 视频 Y」（`refreshCatalogMessage`），避免全量合计与 Tab 过滤列表混报。
- 删除主题迁移临时文件；文档同步手选与唯一主题入口合同。

## 2026-09-21：品牌统一为土豆短剧 / potato

- 产品显示名改为「土豆短剧」；README 定位为 AI 短剧工作流，远端仓库改为 `https://github.com/luckymaomi/potato`。
- 包名、文档、Skill、测试临时目录前缀与环境变量 `POTATO_CONFIG_PATH` / `POTATO_AUDIT_LOG_PATH` 统一为 potato；保留本地工作目录名与 `tomato_ai_drama.db` 文件名不改。
- favicon 使用土豆样式；主题布局、手选生成与刷新分项计数一并收口。

## 2026-09-21：删除工程 ZIP，改为成片与镜头包交付

- 删除 `projectArchiveService`、项目 ZIP 导入导出路由、项目列表导入/导出入口、ZIP 往返测试及 `unzipper` 依赖；旧 `GET /dramas/:id/export` 与 `POST /dramas/import` 现在返回 404。
- 新增 `EpisodeDeliveryService` 作为交付事实 owner：成片下载只接受当前已完成且本地存在的合成 generation；镜头包按本集顺序支持整集或选中镜头，写入当前镜头视频、分镜图、绑定资产标准图和 `shots.csv` / `meta.txt`，不带输入参考图或 generation 历史。
- 分镜台新增导出成片、勾选镜头和整集/选中镜头包下载；项目列表移除工程归档操作。`spec.md`、`README.md` 与 `media-archive` skill 同步为交接导出合同。
- 自动验证：后端 80/80、typecheck、build；前端 25/25、lint、build 均通过。保留前端 4 条既有 lint warning 与 Vite 大包体 warning；未调用真实供应商，未进行浏览器人工打开包验收；commit/push 由 owner 本次明确授权执行。

## 2026-09-21：土豆漫画断裂改造两轮失败（薄壳 / 半体积压缩壳）

- 背景：owner 已定断裂方向——产品由「土豆短剧」改为「土豆漫画」（删视频主链，保留项目/话/定妆/底板生图，新增字层、页模板真实像素拼页、freedub TTS、话数包；路由 `/comic/`；空库不迁移）。理念与执行合同分别为 `AI漫画工作流.md` 与根目录漫画版 `plan.md`。体验硬约束随后明确为：**成熟短剧台 → 成熟漫画台，体验零阉割；工作台代码量相对旧 `StoryboardWorkspace` 只能多不能少，且禁止为凑行数注水。**
- 基线提交：`4ba454c`（删除工程 ZIP 并新增成片镜头包导出）。成熟分镜台事实：`frontend/src/features/workspace/StoryboardWorkspace.tsx` 约 820 行 / 约 45k 字符，含轨道 + 预览 + 检查器、显式组装、配方快照、能力摘要、手选画幅门闸、生成历史选用、待复核等。

### 第一轮：功能能跑的薄壳（已作废）

- 做法：一次性大幅删除短剧视频链与前端分镜台，另起极薄漫画入口；工作区相对 `4ba454c` 约 **+641 / -6203**。
- 典型阉割证据：`StoryboardWorkspace` 从约 820 行压到约 **25 行**；新建 `freedubService` / `pageLayoutService` / `panelPromptAssembler` / `comicPipeline.test` 合计约百行级；**约 20 个后端测试文件整文件删除**（含大量非视频测：provider/transport/errorContract/资产组装等），仅留一份薄漫画测。
- 体验缺口：分格台近似「列表 + 简表单」；组装/话数包等 API 有而 UI 入口不全或主路径卡死；字层无拖拽；用净删行数与少量绿灯测冒充交付。
- 执行代理曾报告 Agnes 图片全链与四格导出、以及 freedub 外部失败取证；随后 owner 判定体验不合格。
- owner 处置：保留漫画 `plan.md` 与 `AI漫画工作流.md`（并备份至 `_owner_stash/`），**`git reset --hard 4ba454c`**，清除薄壳未跟踪残骸。第一轮工作区与其上外部验收证据一并作废，不得当作第二轮完成证明。

### 第二轮：口头零阉割，实为半体积压缩壳（仍不合格）

- 做法：确认基线后口头承诺「以旧 `StoryboardWorkspace` 为母版、体验零阉割」，但实际 **删除** 成熟 `StoryboardWorkspace.tsx`，**新建** 高度压缩的 `PanelWorkspace.tsx`（约 **40 行**、约 **19k 字符 ≈ 旧台 42%**），不是母版展开式改写。
- 整仓相对 `4ba454c` 仍约 **+569 / -5133** 量级净删；AI 配置页等也明显收缩。Provider 等部分非视频测得以保留，但仍删除 `assetRepository` / `coreServices` / `demoInitialization` 等非纯视频测，漫画正向测仍偏薄压缩体。
- 相对第一轮的改进（已确认存在于压缩壳内）：组装按钮、历史选用、待复核/待重装标签、手选画幅门闸钩子、字层拖拽几何、页导出与话数包入口、`director-*` 三栏 class 骨架。**有入口钩子 ≠ 成熟台同等厚度。**
- 过程错误：在工作台未达量尺前开始 README/文档收口与「交接摘要」完成叙事；执行代理交接中亦自承「JSX 高度压缩，需人工检查是否薄壳」，却仍继续收口向推进。
- owner 纠偏（未改产品目标）：禁止另起薄壳；必须 `git show 4ba454c:.../StoryboardWorkspace.tsx` 取母版原地改写；工作台字符量 **≥** 旧台且只能多不能少；禁止空注释/重复样板/无用抽象注水；先补工作台与误删测试，最后才文档收口。

### 取舍与未完成

- **正确可删**：图生视频、视频配方、整集 compose、成片/镜头包、`video_*` 字段与视频 generation 主路径。
- **错误已犯且禁止再犯**：把成熟台压成列表/单列/压缩巨行；用删测试和删行当进度；API 有 UI 无；未达标先收口文档；把已 reset 轮次的外部验收挪到新树上复用。
- 截至本条追加时：第二轮工作台仍约旧台四成字符量，**未达到「只能多不能少」合同**；漫画主链是否真实可点通、Agnes/freedub/浏览器验收均不得因第一轮报告或第二轮交接摘要而视为已闭合。

## 2026-09-22：漫画主链补厚与话数包闭环

- 在保留视频主链退场的前提下恢复漫画侧完整工作区：分格台保留三栏轨道、预览、检查器、配方、手选画幅、生成/上传、历史选用和待复核；页组装独立提供槽位排序、单格/2×2/竖四格、真实像素拼页、气泡拖拽缩放保存、按格 TTS、页图导出和话数包下载。
- 主路径统一为 `panels`；独立 `/comic/:id/panels` 与 `/comic/:id/compose` 两个区位，TTS 配置独立于 AI 生图配置。TTS 适配器完成后把音频写入本地归档，业务只保存本地指针。
- 页布局导出校验槽位底板存在和比例一致，字层坐标归一化并限制缩放；话数包包含底板、字层、阅读序、页信息、页图历史和可用本地音频。补充直接点击下载时按当前模板先生成并归档页图，避免只打包 JSON 和底板。
- 正向测试覆盖配方、上传/历史、字层坐标、四格像素、比例拒绝、TTS 本地文件、话数包底板与页图、两路由可达及 TTS 配置密钥不回显。后端 `73/73`、typecheck、前端 build 通过；lint 仅保留既有 warning，Vite 仍提示单 chunk 体积较大。
- 未验证：Agnes/freedub 真实外部调用、浏览器人工视觉与拖拽验收；未执行 commit、push、部署或发布。相对旧基线的删除主要是视频生成、视频配方、整集合成、成片/镜头包及其专属路由/测试，留下的漫画能力删除不作为完成依据。

## 2026-09-22：视频删了分镜壳还在（任务 K 语义补丁）

- Owner 实机复测确认：结构主链已切到漫画，但分格检查器仍显示「规格与画面描述」、景别、机位、镜头运动、环境声和对白，表名改成 `panels` 不能证明漫画语义完成。
- 语义断裂补丁把空库 `panels` schema 与前后端类型改为 `description`（节拍说明）、`action`（动作定格）、`expression`、`framing`（取景）、`viewpoint`（视角）、构图、光线和氛围；删除 `camera_movement`，对白/声音不再作为 A 层 panels 写入，分别由页组装 captions/TTS 承载。
- 图片配方 owner 按主干 → 资产文本 → 取景/构图/视角 → 动作定格/表情 → 光线/氛围顺序组装，并附干净画面约束；Demo 默认值改为漫画取景、视角和表情。正向配方、仓储、Demo 与整仓后端测试均通过。
- 本条只记录语义补丁证据；浏览器人工视觉、Agnes/freedub 真实调用仍未验证，任务 K 其余收口检查继续以 `plan.md` 为准。

## 2026-09-24：分格台配方 UI 对齐「提示词 + 参考图」心智

- 去掉「已写入配方的参考图」URL tags 与「配方参考图预览」双轨重复；保留可视化「配方参考图」并可从快照删除。
- 出场资产勾选改为标准图缩略图 / 待定妆占位；文案明确：配方 = 最终提示词 + 参考图（资产标准图 + 额外参考图）。
- 未跑浏览器人工点选验收。

## 2026-09-24：资产产出「图片参考锁定」定稿

- 与预设模板同为可选下拉：角色锁脸 / 场景锁景 / 道具锁物；可清空。不选则组装不含锁定段，选中后点「组装提示词」才写入 `output_prompt` 最前。
- 锁定文案含「以我上传的参考图为唯一…锚点，img2img 图生图」与正向一致约束；已去掉禁止/只允许/纯色背景/不出现无关物等负向句。
- 选项只随组装请求与前端草稿，不单独入库；生成仍只读已保存 `output_prompt`。过程曾试过身份多档、关开开关、强制写入，均按 owner 纠偏收敛为本形态。
- `ref/` 浅克隆第三方漫画/生图参考库已忽略入库；`.gitignore` 增加 `ref/*/`。
- 定向测：`assetOutputPromptAssembler`、coreServices 组装断言通过；未做真实供应商出片验收。未验证项：浏览器点选组装与人工读提示词。

## 2026-09-24：页组装取消四格拼页，配音挂底板旁

- Owner 要求：音频放在底板预览旁；取消四格/竖四格拼页 UI；不要原来的底部独立区域。
- 页组装改为左侧底板库 + 主区「底板字层与配音」；导出/话数包固定按当前选中底板走 `single`。后端仍保留多模板拼页能力与既有测试，产品入口不再暴露。
- 未做浏览器人工点通验收。

## 2026-09-24：砍掉页组装与配音，工作区只留三区

- Owner 明确：删除页组装整页与音频/TTS；产品只保留总览与剧本、资产图、分格台。
- 已删前端 `ComposeWorkspace`、`TtsConfigPage`、导航与路由；后端删 `captionService` / `pageLayoutService` / `freedubService` / `ttsAdapter` 及 compose/captions/tts/pages 路由；schema 去掉 `panel_captions`、`panel_audio`、`page_layouts`、`tts_configs`；去掉 `archiver` 依赖与 `panelWorkflow` 测。
- `spec.md` / `README.md` 主链与验收合同已同步；`AI漫画工作流.md` 与 `plan.md` 文首标注旧页组装/TTS 条款作废。
- 验证：后端 typecheck + `69/69` 测试通过；前端 vitest `25/25`、build 通过。未做浏览器人工点通；本地若仍有旧库表（captions/audio/layouts/tts），空库重建或 `clear_database` 后才与新 schema 一致。

## 2026-09-24：故事源简化与分格组装定稿落地

- Owner 定稿：总览只保留画风锁（基调/参考设定）+ 合一故事剧本；不建镜头、不与分格挂钩、不自动建格。
- 分格组装顺序改为：画风锁 → 本格动作 → 资产文本 → 画面细化；参考图含资产标准图、额外参考图与可选上一格底板。
- UI 去掉节拍说明/图像提示词双写；规格收敛为本格动作 + 取景/视角/构图/表情/光线/氛围；组装可选「承接上一格」。
- 空库前提，不做迁移；保存故事页时旧总览/本话结构列写空串。
- 验证：后端 typecheck + `69/69`；前端 typecheck + vitest `25/25`。未做浏览器人工点通。

## 2026-09-26：分镜台改为生成即用最新规格

- Owner 否决待复核 / 勾选批量确认 / 待重装硬门闸：漫画只出底板后这些不是刚需。
- 生成路径改为：保存当前规格后，若配方过期或为空则自动组装，再提交生图；规格未变时仍消费人工改写配方。
- 分镜台 UI 去掉待复核 / 待重装标签、轨道勾选与批量确认；保留可选「组装图片配方」预览。
- `spec.md` / `README.md` 与定向测同步；后端相关测与前后端 typecheck 通过。未做浏览器人工点通。

## 2026-09-26：分镜列表 UI 收口

- Owner 反馈左侧「分镜轨道」区过杂：全部 / 缺底板筛选、「缺底板」标签与统计条重复表达空缩略图已能看出的信息。
- 左侧改为「分镜列表」：缩略图 + 序号标题；去掉筛选条、状态 Tag、勾选列残留布局；新增改为完整「新增分镜」按钮。
- 页眉只保留「话 N · N 镜」，不再挂已有/缺底板计数 Tag。

## 2026-09-26：职责拆分落地（仓储 / 分镜 UI / CSS）

- 按 plan 语义拆分，不为行数而拆。
- 后端：`projectAssetRepository`（资产）与 `panelRepository`（分镜+配方失效）分离；`assetRepository` 保留 `services.assets` 门面；`workspaceNormalize` 抽共用规范化；`projectService` 复用 panel/asset hydrate。
- 前端：分镜台展示迁入 `workspace/panels/*`；`StoryboardWorkspace` 只编排；`app.css` 存活 class 改 `panel-*` 并删无用 `director-*`。
- 未拆：`workspaceRoutes`、`imageGenerationService`、`agnes`。
- 验证：相关后端测 22 通过；前后端 typecheck 通过。未做浏览器人工点通。未 commit。

## 2026-09-26：资产卡字段收敛为少数字段

- Owner 要求去掉无意义细栏：角色保留名称 + 年龄/性别 + 外形（自由文本）+ 服装 + 表情；去掉职业/阵营/气场及脸型五官等拆栏。
- 场景收敛为地点 / 空间外观 / 光影 / 氛围；道具收敛为外形 / 细节 / 状态。表单加 placeholder。
- 同步仓储校验、组装标签、Demo 与测试；`spec` 资产文本合同更新。
- 按 owner 授权执行 `clear_database.py` 后 `init:queen-accession-demo` 重建示例库。

## 2026-09-26：Demo 去掉「未着衣物」违规表述

- Owner：提示词禁止写未着衣物 / no clothing 等；可写衣物覆盖到何处（如绕胸至大腿中段）。
- 《女王出浴》Demo 改为薄巾披裹描写；道具改名「备用巾」。清库并重建示例。

## 2026-09-26：分镜检查器画幅下移与去掉复制分镜

- Owner：画幅/生成放检查器最下方（先组装再生成）；「复制分镜」无必要删除。
- `PanelInspector` 顺序改为规格 → 资产 → 配方 → 组装 → 画幅与生成 → 历史；去掉 duplicate 入口。

## 2026-09-26：分镜规格压缩为画面 + 强资产 + 两类参考图

- Owner 对照 ref 成熟链路：去掉取景/视角/构图/表情/光线/氛围六栏与「上一镜底板」自动开关；文本只留标题 + 本镜画面（`action`）；图片参考只分出场资产标准图与其他参考图（上一镜需自行上传到其他参考图）。
- 组装合同：画风锁 + 本镜画面 + 资产卡文本；参考图 = 资产标准图 + `extra_reference_images`。schema / 类型 / 仓储 / 路由 / UI / Demo contract `v11-panel-compact` / `spec` / `README` 同步。
- 验证：前后端 typecheck 通过；相关后端测 27 通过。空库重建以对齐新 panels schema（见下条若已执行）。

## 2026-09-26：分镜组装与总览/资产文本彻底解耦

- Owner 确认采用「分镜只拼本镜画面 + 资产标准图引用」：最终提示词不再注入总览 `tone`/`reference_setting`，也不再注入资产 `text_profile` / `output_prompt`。
- 一致性靠出场资产标准图与其他参考图；风格若需要写进本镜画面。总览画风锁文案改为「不自动注入分镜」。
- 验证：组装单测与 `coreServices` 消费测同步；相关后端测 19 通过；前后端 typecheck 通过。未做浏览器人工点通。

## 2026-09-26：资产卡合并为名称 + 一段 brief，并支持复制给其他 AI

- Owner：年龄/性别等细栏可合并；资产资料只要名称（标识）+ 一段提示词；placeholder 写清建议覆盖项；加复制按钮方便粘到外部 AI 写好再贴回。
- `text_profile` 唯一键改为 `brief`；UI 去掉多栏表单；「复制给其他 AI」带写作说明+草稿，「复制生成提示词」复制 `output_prompt`。
- Demo contract `v12-asset-brief`；`spec` / 测同步。相关后端测 28 通过；前后端 typecheck 通过。

## 2026-09-26：资产「复制给其他 AI」模板改为分条输出样例

- Owner：不要写「用中文」；要写成可直接粘贴回资产台的提示词；覆盖项尽量写全但非死板填空；输出必须分条「标签：内容」（编号可选），并给出样例。
- 角色/场景/道具三类复制模板与 placeholder 同步重构。

## 2026-09-26：生图远程预览 + 后台有界归档

- Owner：归档超时/失败时界面应先显示远程 URL；远程就是远程，归档成功后再把指针切到本地；后台轮询待归档记录。
- generation 新增 `remote` 状态与 `archive_attempts`；供应商成功先 `acceptRemote`；同任务内尝试归档，失败不抹预览；`startArchiveRetryLoop` 有界重试（超时 45s、最多 5 次）。
- 前端状态区分远程预览·待归档 / 本地保存失败 / 已完成；选用允许 remote。
- `spec` / media-archive skill / 测同步。

## 2026-09-26：清理待复核死路径与配方过期接线

- Owner：归档推送后做整体调试：删死代码/错误逻辑，再二次推送。
- 删除 `confirm-review` 路由、facade 与前端 API；去掉已无调用的 `assembleStoryboardRecipes`。
- `markPanelImageChanged` 恢复单参；资产标准图/文本变更经 `markAssetImageChanged` 置 `recipe_needs_reassembly`（有配方或底板的引用分镜）。`image_needs_review` 列仍保留兼容，产品面不再使用。
- 回归：新增仓储测；后端 77 测与 typecheck 通过。
