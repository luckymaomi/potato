# tomato-ai-drama 变更记录

# 2026-09-17：节点自治、显式数据流、Everything 日志与可恢复 PearAPI 验收

- 真实失败证据表明项目只写入 `demo_version=12`、项目和剧集，却没有角色、场景、道具、分镜与媒体；初始化脚本只看版本号提前返回，错误地把半成品当完成。回归测试现保护“同版本半成品原位补齐、重复初始化不创建第二个项目、不覆盖完整画布”。
- owner 最终明确节点自治合同：面板参数是完整输入，连线只追加已经完成的直接上游数据；单节点不补跑祖先，未完成上游不阻断，后端也不得从资产仓库补提示词或参考图。图片、视频和合成服务已删除隐式仓库取数，整集合成只接受显式视频列表。
- Demo 升级为 v13：46 个节点、66 条边、1 个工作流组。10 个逐镜文本节点和实际入画资产图共同进入对应分镜图，分镜图进入视频，10 路视频汇入合成；资产图节点不再连接提取节点。媒体固定为 PearAPI `gpt-image-2` 与 `grok-imagine-video`。
- 新增默认位于 `backend/logs/everything.log` 的 JSONL Everything 日志，覆盖 HTTP、项目/剧集/画布 CRUD、生产命令、任务、供应商、本地归档、合成、初始化与验收。Key、Authorization、token、secret、查询串、Data URL、Buffer、循环对象和超长值统一脱敏，日志不放在 `backend/data`。
- 新增 `npm.cmd run accept:rainy-night-demo`：可恢复初始化、刷新 PearAPI 模型目录、跳过本地有效 generation、逐节点运行和保存，对 429/暂时性错误做有界重试，最终只验证任务与本地媒体可用，不断言随机生成内容。
- 确定性验证：前端 35/35、lint、build 通过；后端 58/58、strict typecheck、build 通过。owner 最后要求保持 `backend/data` 不存在并亲自执行真实验收，因此本轮没有初始化真实库、没有启动服务、没有调用 PearAPI；真实外部结果仍未知。

## 2026-09-17：从单链纠偏为资产复用 DAG，并收口本地媒体历史

- 失败证据：首版《雨夜外卖》把 37 个节点放在同一水平线并以 36 条相邻边串成一条长链。owner 的实际截图证明这种结构看不出资产一对多、镜头多输入和合成汇聚，且把数据依赖误画成时间轴。
- 取舍：删除线性工作区构造器和“边数 = 节点数 − 1/同一 y/中间节点一进一出”合同，改为分层 DAG。当前 Demo 仍为 37 个节点，但有 80 条真实依赖边和 1 个工作流组；镜头 6/7/9/10 的资产入边、10 路视频汇入合成及整图无环由结构测试保护。
- 执行：上下文解析只读取插件声明和直接入边；资产图节点的结果携带稳定资产 ID 与本地图片，分镜图按实际连线取得参考图。全图继续拓扑排序，任何直接上游未完成时明确拒绝，不把未满足依赖当作可运行。
- 资产生成：同一角色/场景/道具插件支持文生图与图生图；图生图命令可同时携带文本提示词和多张参考图，数量由实时 Agnes 模型能力再次校验。
- 媒体历史：图片、镜头视频和 FFmpeg 整集合成都建立不可变 generation；角色/场景/道具、分镜和剧集只更新当前版本指针，检查器可选择旧版本。供应商 URL 只作来源记录，本地归档或本地合成失败不会标为完成。
- 项目归档：确认 `multer.memoryStorage()` 与 50MB 限制没有业务依据，且 `AdmZip(path)` 内部仍会整包 `readFileSync`。导入现改为上传流直写临时磁盘、逐 ZIP 条目流式解压；导出也改为媒体文件流式写 ZIP。两端均无人工 ZIP 大小上限并在成功或失败后清理传输文件。
- 确定性验证：前端 30 项测试、lint、build 通过；后端 51 项测试、strict typecheck、build 通过。真实《雨夜外卖》Agnes 全链、隔离空库启动和浏览器视觉验收仍待收尾，不能用历史成功或 Mock 替代。

## 2026-09-17：节点插件、自治上下文与资产仓库重构前调研

- 问题：当前生产目录只拥有部分角色元信息，执行器仍按直接上游材料和角色猜文本、图片及记录 ID；角色、场景、道具和分镜虽已落库，却没有统一的稳定引用与上下文解析合同。
- 假设：成熟节点工作台会让节点类型自己声明输入输出与执行能力，短剧领域则需要在端口数据流之外增加按稳定 ID 查询的资产仓库。
- 尝试：在固定本地提交上核验 tldraw Image Pipeline、ComfyUI frontend、AICON 和 Huobao Drama 的节点定义、注册、模型目录、资产关系、执行状态、错误与恢复实现，并与当前 `catalog.ts`、`executor.ts`、实体/分镜服务及统一生产入口交叉对照。
- 证据：tldraw 与 ComfyUI 均由集中节点定义投影创建、控件和执行；AICON 的模型能力由服务端 catalog 拥有且生成历史绑定资源 ID；Huobao 以项目级资产、分镜关联表和显式业务 ID 读取本镜头上下文。当前仓库的 `prepareNodeForExecution` 只看直接上游，而实体与分镜 replace 服务会先删后建，可能破坏媒体和引用。
- 结果：研究证据已足以进入断裂式实现；生产插件目录、节点输入合同和资产仓库分别成为唯一 owner，连线只保留数据流方向。真实 Agnes 可用性仍须在实现后通过证据闸门核验。
- 淘汰路线：不采用纯端口上下文、按祖先角色猜测、旧字段兼容、双写、ComfyUI 迁移层、tldraw 并行策略、Huobao 全量替换和进程内任务状态。
- 留下边界：保留现有统一后端生产主干、SQLite 任务、错误合同、首次失败终止与取消；owner 随后明确把本地媒体归档、生成历史、带媒体导入导出与《雨夜外卖》全链真实验收纳入同一交付，仍不扩张到配音、字幕或完整剪辑。详细来源与采用边界见 `reference/README.md`。

## 2026-09-17：唯一生产主干、模块化错误与产品身份收口

- 物理删除旧画布节点目录、旧编排器、旧工作台生成服务和旧生成 API；节点快照只保存稳定生产角色 `role`，材料、生产阶段、资产分类、文本动作与默认生成方式全部由 `features/production/catalog.ts` 派生。读取画布不识别、不升级也不报错提示旧节点结构；空数据库只生成当前合同。
- 前端执行器把手动文本、AI 文本、图片、视频和整集合成翻译成统一生产命令；后端 `ProductionWorkflowService` 成为唯一编排入口，HTTP 字段解析、领域编排、底层生成服务、Provider 适配、任务和持久化各自独立。核心测试确认文本、文生图、图生图、文生视频和图生视频全部经过同一生产主干。
- 建立前后端统一错误合同：保留机器错误码、HTTP 状态、供应商和可重试事实，集中展示 400、401、403、404、408、409、413、422、429、500、502、503、504、网络、超时、配置和能力错误。批量运行首次失败即终止当前范围，未开始节点不提交；429 只做短暂有界重试，耗尽后由用户手动重跑。
- 产品身份从旧名断裂式统一为 `tomato-ai-drama`：npm 包、页面标题、应用品牌、根配置、SQLite 文件名、导出文件、启动日志、文档、测试临时目录和仓库 skill 均已同步；磁盘根目录按 owner 要求暂不改名。Git 远端切换到 `https://github.com/luckymaomi/tomato-ai-drama.git`。
- 确定性验证：前端 25/25 测试、lint、build 通过；后端 44/44 测试、全局 strict typecheck、build 通过；前后端依赖审计均为 0 漏洞。实际启动返回新应用名和 0.0.1，页面标题、六类提示词、项目 CRUD 与 Vite API 代理通过；临时项目删除，服务停止。
- 主实现提交 `9f24838` 已推送新远端 `origin/master`；私有配置、运行数据库和媒体目录均未进入提交。
- 本轮未调用真实供应商、未部署、未发布。前端构建仍有约 1.47 MB 单块警告；第三方不支持撤销时，本地停止不能物理撤回已经到达供应商的计算。

## 2026-09-17：短剧生产语义与可停止运行断裂式重构

- 删除画布旧的混合节点合同，改用稳定生产角色和统一生产目录派生材料、阶段与资产分类，不保留旧字段兼容或双写。
- 默认模板升级为 11 节点、13 条曲线连线的完整生产链；Agnes Demo 升级为 v7 并使用相同合同。节点创建入口收敛为单一加号菜单，按生产链与通用材料分组，真实浏览器截图确认不再遮挡最左侧故事节点。
- 新增后端文本提示词目录和 `GET /api/v1/production/text-prompts`。六类系统提示词在检查器完整可见、可编辑、可恢复默认，用户覆盖通过 `system_prompt` 到达 Provider；默认正文仍只由后端目录拥有。手动剧本运行会保存到关联剧集，提取和分镜可通过 `source_text` 直接消费连线上游文本。
- 新增单一 `CanvasRunSession`：记录停止信号和当前任务 ID，停止时调用任务取消接口、结束轮询并阻止后续节点提交。取消信号传播到文本、图片、视频、Provider 重试等待和 FFmpeg；后端在结果落库前再次检查取消，迟到响应不能把已取消任务改成成功。第三方不提供撤销协议时仍无法承诺其远端计算物理停止。
- 修复两条审查中发现的真实链路缺口：AI 文本节点重跑时重新读取上游内容，不把上次输出当输入；角色/场景/道具资产图的专用接口现在接收节点提示词覆盖和参考图，图生图不再退化为文生图。检查器允许明确选择关联资产或分镜记录，手动选择会覆盖自动序号匹配。
- 新增或更新节点目录、默认模板、Demo、拓扑、连线输入、手动剧本、资产图参考图、保存快照、媒体展示、运行停止、提示词覆盖、取消后不落库和 Provider 重试取消测试。未调用真实供应商模型。
- 空库启动验收：后端返回健康状态和当前中文提示词目录，前端首页 HTTP 200；临时项目由浏览器初始化出 11 节点、13 连线和 1 个工作流组，节点均使用当时的当前合同。临时项目随后删除，前后端服务已停止；启动产生的空 SQLite 结构保留。
- 本轮未执行 commit、push、部署或发布。浏览器验证覆盖 1440×1000 桌面画布；移动端和真实供应商生成未在本轮复验。

## 2026-09-17：数据清理触发规则纠正

- owner 澄清“需要时直接删除 data”是对执行者的显式操作要求，不是每次测试或启动都清库。移除 `dev:fresh`、`clean:data` 与 `pretest`，普通启动、测试和构建不再删除 `backend/data`。
- 根 `start.py` 移除已经不存在的 `migrate` 调用，只分别启动前后端开发服务。迁移体系仍保持删除状态，不增加兼容命令。
- 后端测试使用内存 SQLite 与系统临时目录，不依赖自动删除日常数据库；今后只有 owner 明确要求清空时，执行者才在停止服务并确认目标后手工删除 `backend/data`。
- 验证：启动器进程参数仅包含前后端 `npm run dev`；后端 36/36 测试、严格类型检查与构建通过，测试后 `backend/data` 仍保持原先的不存在状态；活动入口静态扫描未发现迁移或自动清库调用。

## 2026-09-17：画布保存合同断裂式重构

- 调研 `C:\Users\Administrator\Desktop\repository\mo` 的 `StorySaveCoordinator`、`WorkspaceRepository`、Web Locks、BroadcastChannel 和编辑器接线：确认成熟保存必须同时处理不可变快照、防抖合并、写入串行、expected revision、失败保留、冲突停止与离开保护。tomato-ai-drama 的持久化 owner 是 Node/SQLite，因此没有照搬 IndexedDB、浏览器锁或本地广播。
- 对照 AICON 的节点级 300ms 防抖和生成前 flush、tldraw 的 document/session 分离、ComfyUI 的 revision dirty 语义；拒绝 AICON 仅记录控制台错误且无冲突保护的做法，也没有引入超出单机产品需求的 CRDT。
- 删除“900ms effect 直接 PUT”的旧保存主干，新增独立 `CanvasSaveCoordinator`：规范快照指纹去重、700ms 防抖、单 Promise 串行写入、保存中继续排队、失败快照保留和显式 idle/dirty/saving/error/conflict 状态。生成、手动保存和画布内部导航统一 flush，待保存内容触发浏览器关闭提示。
- 画布快照成为唯一 owner：`canvas_layout` 内含节点、坐标、连线、工作流组和模板；删除重复的顶层 `workflow_groups` 写入。React Flow 的节点/连线 selected、dragging 等瞬时 UI 状态不入库。
- 当前唯一 schema 新增 `dramas.canvas_revision`。保存接口必须提交 `expected_revision`，ProjectService 在 SQLite 事务内 compare-and-set 并递增版本；旧页面收到 HTTP 409，不能覆盖较新画布。没有迁移、兼容参数或双写路径。
- 回归测试先证明旧实现缺口，再验证连续快照只保存最新值、写入严格串行并递增 revision、网络失败可重试、冲突停止后续覆盖、选择态不持久化和服务端旧 revision 不改写数据。前端 24/24、lint、build 与后端 strict typecheck、36/36、build 均通过；前端仍有单包超过 500 kB 的构建警告。
- 官方 npm 审计发现旧版 `adm-zip`、Express 依赖链、`js-yaml` 与 `sharp` 存在高/中危告警；分别升级到 0.6.1、4.22.3、5.4.2 与 0.35.4，移除多余的 `@types/js-yaml`，并按 sharp 要求把 Node 下限收敛为 20.9。升级后前后端审计均为 0 漏洞，完整类型、测试和构建重新验证通过。
- 本轮保存改造未重新调用任何付费媒体模型；此前同一交付中的 Agnes 复杂 Demo 真实全链验收仍有效。未执行 commit、push、部署或发布。

## 2026-09-17：模型画幅能力、空库 schema 与 Agnes 复杂 Demo

- owner 明确当前为快速开发期，数据库不保留。服务停止后已删除整个 `backend/data`，其中 SQLite、WAL/SHM、媒体 storage 与旧备份均不可恢复；删除旧 `migrate.ts`、`runMigrations.ts` 和 `migrate` 命令，`src/db/schema.ts` 成为唯一结构 owner。后端测试通过 `pretest -> clean:data` 每次先删除数据目录。
- 模型能力合同新增画幅比例。PearAPI 公开丰富目录实测返回模型级 `aspect_ratio`，适配器将它与 `/v1/models` 的凭据可用目录按模型和渠道求交集；Agnes `/v1/models` 不返回比例，改由 Agnes 适配器复用真实协议中的图片、Video 2.5 和 Video 2.0 比例集合。
- 图片请求拆出独立 `aspectRatio`，修复过去把 `9:16` 塞进 `size` 后被 Agnes 回退成 `1:1` 的缺陷。前端按具体模型展示比例，切换模型清理无效值；后端在创建生成记录和收费任务前校验模式、参考图数量与比例，未知能力不会静默放行。
- 参考 `reference/aicon` 的服务端模型能力目录与模型切换规则、ComfyUI 的节点参数归属；拒绝 Huobao 的项目级固定比例，因为同一无限画布允许不同模型和媒体节点。没有复制第三方代码、品牌或视觉。
- Agnes Demo 断裂式升级为《雾港回声》：18 个节点、27 条曲线连线、5 个工作流组，包含 2 角色、2 场景、2 道具、3 个分镜图与 3 个分镜视频分支。提取节点保存全部记录 ID，分支通过显式序号写回正确记录；分镜数量成为显式节点参数并由后端限制。
- 真实 Agnes 验收从空库开始：同步到 6 个文本、3 个图片、3 个视频模型，图片模型 8 种比例、Video 2.5 五种比例、Video 2.0 六种比例。免费文本接口两次返回 429，按节点串行重试后成功；最终得到 2 个角色、2 个场景、3 个道具、3 张分镜图、3 段 `9:16` 分镜视频，整集合成任务 completed 且剧集有真实最终视频。PearAPI 未发起任何媒体生成。
- 本轮所有新增界面仍使用完整边框；没有单边强调色、状态 rail 或伪元素侧边条。未执行 commit、push、部署或发布。

## 2026-09-17：PearAPI 双凭据适配修复

- 真实失败证据：项目 3 的 PearAPI 图片记录 10、12、13（图生图）和 14（文生图）均返回“余额不足或用户不存在”；无参考图仍失败，排除了 Base64 与图生图参数。
- 根因经 PearAPI 人员回复和真实调用共同确认：默认渠道 `sk-` 令牌只供 `/v1` 接口并放在请求头，普通分发 Key 供 `/api/image_generate`、`/api/video_generate` 并放在请求体；旧适配器错误地把前者当作后者。
- 根配置现在分别保存 `api_key` 与 `settings.generation_key`；PearAPI 媒体适配器只接受普通分发 Key，缺失时明确拒绝，不再回退到 `sk-` 令牌。示例配置与用户文档同步说明两者用途。
- 修复后的真实图片记录 15 使用 `gpt-image-2` 完成，返回 `image/png`，大小 2,065,056 字节，结果主机为 `cdn.pearapi.ai`；这证明普通分发 Key 的媒体调用和账户余额可用。按 owner 后续要求不再发起新的生图请求，也未把该验证结果写入 Demo 画布。
- 实时模型目录返回 80 个模型，包含 `gpt-image-2`、`gpt-image-2-2k` 和 `gpt-image-2-4k`；目录不返回价格字段，因此只能把基础型号作为最低价候选，不能声称已通过价格数据验证。
- PearAPI 控制台展示的余额 URL 以普通 Key 查询，但实测仍返回 HTTP 404 与“key参数不能为空”；前端页面只展示该示例文本，没有可复用的真实请求。该异常需要 PearAPI 确认实际路由或参数，不影响已完成的生成验证。
- 验证：后端 strict typecheck、33/33 测试和 build 通过；前端 17/17 测试、lint 和 build 通过；差异、敏感信息、UTF-8 无 BOM 与单边强调色扫描通过。前端构建仍有单包超过 500 kB 的既有性能警告。
- 本轮未执行 commit、push、部署或发布；临时开发服务已停止。

## 2026-09-17：本地参考图 Base64 与缩略图修复

- 真实失败证据：项目 3 的图片记录 6、7 使用本地 `/static/uploads/*.jpg` 和 `agnes-image-2.5-flash`，均返回 HTTP 400。文件本身存在且可读，根因是旧实现把本地路径拼成 `http://localhost:5679/static/...` 交给远端 Agnes。
- 新增统一媒体引用解析服务：公网 URL 原样保留；本地静态图片在 storage 根目录内完成真实路径、文件存在、16MB 上限和 JPEG/PNG/GIF/WebP 魔数校验后，临时转换为 `data:image/...;base64,...`。路径穿越、缺失文件和伪造图片均明确拒绝。
- Agnes/PearAPI 图片适配器统一在提交前解析参考图，业务服务不增加供应商分支；视频接口仍只把实际公网 URL 当作公网引用，不再伪装 localhost 地址。
- Provider 传输层会从 `error.message`、`detail`、`message`、`msg` 等常见字段提取不超过 300 字的上游原因，使节点能显示比“HTTP 400”更具体的错误。
- 画布检查器用独立参考图组件替代 URL 标签：保留 URL 添加和本地多图上传，下方显示缩略图，支持点击预览和单张删除；所有新增样式使用完整边框，无单边强调色。
- 生成结果不再只显示文字链接：右侧检查器新增当前结果缩略图，图片可点开大图，视频可直接播放，同时保留新窗口打开入口；画布节点与检查器共用媒体类型判断。
- 真实 Agnes 复验：使用原失败节点的本地 JPEG、原提示词和 `agnes-image-2.5-flash` 创建图片记录 8，任务完成；返回 PNG 可下载，HTTP 200，大小 4,260,720 字节。结果已写回项目 3 的角色参考图节点。
- PearAPI 密钥已写入被 Git 忽略的根 `config.yaml` 并完成脱敏读取确认；遵照 owner 要求没有向 PearAPI 发起请求。
- 验证：后端 strict typecheck、32/32 测试和 build 通过；前端 17/17 测试、lint 和 build 通过；单边强调色扫描无命中。未做浏览器人工视觉验收，构建仍有单包超过 500kB 的既有性能警告。
- 本轮未执行 commit、push、部署或发布。

## 2026-09-17：Demo 画布编码回归修复

- 截图与数据库核验表明：项目、剧本、实体和分镜均为正常 UTF-8，只有 `dramas.metadata.canvas_layout` 中文变成了字面量 `?`；不是字体缺失或前端渲染问题。
- 根因是验收阶段通过 Windows PowerShell 回写整份画布 JSON 时，请求 Body 未按 UTF-8 发送，中文在进入后端前已不可逆丢失。
- 修复前使用 SQLite 在线备份保留恢复点；随后以正常剧本、角色、场景、道具和分镜记录为事实源重建画布，保留节点坐标、媒体 URL、任务 ID、完成状态和整集结果。
- Demo 升级到 v5：模板可从已有项目事实水合，并识别连续问号型损坏画布后原位修复。回归测试覆盖结果水合和损坏识别。
- 数据复验：11 个节点标题全部恢复，连续问号字段 0、Unicode 替换字符 0、完成节点 10、媒体节点 5、任务节点 10，第 1 集仍为 completed 且保留整集视频。
- 本轮修复未获得新的 commit/push 授权，远程 `origin/master` 暂未包含此修复。

## 2026-09-17：Agnes 真实 Demo 全链路验收

- 完整 Demo 升级为 `agnes-short-drama-demo` v4：11 个节点、13 条曲线连线；除手工梗概外所有节点默认选择 Agnes，模型仍从实时目录读取。
- 删除虚拟成功结果后真实生成《雾港来信》：完成剧本、角色/场景/道具提取、角色/场景/道具参考图、六个分镜、首张分镜图、文生视频与图生视频；真实结果写回本地项目 2、剧集 3 和画布节点。
- 修复提取节点不传实体 ID、分镜图误取角色 ID 两个编排缺陷，并增加对应前端测试。旧 Agnes Demo 点击入口时原位升级，不重复创建项目。
- Agnes 文本并发曾返回 429，道具图曾发生一次网络失败；采用串行调用和明确重试后完成。失败任务保留失败事实，没有改写成成功。
- 实时目录的 `agnes-video-2.5` 对当前凭据返回 403，`agnes-video-2.5-flash` 实测可用；新增根配置 `default_models`，节点不硬编码模型，配置值必须存在于实时目录。
- 首次整集合成因 FFmpeg concat 拒绝 HTTPS 失败；加入最小协议白名单并新增远程 HTTP 视频回归测试后成功生成本地 MP4，FFmpeg 解码验证通过。
- 验证：后端迁移、严格类型检查、27 个测试、构建通过；前端 11 个测试、lint 0 诊断、构建、0 漏洞审计通过；禁用单边强调色的静态扫描命中 0。
- 未验证：PearAPI 真实付费生成与浏览器 GUI 人工视觉/拖拽验收。完成验证后经 owner 明确授权提交并推送 `origin/master`；未执行部署或发布。

## 2026-09-16：工作台可管理性与运行体验重构

- 默认模板固定为 10 个节点、12 条依赖连线和 1 个可保存工作流；新项目首次初始化，已有画布不覆盖，虚拟 Demo 按版本幂等打开。
- 项目列表补齐搜索、新建、打开、编辑、删除、导入和导出；节点补齐新增、编辑、复制、确认删除；工作流组补齐保存、改名、运行和删除。
- 所有运行入口收敛到同一执行器：单节点、所选节点、当前节点下游、整个画布和工作流组按拓扑顺序运行，循环连线和上游失败均明确反馈。
- 连接会传递必要的上游文本与图片；图生图/图生视频无参考图时明确拒绝；文本提取和分镜结果会回写节点内容供后续节点使用。
- 全面重做项目页、画布、节点、检查器和配置页视觉层级，移除所有单边强调色、状态 rail、`border-left/right` 和伪元素色条。
- 新增 Vitest 编排测试 6 项；前端构建、lint、测试与依赖审计通过。Chrome/Edge 无头截图在当前环境未产出，真实 Agnes/PearAPI 生成未因缺少凭据而宣称通过。

## 2026-09-16：前后端目录标准化

- owner 确认项目不需要路径兼容，因此将两个应用目录断裂式统一为 `frontend/` 与 `backend/`，没有保留副本、软链接或旧路径探测。
- 同步更新根启动脚本、后端静态前端产物定位、当前产品事实、安装运行命令和 FFmpeg 说明；API、数据库 schema、Provider 合同和页面功能均未改变。
- 验证：后端迁移、完整严格类型检查、21 个测试与构建通过；前端 lint 与构建通过；根启动脚本 Python 语法检查通过；旧目录名静态扫描为 0。
- 运行探针：从 `backend/` 启动正式构建产物后，`GET /health` 返回 `0.0.1`，根路径成功返回 `frontend/dist` 页面；探针结束后服务已关闭。
- 外部边界：没有真实第三方凭据，本次未复验 Agnes/PearAPI 生成；未执行 commit、push 或部署。

## 2026-09-16：仅 Agnes 与 PearAPI 的严格 TypeScript 后端

- owner 明确本项目没有生产数据或旧版本兼容要求，因此采用断裂式重建，不迁移历史供应商和旧业务模块。
- 修改前证据：全后端完整严格检查有 2950 条诊断、涉及 79 个旧文件，其中 2253 条为隐式参数类型，463 条为未收窄异常值。
- 取舍：删除历史供应商实现、协议推断、供应商专用路由、旧迁移、旧测试与无用依赖；项目、配置、任务、实体、分镜、文本、图片、视频、归档和整集合成按变化原因拆为独立服务。
- Provider 核心只按明确身份注册和选择适配器，当前注册 Agnes 与 PearAPI。Agnes 补齐文本生成；PearAPI 保持图片与视频能力。业务服务不读取供应商响应结构。
- 数据：旧开发数据库及 WAL/SHM 已删除，新 SQLite schema 只保留工作台当前使用的表；这次操作不可恢复，符合 owner 明确的无兼容边界。
- 验证：全后端完整严格 TypeScript 检查 0 诊断；迁移、构建、21 个测试全部通过。测试覆盖两家供应商的文本/图片/视频合同、未知供应商拒绝、项目和画布持久化、归档往返、文本与四种媒体模式，以及使用仓库内 FFmpeg 的真实本地整集合成。
- 运行探针：构建产物启动和健康接口通过；Provider 目录只返回 Agnes/PearAPI；项目、集数、画布、归档导出通过；未知供应商返回 400，不存在项目返回 404，缺少图片配置返回 400；前端入口和 Vite API 代理通过。
- 外部边界：没有真实第三方凭据，不能宣称 Agnes/PearAPI 的生产生成已验证。前端构建仍报告单个产物块超过 500 kB 的性能警告。
- 收尾：HTTP 验收产生的项目数据已删除并重建空库；前后端服务已关闭；未执行 commit、push 或部署。
