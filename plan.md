# 当前执行合同

> 本文件只描述 2026-09-19 工作台体验收口。产品主链与业务规则以 `spec.md` 为准，探索和结果只追加到 `history.md`。

## 需求

- 保留已经完成的短剧主链、领域对象、多选能力和 Provider 隔离，不推倒重做，不恢复节点画布。
- 对照本地 `reference/moyin-creator`、`reference/huobao-drama`、`reference/dramai` 和 `reference/ai-short-drama` 的工作台组织，重做页面层级与操作密度；不复制品牌、皮肤或源码。
- 项目主导航只表达四个制作阶段：剧本、资产图、分镜台、生产。人物、场景、道具与全局库都属于资产图工作台内部。
- 资产图以标准图画廊为主，人物、场景、道具是筛选维度；资产支持一个或多个自定义标签，并可按标签筛选显示。
- 分镜台使用可视化资产缩略图选择人物、场景和道具，不把选择动作埋在下拉框里。
- 剧本、资产图、分镜台和生产使用同一项目工作区高度与内部滚动规则。

## 已确认问题

- 当前项目壳有六个平级入口，人物、场景、道具重复标记为 `02`，全局资产库只能从资产页按钮进入。
- 当前资产页固定为左侧缩略目录与右侧长表单，标准图不是页面第一视觉层。
- 当前 CSS 至少存在四套互不一致的高度：剧本 `calc(100vh - 378px)`、资产列表 `690px`、分镜台 `calc(100vh - 270px)` 且最低 `680px`、生产媒体 `138px`。
- 当前分镜资产只能在右侧多选下拉框中勾选，中央素材区只能查看，不能直接编排。
- 当前 `project_assets` 和 `asset_library_items` 没有标签字段，前端类型、保存、归档与测试也没有标签契约。

## 参考结论

- 成熟实现把项目导航按制作阶段收敛，资产类型放在资产工作台内部切换。
- 资产首页以可比较的大图卡片为主体，生成、上传和历史选用动作靠近图片；详细字段只在编辑当前资产时展开。
- 镜头顺序适合横向镜头带或紧凑任务列；镜头引用资产应显示图片、名称和选中状态。
- 固定外壳只定义一次，页面主体使用 `flex: 1`、`min-height: 0` 和内部滚动，不由每一页单独计算视口。

## 不做范围

- 不修改供应商适配器、模型选择、任务轮询、并发、生成费用确认或媒体归档规则；工作台视频入口只移除任意时长输入。
- 不新增画布、DAG、节点、时间线连线或自动视频提交。
- 不复制参考项目品牌、源码、图标皮肤、宣传文案或暗色主题。
- 不调用真实图片或视频供应商，不部署；数据库重建按 owner 明确授权执行。

## 设计

### 项目壳

- 主导航收成四项：总览与剧本、资产图、分镜台、生产。
- 项目壳拥有唯一可视高度；标题、剧集选择和导航固定，工作区主体内部滚动。
- 全局资产库是资产图下的同级子视图，主导航中的资产图在两个子视图都保持选中。

### 资产图

- 项目资产默认显示为标准图画廊；类型分段控件、标签筛选、提取、批量生成、新建与全局库入口位于同一工具区。
- 卡片固定图像比例，直接显示名称、类型、图片状态和自定义标签。
- 点击卡片打开制作抽屉；标准图占主要面积，名称、图片描述、标签、参考图、生成与历史保持现有能力。
- 标签持久化为去重字符串数组；可自由输入，可不填，可多选；项目归档往返保留标签。

### 分镜台

- 镜头带横向排列并独立滚动，不再占据一整列。
- 主体为画面制作区和参数区两列。画面区包含分镜图与三类可视化资产选择架；点击缩略图即可独立多选。
- 参数区保留叙事、镜头语言、生成输入和网格提示，不再重复出现资产下拉框。

### 生产

- 保留按镜头生产、批量和合成规则；内容放进统一高度的滚动制作区。
- 镜头媒体使用稳定比例，不再固定 `138px`；卡片保持可扫描的一致密度。

## 影响面

- 前端：`App.tsx`、`ProjectShell.tsx`、四个工作区组件、资产库视图、领域类型、CSS 和相关测试。
- 后端：数据库 schema、资产读写、领域类型、项目归档导入与测试。
- 文档：`spec.md`、`README.md`、本计划；`history.md` 只在末尾追加。

## 任务

- [x] 收敛项目导航与统一工作区高度。
- [x] 把项目资产改成带类型和标签筛选的标准图画廊及临时制作抽屉。
- [x] 为项目资产和全局资产补齐自定义标签持久化、归档与回归测试。
- [x] 把分镜台改成横向镜头带、两列工作区和可视化多选素材架。
- [x] 把生产区接入统一高度并移除固定媒体高度。
- [x] 同步 `spec.md`、`README.md`，在 `history.md` 末尾追加证据与取舍。
- [x] 通过前后端完整验证与浏览器桌面/移动验收。
- [x] 取证参考 README 图片并修正窄屏项目壳的纵向布局与单列画廊。
- [x] 断裂式移除资产锁定、资产依赖和版本升级语义；资产历史只保留“选用这张”。
- [x] 视频入口不再读取镜头任意时长，改由选定模型目录的固定能力决定。
- [x] 按 owner 授权删除并重建 `backend/data`，重新启动后端并初始化《雨夜外卖》Demo。
- [x] 对照魔音资产页截图移除资产子导航、抽屉和 Segmented 框；资产页收成阶段栏、画廊、当前详情三段结构。

## 验证

- 自动：前端 build、lint、test；后端 typecheck、build、test；`git diff --check`；禁止样式静态扫描。
- 浏览器：桌面和移动分别走剧本、资产图、分镜台、生产；验证页面高度稳定、内部滚动、标签筛选、资产抽屉和分镜缩略图多选。
- 参考取证：README 中的 14 张界面图片已下载到 `reference/moyin-creator/readme-images/`；已查看资产页与导演台图片并对照项目截图。
- 外部：本轮不调用真实供应商，不以 Demo 文本或 HTTP 200 代替外部验收。

## 外部状态

- 当前分支：`master`。
- 工作区包含上一轮未提交的首页与剧本区定高改动，本轮在其上继续，不回滚。
- 本轮已按 owner 授权完成本地数据库删除/重建和 Demo 初始化；未调用真实供应商，未 commit、push、部署或生产写入。
- 后端 `http://localhost:5679` 与前端 `http://localhost:3012` 当前运行中，浏览器已打开 `/film/1/assets?episode_id=1`。
