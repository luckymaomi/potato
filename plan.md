# 当前任务：删除项目 ZIP，改为成片 / 镜头包导出

## 需求

- 删除当前项目 ZIP 导入 / 导出体验与实现（`format: 8` 工程备份包）。
- 改为后期友好的导出：
  1. **导出成片**：下载本集当前整集合成预览 MP4（若已合成）。
  2. **导出镜头包**：按分镜顺序打包「当前选用」镜头视频、分镜图、本镜绑定资产标准图，以及提示词 / 资产 ID 等 meta；支持**整集全部有视频镜头**或**仅选中镜头**。
- Owner 明确：本地 `data` 已够用，不再需要 ZIP 工程往返；剧本与资产保持分离式设计，本任务**不**做可拍清单或剧本↔资产桥接。

## 当前事实

- 产品主链见 `spec.md`：人工剧本 → 项目资产卡 → 分镜台 → 分镜图 → 镜头视频 → 整集合成。
- 工程 ZIP 服务、导入导出路由、项目列表导入/导出入口和 ZIP 往返测试已删除；`unzipper` 及其类型依赖已移除。
- 整集合成由 `CompositionService` 产生本地 `video_generations` 成片并更新 `episodes.current_video_generation_id`；新 `EpisodeDeliveryService` 只接受当前、已完成且本地存在的成片。
- 分镜已有当前图片/视频指针、绑定项目资产 ID、已保存图片/视频配方、待复核状态；新交付服务按本集顺序输出整集或选中镜头包。

## 失败测试或失败证据

实施前先建立（或改写）失败证据，再改实现。至少覆盖：

1. 项目列表页仍存在「导入 ZIP / 导出项目 ZIP」入口或对应 API 仍可成功——应删除后失败或不存在。
2. 请求旧 `GET /dramas/:id/export`、`POST /dramas/import`——应 404 或不存在（不允许残留工程 ZIP 合同）。
3. 本集已有选用镜头视频时，导出镜头包缺视频、缺分镜图、或缺本镜绑定资产标准图——应失败或按合同明确跳过并在 meta 标明；默认倾向：**缺镜头视频则该镜不得进入包，整集模式缺任一选中镜视频则明确失败**。
4. 导出内容不得包含资产卡「生成输入参考图」、不得倾倒全部 generation 历史；只含当前选用媒体与本镜绑定标准图。
5. 成片导出在无本集合成成片时应明确失败，不得用供应商远程 URL 冒充本地文件。

## 目标

- 用户可从**分镜台（本集上下文）**导出：
  - 成片（本集当前合成预览）；
  - 镜头包（整集或选中镜头）。
- 项目列表页不再提供 ZIP 导入/导出。
- `spec.md` / `README.md` / `media-archive` skill 不再把项目 ZIP 列为当前能力；改为描述成片与镜头包导出。
- 本地项目数据与媒体归档目录继续是唯一工程真相；导出只做**交接交付物**，不做往返恢复。

## 不做范围

- 不做剧本页「可拍清单」、AI 抽资产、AI 拆镜、剧本与资产自动联动。
- 不做配音 / 字幕 / BGM / 转场 / 精剪。
- 不做跨项目全局资产库。
- 不做「从镜头包再导入恢复工程」；无 ZIP 替代品往返。
- 不改供应商协议、模型目录、分镜组装规则、标脏/复核规则（导出只读当前选用与绑定关系）。
- 不在本任务重做品牌、远端或无关 UI 重构。

## 设计

### 产品合同

| 能力 | 入口 | 产出 |
| :--- | :--- | :--- |
| 导出成片 | 分镜台本集工具栏 | 单个本地 MP4（本集当前合成成片） |
| 导出镜头包 · 整集 | 分镜台本集工具栏 | 一个 ZIP（仅作传输容器，**不是**旧工程归档格式） |
| 导出镜头包 · 选中 | 分镜台先勾选镜头，再导出 | 同上，仅含勾选镜头 |

### 镜头包目录约定（交付形态）

```
{项目名}_第N集_镜头包/
  shots.csv                 # 总表：序号、镜头标题、对白、资产ID、提示词摘要或全文路径
  01_{safeTitle}/
    video.mp4               # 当前选用镜头视频（必有）
    storyboard.jpg          # 当前选用分镜图（若有；无则 meta 标明缺失）
    assets/
      {kind}_{name}.jpg     # 本镜绑定资产的当前标准图（仅标准产出图）
    meta.txt                # 图片/视频最终提示词、资产 ID 与名称、对白/运镜等关键字段
  02_...
  preview.mp4               # 仅「整集导出」且已有成片时可选附带；选中导出默认不附
```

### 规则

1. **只导出当前选用**：分镜图、镜头视频、资产标准图均取业务对象当前指针；不导出 generation 历史堆。
2. **资产图边界**：只带本镜 `storyboard_project_assets` 绑定卡的标准图；**禁止**带入 `input_reference_images`。
3. **文本**：`meta.txt` / `shots.csv` 至少包含资产 ID、对白、已保存的图片/视频最终提示词（全文可接受）；不在导出时重新组装配方。
4. **就绪**：
   - 成片导出：必须有本集本地合成成片文件。
   - 镜头包：每个导出镜头必须有本地可用镜头视频；整集模式若用户未改选，则对「本集全部镜头」检查，缺视频明确失败并列出镜头。
   - 待复核：建议允许导出但在 `shots.csv` / `meta` 标记 `needs_review`；若不做标记则须在 UI 警告。二选一写进实现与测试，推荐**允许导出 + 标记**。
5. **传输 ZIP**：镜头包可以用 zip 作为下载容器，但 payload **不得**再使用旧 `format: 8` / `project.json` 工程归档协议；命名与文案必须是「镜头包」或「成片」，禁止再叫「导出项目 / 导入项目」。
6. **删除面**：移除 `projectArchiveService` 工程导入导出、对应路由、前端导入上传、ZIP 往返测试、文档与 skill 中的项目 ZIP 合同；清理仅服务于工程 ZIP 的依赖与临时目录逻辑（若镜头包仍需 zip 流式写出可保留 `archiver`，但导入/`unzipper` 工程恢复链应删除）。

### 事实 owner

- 新导出编排服务（建议名如 `episodeDeliveryService` 或等价）为镜头包/成片打包的唯一 owner。
- 路由挂在剧集/分镜工作区上下文（例如 `/dramas/:id/episodes/:episodeId/export-preview` 与 `.../export-shots`），不要挂回「项目工程归档」语义。
- 前端分镜台负责选中态与触发下载；项目列表页删除导入/导出按钮。

## 实施任务

- [x] 调查：列出 ZIP 导入导出的全部引用，确认整集成片指针与分镜当前媒体字段。
- [x] 删除：工程 ZIP 服务、导入导出路由、前端入口与 API、ZIP 往返测试；更新 container 注册。
- [x] 实现：成片下载接口 + 镜头包（整集/选中）打包下载；分镜台 UI（导出成片、勾选、导出镜头包）。
- [x] 测试：先红后绿；覆盖删除旧合同、成片失败/成功、镜头包缺视频失败、只含当前选用与绑定标准图、选中子集、禁止输入参考图泄漏。
- [x] 文档：更新 `spec.md`、`README.md`、`.agents/skills/media-archive/SKILL.md`；`history.md` 已追加本轮取舍。
- [x] 验证与收口：已跑约定验证命令，回填本 plan 收口区；不执行 commit/push。

## 验证计划

- 后端：定向新/改测试 + 全量 `npm test` / `typecheck` / `build`（以仓库当前脚本为准）。
- 前端：相关测试 + `lint` / `typecheck` / `build`；确认项目页无 ZIP 入口，分镜台有导出入口。
- 静态检查：生产源码中不再存在可调用的工程 `format: 8` / `project.json` 导入导出合同；旧 URL 仅由 404 回归测试断言。
- 真机手测（可列未验证）：对本集合成成片下载；选中 1–2 镜导出镜头包并在资源管理器打开核对目录。
- 分开报告：确定性自动测试 / 人工打开包验收 / 未验证项。

## 收口

- 完成事实：删除工程 ZIP 往返；新增分镜台成片下载与整集/选中镜头包交付；镜头包仅写当前媒体、绑定资产标准图和文本 meta，输入参考图与 generation 历史不进入包。
- 实际命令：后端 `npm.cmd test`（80/80）、`npm.cmd run typecheck`、`npm.cmd run build`；前端 `npm.cmd test`（25/25）、`npm.cmd run lint`、`npm.cmd run build`。
- 未验证项：未进行真实供应商调用、浏览器人工操作和资源管理器打开 ZIP 的人工验收。
- 剩余风险：镜头包缺分镜图或资产标准图时按合同允许导出并在 `meta.txt` 标记缺失；前端保留 4 条既有 lint warning，构建保留 Vite 大包体 warning。
- 外部操作（commit/push）：owner 已明确授权，本次执行。
