# 当前任务：断裂式变更为土豆漫画

> ## 测试铁律（置于一切之前，不可违反）
>
> **绝对禁止编写负性测试。**  
> 禁止以「不存在 / 不应出现 / 不要 / 不得包含 / 已删除 / 404 证明没了 / 符号扫描为 0」等为断言目标的测试用例。此类测试毫无意义，不准写、不准留、不准用负例冒充验收。  
> **只用正向测试保护正向行为**：断言用户与系统**应当成立**的结果——组装成功、生图/归档完成、字层可保存、四格页导出文件可读且尺寸符合模板、TTS 归档后本地文件可用、话数包导出成功、路由 `/comic/...` 可打开工作区、文案展示为「话」等。  
> 缺前置条件时的**明确拒绝**（例如四格缺底板导出失败并返回可见错误）属于正向契约，允许写；但禁止写成「旧短剧视频接口不存在」这类追悼式负测。  
> 短剧残骸靠实现里直接删掉 + 实现代理交付时自查；**不要**为此写自动化负测。

> ## 体验与代码量铁律（与测试铁律同级，不可违反）
>
> **本任务是「成熟 AI 短剧工作台 → 成熟 AI 漫画工作台」，不是 Demo、不是薄壳、不是先砍再补。**  
> Owner 已在 `history.md`（2026-09-21「土豆漫画断裂改造两轮失败」）记录两轮偷懒：**+400 级新增 / -6000 级删除**、分镜台 820 行压到 25 行或压缩成约 42% 字符量的巨行 JSX——**一律判失败并作废**。再犯同构错误视为未执行本合同。
>
> ### 量尺（交付前必须贴数字，差额为负=未完成）
>
> | 量尺 | 基线（`4ba454c` / 当前代码基线） | 硬要求 |
> | :--- | :--- | :--- |
> | 分格工作台文件 | `StoryboardWorkspace.tsx` ≈ **820 行 / ≈44872 字符** | 改写后的分格台（可改名 `PanelWorkspace.tsx`）字符量 **≥ 旧台**，**只能多不能少** |
> | 整仓相对基线的 `git diff --shortstat` | 成熟短剧台完整树 | **禁止**出现「+几百 / -几千」这种腰斩式净删。删视频主链省下的行，**必须**被漫画层（字层/页/声/话数包）与同级 UI/测试填回。owner 可接受的净变化量级大约是 **±两三百行量级的偏差**，不是 ±六千。若 shortstat 净删超过约 **800 行**，默认视为又在偷懒阉割，必须解释每一大块删除对应补回了什么，且工作台量尺仍须先过关 |
> | 可读性 | 旧台正常分行 | **禁止**把数百行逻辑压成几十行巨型单行 JSX 伪装「文件还在」；压缩巨行 = 作弊 = 未完成 |
> | 注水 | — | **禁止**为凑字符量写空注释、重复样板、无用抽象、死代码、假组件。多出来的只能是真实漫画能力与同级交互 |
>
> ### 母版改写（唯一合法前端做法）
>
> 1. 必须用命令取出母版全文，禁止凭记忆从零写薄文件：  
>    `git show 4ba454c:frontend/src/features/workspace/StoryboardWorkspace.tsx`  
> 2. **在母版上改写**：分镜→分格；删除视频区块；在原轨道/预览/检查器结构里接入字层、页导出、配音、话数包。  
> 3. **禁止**删除母版后另起 25～40 行壳；禁止在压缩壳上打补丁装厚。  
> 4. 三栏密度必须保留：轨道（缩略图+勾选+待复核/待重装）→ 预览（底板+字层可拖）→ 检查器（规格 / 显式组装与配方快照 / 能力摘要与手选画幅 / 底板生成·上传·历史选用 / 字层 / 配音 / 页模板导出）。少一整块 = 阉割。
>
> ### 工序锁死（跳步无效）
>
> ① 工作台过字符量闸门 + 主路径可点通 → ② 只删视频相关测、恢复/迁好运非视频测并补漫画正向测 → ③ **最后**才改 `spec` / `README` / skill / 品牌文案收口。  
> 未过 ① 时禁止写「交接摘要 / 已完成 / 可以收口」；禁止先改文档装完成。
>
> ### 完成自检句（每次声称前端完成前必答）
>
> 「去掉话/分格/漫画新词后，还像不像 `4ba454c` 那个成熟分镜台？」不像 = 没做完。  
> 同时贴出：旧台字符量、当前工作台字符量、差额、`git diff --shortstat`。

> **变更类型：断裂式重建（Breaking Rebuild）**  
> Owner 已清空旧短剧业务数据。本任务**不做**数据库前向迁移、**不做**旧视频字段兼容、**不做**旧工程 ZIP / format:8 往返、**不做**「短剧+漫画双模式并存」。  
> 理念合同以仓库根目录 `AI漫画工作流.md` 为准；本 `plan.md` 是执行合同。实施中若与工作流冲突，先改工作流或本计划，再改代码。  
> **写入纪律**：实施代理应分批改文件，禁止一次性巨量覆盖无关模块。  
> **代码基线**：仓库已回退并推送文档合同；实现从当前 `master` 上的成熟短剧代码起步（含完整 `StoryboardWorkspace`），**不要**续做已作废的薄壳工作区。失败教训见 `history.md` 同日条目。

## 需求

1. 产品由「土豆短剧」断裂为「土豆漫画」：删除图生视频、视频配方、整集视频合成等短剧主链。  
2. 保留并改写：项目 / 话（原集）/ 项目级角色·场景·道具定妆 / 分格（原分镜）底板生图 / 显式组装 / 本地归档 / 单格返工。  
3. **体验零阉割**：旧成熟分镜台的交互密度、检查器、门闸、历史选用、待复核、空状态与错误可见性必须继承；只做语义替换与视频位→漫画位替换，不把工作台变瘦。  
4. 新增编排层（必须有完整 UI 主路径，禁止只接 API）：  
   - **B 字层**：气泡贴字可拖（非生图）；文案 + 气泡类型 + 位置/缩放均进 UI  
   - **C 页合成**：单格 / 四格 2×2 / 竖四格模板引用底板；**必须能导出合成页图（含四格）**；拼页几何必须基于各槽**真实底板像素比例**，禁止伪造通用比例表  
   - **D 声层**：PearAPI `POST/GET https://api.pearapi.ai/api/freedub`，按格 TTS，下载本地归档  
5. **定妆标准图与分格底板均支持 AI 生成或本地上传**（上传后同样走本地归档与选用指针）。  
6. **画幅/比例/参考图上限**：只投影当前手选图片模型真实能力；缺手选画幅则禁用生成并说明原因；不合并多模型、不注入通用比例表。  
7. 对用户可见文案统一称「话」；禁止「集」。代码可用 `episode`。  
8. 交接：话数包 + 合成页导出；工程 ZIP 若仍在则删除。  
9. **真实外部全链验收以 Agnes 图片模型为主**（免费）；确定性测试通过后跑通定妆→底板（含上传抽查）→四格导出；TTS 用 freedub 分开报告。  
10. **代码量纪律见文首铁律**：工作台只能多不能少；整仓禁止 +400/-6000 式偷懒差。

### 命名硬规则（验收必查）

| 表面 | 允许 | 禁止（产品主路径） |
| :--- | :--- | :--- |
| 用户可见中文 | 话、分格、分格台、底板、话数包 | 集、分镜（指镜头）、镜头视频、预览成片 |
| 代码标识符 | `episode`（话）、`panels`（分格表）、`panel` | 用户 `message` 写「第 N 集」；产品路由 `/film/` |

### 分格台主路径（少一步都算没做完）

用户打开 `/comic/:id/panels`，不读文档即可完成：

选格 → **显式组装** → 保存配方快照 → 选手选画幅 → 生成或上传底板 → 历史选用/清除 → 改字层（含拖拽位置）→ 配音 → 导出单格/四格/竖四格页 → 导出话数包。

门闸与错误可见性不低于 `4ba454c` 分镜台。

## 当前事实（实施前基线）

- 代码与文档合同已在 `master`：成熟短剧实现 + 漫画 `plan.md` / `AI漫画工作流.md` / history 失败记录；**无**合法进行中的漫画薄壳实现可续做。  
- 现 `spec.md` 仍描述短剧主链：`分镜台双配方 → 分镜图 → 镜头视频 → 整集合成`。  
- 后端存在视频生成、compose 整集合成、`video_generations`、分镜视频字段与待复核；工程 ZIP 已在 `4ba454c` 删除，交付曾为成片/镜头包。  
- 前端工作区为剧本 / 资产 / 分镜；**成熟** `StoryboardWorkspace`（约 820 行 / 约 45k 字符）含出视频与镜头包等。  
- Provider 公开图片+视频；漫画主路径只需图片 + `freedub` TTS。  
- Owner：**数据已清空** → 允许空库新 schema，不必迁移。  
- 理念已定：`AI漫画工作流.md`。  
- **history 已钉死**：目录能力投影、手选画幅、禁止通用比例表、`adapter-override`；以及 **两轮漫画薄壳/压缩壳失败**——实施代理必须先读该条目再动手。

## 正向测试合同（先红后绿）

只写保护下列**应当成立**行为的测试；名称与断言都说「做成了什么」，禁止「没有什么」：

1. 显式组装产出可用的 `panelRecipe`（含 prompt 与按 ID 带入的资产标准图 references），保存后生图入口消费该快照。  
2. 字层保存与重读成功；更新字层后底板指针与 generation 历史保持可用。  
3. **四格导出成功**：`grid_2x2` 填满 4 个本地底板 → 本地可读图片；画布由真实宽高比拼出。  
4. **四格导出明确拒绝**：缺底板或比例不一致 → 失败且错误可见。  
5. **底板上传成功**：上传可作为当前底板选用。  
6. **生图画幅手选**：仅当前图片预设目录画幅；无画幅则禁用并说明原因。  
7. **TTS 成功归档**：业务指针指向本地可读音频（禁止业务指针留 `http(s)`）。  
8. 话数包导出成功。  
9. `/comic/:id/...` 可达；用户可见「话」。

**测试删除边界（防偷懒掏空回归）：**

- **允许删除**：明确只服务视频主链的测试文件（视频生成、compose、成片/镜头包、storyboard 视频配方/视频就绪等）。  
- **禁止整文件删除**：provider / transport / registry / errorContract / audit / config / 资产组装 / mediaReference / taskProgress 等非视频测；应保留或迁到 `panels` 语义后仍保护同等行为。  
- **禁止**用一份十几行压缩漫画测顶替被删的大片回归面。  
- **不要**改写成「视频路由应 404」负测。

## 目标

- 自动测试与文档共同证明：土豆漫画主链可运行，且工作台体验与代码量不低于成熟短剧台。  
- 主链：`剧本 → 定妆 → 分格底板 → 字层 → 页模板（可导出四格页）→ 可选 TTS → 话数包`。  
- 短剧视频主链从代码、API、UI、spec、README、相关 skill 主路径消失。  
- **消失的是视频能力，不是工作台厚度。**

## 不做范围

- 不做旧数据迁移、双写、兼容层、旧 ZIP 导入。  
- 不做 AI 写剧本 / 自动拆格 / AI 一次画四格 / I2V / 口型。  
- 不做自由排版画布、出版级精排、逐气泡高亮听书、完整 DAW。  
- 不做短剧模式开关或「同一产品双主链」。  
- **不做「先薄壳能跑再补厚度」**；不做压缩巨行伪装；不做未过工作台量尺就文档收口。  
- 默认不 commit/push，除非 owner 当次授权。

## 设计

### 断裂策略

1. **唯一主干**：漫画工作流；删除短剧视频主干，不留「废弃但可调用」的产品入口。  
2. **空库重建**：schema 以漫画字段为准直接建表/改表；允许删除 `video_*` 列、视频 generation 表或停用其产品路径。  
3. **体验不断裂**：领域与媒体主链可断；工作台成熟度不断。删除视频相关代码后，必须用漫画 UI/服务填回厚度，使整仓 diff 与工作台量尺符合文首铁律。  
4. **事实 owner**：  
   - 底板配方：assembler 本轮改为 panel 专用（如 `panelPromptAssembler`）  
   - 资产生成提示词：保持显式组装 owner  
   - 页合成：新建 page layout 服务（模板 + 槽位 + 真实像素拼图导出）  
   - TTS：新建 freedub 适配器 + 音频归档  
   - 字层：captions（文案+气泡类型+归一化几何）  
5. **品牌与路由（本轮一次改完）**：显示名「土豆漫画」；`/comic/:id/script|assets|panels`；删除产品主路径 `/film/`；用户可见一律「话」。  
6. **标脏按层（禁止剧本一刀切脏底板）**：  
   - 只改对白/旁白草稿或 B 字层 → 不自动脏 A  
   - 改出场资产、底板规格、资产标准图/文本影响引用 → 脏 A  
   - 只改 C 槽位/模板或只重跑 TTS → 不脏 A  
   - 剧本大改情节不自动扫全话底板为脏  
7. **定妆→底板一致性**：组装时出场资产标准图按 ID 写入 references；上限不足 → 明确失败（优先），禁止静默丢图。  
8. **阅读序**：分格 `order` 为故事阅读唯一真相；`reading_order.json` 含分格序与页序。

### 旧分镜台 → 漫画分格台替换表（实施时逐项勾销）

| 旧成熟台能力位 | 漫画落点 | 禁止的偷懒形态 |
| :--- | :--- | :--- |
| 镜头轨道+缩略图+勾选 | 分格轨道+缩略图+勾选 | 纯文字列表无预览 |
| 预览区+空状态 | 底板预览+空状态 | 无预览仅表单 |
| 规格检查器 | 分格规格检查器 | 只留标题/对白两字段 |
| 显式组装+配方可编辑快照 | 同样保留组装按钮与可编辑最终提示词/参考图 | API 有组装、UI 无按钮 |
| 图片能力摘要+手选画幅门闸 | 仅图片模型四项投影；无画幅禁用 | 去掉门闸或自动兜底 |
| 生成/上传/历史选用/清除 | 底板同等 | 只能生成不能上传或无历史 |
| 图片待复核 / 配方待重装 | 保留（无视频复核） | 去掉标签与确认 |
| 视频配方 / 出视频 / 成片 / 镜头包 | **删除**；原检查器/顶栏位置改接字层、页导出、配音、话数包 | 删掉后留下空白不补 |
| 任务跟踪与错误可见 | 继续用现有 generation tracker / 错误合同 | 失败无提示 |

### 数据形态（空库直接建成；禁止「表名暂留」）

- `dramas` / `episodes`（语义=话）/ `project_assets` 保留  
- **`panels`**：由 `storyboards` 本轮改名/重建；含配方、底板指针、captions、音频指针、图片待复核、配方待重装；**无视频字段**  
- `page_layouts`：`single` / `grid_2x2` / `vertical_4`  
- `image_generations` 保留；删除 `video_generations` 与视频 compose  
- 音频：本地路径为业务指针；来源 URL 仅审计

### freedub 对接

- `GET /api/freedub`：role/style 列表  
- `POST`：`{ text, role?, style? }` → 下载 `audio_url` → 本地归档 → 绑格  
- 完成断言：业务指针非 `http(s)`；本地文件存在

### 供应商能力与画幅纪律（继承 2026-09-21，不得回退）

| 投影项 | 含义 |
| :--- | :--- |
| **模式** | 文生图 / 图生图；由是否有参考图决定 |
| **参考图上限** | UI 显示已选/上限；超限拒绝，禁止静默丢图 |
| **画幅列表** | 仅目录声明；必须手选；禁止通用比例表与首项兜底 |
| **能力来源** | 目录原始或 `adapter-override` |

资产页与分格台只投影当前手选图片预设；无预设或无画幅 → 禁用生成。定妆与底板均支持生成或上传；上传像素即拼页比例事实。

### 页导出

- 模板：`single` | `grid_2x2` | `vertical_4`  
- 只认本地底板真实宽高；同页比例必须一致（约 1px 误差），否则明确拒绝  
- 缺槽/缺底板 → 明确失败；烧字可二期

### UI 阶段

- 总览与剧本（话）  
- 项目资产库（定妆：生成或上传 + 手选画幅；**保持现有成熟资产页密度，禁止借机阉薄**）  
- 分格台：按文首母版改写 + 底板/字/页/TTS/话数包  

删除产品入口：出视频、整集视频合成、成片/镜头包（若仍在）、工程 ZIP。

## 实施任务

### A. 调查与影响地图

- [ ] 通读 `AI漫画工作流.md`、本 plan、`history.md` 两轮失败条目、`AGENTS.md`、`spec.md`、命中 skill。  
- [ ] 用 `git show 4ba454c:.../StoryboardWorkspace.tsx` 核对母版行数/字符量，写入影响地图。  
- [ ] 列出短剧视频相关：路由、服务、类型、前端 API、字段、compose、测试名、文档段落。  
- [ ] 列出可保留：项目/话/资产/图片生成/任务/归档/显式组装/成熟 UI 结构。  
- [ ] 列出视频位删除后，字层/页/声/话数包如何填回同一检查器与顶栏。  
- [ ] 确认 PearAPI 客户端如何复用到 `freedub`。

### B. 断裂删除短剧视频主干（同时准备填回）

- [ ] 删除分镜视频提交、视频配方、视频待复核、整集 compose、成片/镜头包主路径 UI。  
- [ ] Schema/类型去掉视频产品字段。  
- [ ] 删除**仅**视频相关测试；保留非视频测。  
- [ ] 保留手选画幅门闸与目录能力投影纪律。  
- [ ] 自查：本批删除对应的漫画填回项已列入 C–G，禁止「只删不补」。

### C. 分格底板合同改写

- [ ] 组装只出 `panelRecipe`；标准图按 ID 进 references；上限不足失败可见。  
- [ ] 底板 AI 生成 + 本地上传；手选画幅。  
- [ ] 文案分格/话；标脏按层；待重装/待复核仅图片。

### D. 字层 B

- [ ] captions 持久化（文本、气泡、位置/缩放）。  
- [ ] UI：气泡+旁白；**可拖**；改字不调生图。  
- [ ] 话数包可带 `captions.json`。

### E. 页合成 C

- [ ] 模板注册表；layout；按 order 填槽。  
- [ ] 真实宽高拼页；同比例门闸；成功与拒绝均可测。  
- [ ] `reading_order.json` 含分格序与页序。

### F. 声层 D（freedub）

- [ ] GET/POST 适配、归档、本地文件断言。  
- [ ] UI：按格 TTS；role/style。  
- [ ] 真实调用与 Agnes 分开报告。

### G. 分格台前端（母版改写 · 量尺必过）

- [ ] 从 `4ba454c` 母版展开落盘，再改语义（禁止另起薄壳）。  
- [ ] 贴出旧/新字符量与差额（差额 ≥ 0）。  
- [ ] 三栏与替换表全部勾销；主路径九步可点通。  
- [ ] `git diff --shortstat` 净删未呈腰斩；若净删偏大须逐项说明填回。

### H. 交接与清理

- [ ] 话数包导出（整话/选中）。  
- [ ] 删除工程 ZIP 与视频成片路径（若还在）。

### I. 文档与 skill（仅当 G 量尺已过）

- [ ] 重写 `spec.md`。  
- [ ] 更新 `README.md`、`history.md`（成功交付时追加，勿抹掉失败条目）、相关 skills。  
- [ ] 品牌「土豆漫画」；路由 `/comic/`。

### J. 验证与收口

- [ ] 全量正向 test / typecheck / build / lint。  
- [ ] 工作台字符量闸门与 shortstat 自查写入收口。  
- [ ] **Agnes 真实全链**证据写入收口。  
- [ ] freedub 真实调用分开报告。  
- [ ] 交付前自查短剧残骸已删（不写负测）。  
- [ ] 回填收口；未授权不 commit/push。

## 模块影响地图（实施时逐项勾销）

| 区域 | 动作 |
| :--- | :--- |
| `panelPromptAssembler` | 本轮改名 + 仅 panel 配方；厚度不低于旧 storyboard 图片组装 |
| 页图合成 / 原 composition 视频 | 删视频 concat；新页导出服务 |
| `*video*` 生成服务/路由 | 产品主路径删除 |
| `episodeDeliveryService` 成片/镜头包 | 删除；改为话数包 + 页导出 |
| `workspaceRoutes*` | 分格/字层/页导出/TTS；入口齐全 |
| Pear 适配器 | 新增 freedub；视频适配器移出产品主路径（内部协议测可留） |
| schema | `panels`；去视频；加字层/页/音频 |
| `App.tsx` | `/comic/:id/...`；删 `/film/` |
| **分格台前端** | **母版改写 + 量尺 ≥ 旧台** |
| 资产页 / 剧本页 | 语义替换；**禁止借机阉薄** |
| `ProjectsPage` | 导航进 `/comic/` |
| `frontend/src/api/*` | 去 video/compose；加 panel/page/tts |
| 测试 | 只删视频测；补漫画正向测；禁止掏空 |
| `spec` / `README` / skills | 最后收口 |
| Demo | 漫画话/分格示例（无视频） |

## 验收标准（完成定义）

1. 空库走通主路径九步；体验密度不低于旧分镜台。  
2. **工作台字符量 ≥ 旧 `StoryboardWorkspace`；非压缩巨行；未注水。**  
3. **整仓 diff 无 +几百/-几千腰斩；净变化与「删视频 + 补漫画」叙事一致。**  
4. 字层可存可拖可重读；改字后底板仍可读。  
5. 同比例四格导出成功；缺底板/比例不一致明确拒绝。  
6. TTS 本地音频可读；话数包成功；`/comic/` +「话」+「土豆漫画」。  
7. 手选画幅/目录投影纪律保持。  
8. 非视频回归测未被整锅端掉。  
9. 确定性正向测试通过；Agnes 与 freedub 分开写入收口。

## 验证计划

- 自动：全量正向 test、typecheck、build、lint。  
- **量尺**：汇报旧/新工作台字符量、差额、`git diff --shortstat`。  
- 必过正向：组装、上传、四格成功与拒绝、TTS 归档、话数包。  
- Agnes 图片全链必做；freedub 另报。  
- 禁止负性测试；分开报告：自动 / 量尺 / Agnes / freedub / 人工 / 未验证。

## 风险

- 烧字进四格页可降级；**四格底板合成不可降级**。  
- freedub 临时链：只信本地归档。  
- 参考图上限不足：失败可见。  
- **最大风险（已发生两次）**：把断裂理解成整台变瘦、压缩壳、掏空测试、未达标先收口。本合同文首铁律专克此风险。  
- 禁止 I2V 回潮；禁止自由画布膨胀；复杂页模板首期不做。

## 收口

- 完成事实：（未开始实施；勿沿用任何已作废薄壳/压缩壳交接摘要）  
- 实际命令：  
- 量尺数字（旧字符量 / 新字符量 / 差额 / shortstat）：  
- 未验证项：  
- 剩余风险：  
- 外部操作（commit/push）：默认未执行；需 owner 当次授权。
