# 当前任务：断裂式变更为土豆漫画

> ## 测试铁律（置于一切之前，不可违反）
>
> **绝对禁止编写负性测试。**  
> 禁止以「不存在 / 不应出现 / 不要 / 不得包含 / 已删除 / 404 证明没了 / 符号扫描为 0」等为断言目标的测试用例。此类测试毫无意义，不准写、不准留、不准用负例冒充验收。  
> **只用正向测试保护正向行为**：断言用户与系统**应当成立**的结果——组装成功、生图/归档完成、字层可保存、四格页导出文件可读且尺寸符合模板、TTS 归档后本地文件可用、话数包导出成功、路由 `/comic/...` 可打开工作区、文案展示为「话」等。  
> 缺前置条件时的**明确拒绝**（例如四格缺底板导出失败并返回可见错误）属于正向契约，允许写；但禁止写成「旧短剧视频接口不存在」这类追悼式负测。  
> 短剧残骸靠实现里直接删掉 + 实现代理交付时自查；**不要**为此写自动化负测。

> **变更类型：断裂式重建（Breaking Rebuild）**  
> Owner 已清空旧短剧业务数据。本任务**不做**数据库前向迁移、**不做**旧视频字段兼容、**不做**旧工程 ZIP / format:8 往返、**不做**「短剧+漫画双模式并存」。  
> 理念合同以仓库根目录 `AI漫画工作流.md` 为准；本 `plan.md` 是执行合同。实施中若与工作流冲突，先改工作流或本计划，再改代码。  
> **写入纪律**：实施代理应分批改文件，禁止一次性巨量覆盖无关模块。

## 需求

1. 产品由「土豆短剧」断裂为「土豆漫画」：删除图生视频、视频配方、整集视频合成等短剧主链。  
2. 保留并改写：项目 / 话（原集）/ 项目级角色·场景·道具定妆 / 分格（原分镜）底板生图 / 显式组装 / 本地归档 / 单格返工。  
3. 新增编排层：  
   - **B 字层**：气泡贴字可拖（非生图）  
   - **C 页合成**：单格 / 四格 2×2 / 竖四格模板引用底板；**必须能导出合成页图（含四格）**；拼页几何必须基于各槽**真实底板像素比例**（由生成时供应商目录画幅或上传图实际尺寸决定），禁止用伪造通用比例表硬套  
   - **D 声层**：PearAPI `POST/GET https://api.pearapi.ai/api/freedub`，按格 TTS，下载本地归档  
4. **定妆标准图与分格底板均支持 AI 生成或本地上传**（上传后同样走本地归档与选用指针）；不是只能 AI。  
5. **画幅/比例/参考图上限**：只投影并消费当前所选图片模型在实时目录（及已核验 `adapter-override`）中的真实能力；缺手选画幅则禁用生成并说明原因；不合并多模型能力、不注入通用比例表（纪律同 history 中制作页能力投影与手选门闸）。  
6. 对用户可见文案统一称「话」；禁止「集」。代码可用 `episode`。  
7. 交接：话数包 + 合成页导出；删除工程 ZIP（若仍在）。  
8. **真实外部全链验收以 Agnes 图片模型为主**（免费）；在确定性测试通过后，用 Agnes 跑通定妆→底板（含至少一次上传路径抽查）→四格导出；TTS 用 freedub 真实调用分开报告。
### 命名硬规则（验收必查）

| 表面 | 允许 | 禁止（产品主路径） |
| :--- | :--- | :--- |
| 用户可见中文 | 话、分格、分格台、底板、话数包 | 集、分镜（指镜头）、镜头视频、预览成片 |
| 代码标识符 | `episode`（话）、`panels`（分格表）、`panel` | 用户 `message` 写「第 N 集」；产品路由 `/film/` |

## 当前事实（实施前基线）

- 现 `spec.md` 仍描述短剧主链：`分镜台双配方 → 分镜图 → 镜头视频 → 整集合成`。  
- 后端存在视频生成、compose 整集合成、`video_generations`、分镜视频字段与待复核、项目 ZIP `format: 8` 等。  
- 前端工作区为剧本 / 资产 / 分镜；分镜台含出视频与合成整集。  
- Provider 公开图片+视频；理念上漫画主路径只需图片 + `freedub` TTS。  
- Owner：**数据已清空** → 允许空库新 schema，不必迁移。  
- 理念已定：`AI漫画工作流.md`。  
- **2026-09-21 history 已钉死**：目录能力投影、手选画幅、禁止通用比例表兜底、`adapter-override` 诚实标记、参考图上限门闸；漫画改造必须继承，拼四格也必须吃真实底板像素/目录画幅，不能另造一套假比例。

## 正向测试合同（先红后绿）

只写保护下列**应当成立**行为的测试；名称与断言都说「做成了什么」，禁止「没有什么」：

1. 显式组装产出可用的 `panelRecipe`（含 prompt 与按 ID 带入的资产标准图 references），保存后生图入口消费该快照。  
2. 字层保存与重读成功；更新字层后底板指针与 generation 历史保持可用（正向：仍能读取原底板）。  
3. **四格导出成功**：`grid_2x2` 填满 4 个本地底板 → 得到一张本地可读图片；**画布由各底板真实宽高比拼出**（例如均为目录声明的 4:3，则按 4:3 格拼 2×2，总画幅反映真实比例，禁止假比例表）。  
4. **四格导出明确拒绝（正向拒绝）**：缺底板，或四张底板像素比例不一致 → 失败且错误可见。  
5. **底板上传成功**：本地上传可作为当前底板选用，与 AI 生成同等归档纪律。  
6. **生图画幅手选**：仅列出当前图片预设目录画幅；请求携带所选值；无画幅则禁用生成并说明原因（延续 2026-09-21 手选门闸）。  
7. **TTS 成功归档**：业务指针指向本地存在可读的音频文件。  
8. 话数包导出成功。  
9. `/comic/:id/...` 可达；用户可见「话」。

删除旧短剧视频相关测试文件即可；**不要**改写成「视频路由应 404」负测。

## 目标

- 自动测试与文档共同证明：土豆漫画主链可运行。  
- 主链：`剧本 → 定妆 → 分格底板 → 字层 → 页模板（可导出四格页）→ 可选 TTS → 话数包`。  
- 短剧视频主链从代码、API、UI、spec、README、相关 skill 主路径消失。

## 不做范围

- 不做旧数据迁移、双写、兼容层、旧 ZIP 导入。  
- 不做 AI 写剧本 / 自动拆格 / AI 一次画四格 / I2V / 口型。  
- 不做自由排版画布、出版级精排、逐气泡高亮听书、完整 DAW。  
- 不做短剧模式开关或「同一产品双主链」。  
- 默认不 commit/push，除非 owner 当次授权。

## 设计

### 断裂策略

1. **唯一主干**：漫画工作流；删除短剧视频主干，不留「废弃但可调用」的产品入口。  
2. **空库重建**：schema 以漫画字段为准直接建表/改表；允许删除 `video_*` 列、视频 generation 表或停用其产品路径。  
3. **事实 owner**：  
   - 底板配方：assembler **本轮**改为 panel 专用（文件/符号名同步漫画化，如 `panelPromptAssembler`；禁止长期挂着 storyboard* 当漫画主路径）  
   - 资产生成提示词：保持显式组装 owner  
   - 页合成：新建 page layout 服务（模板注册表 + 槽位引用 + 栅格拼图导出）  
   - TTS：新建 freedub 适配器 + 音频归档；列表 GET、生成 POST  
   - 字层：分格上的 captions 结构（文案+气泡类型+归一化几何）  
4. **品牌与路由（本轮一次改完，不留债）**：显示名「土豆漫画」；前端路由改为 `/comic/:id/...`（如 `/comic/:id/script`、`/comic/:id/assets`、`/comic/:id/panels`）；**删除**产品主路径上的 `/film/:id/...`，不做「暂留 film、二期再改」。旧书签无需兼容（断裂 + 数据已空）。**所有用户可见字符串必须是「话」**（见命名硬规则）。  
5. **标脏按层（禁止剧本一刀切脏底板）**：  
   - 只改剧本里的对白/旁白草稿、或只改 B 字层 → **不**自动脏 A 底板  
   - 改分格出场资产、底板相关规格、或资产标准图/文本影响引用 → 脏 A（配方待重装 / 底板待复核按现有纪律）  
   - 只改 C 槽位引用 / 模板 → 不脏 A  
   - 只重跑 TTS → 不脏 A  
   - 剧本大改「场次/情节」**不自动**扫全话底板为脏；由用户在分格台逐格处理（避免改一句对白整话报警）  
6. **定妆→底板一致性**：组装时出场资产标准图必须按 ID 写入 `panelReferences`（有标准图则必带，不能只靠文本）；模型声明的参考图上限不足时 → **明确失败或截断策略写进 spec**（优先失败并提示减资产/换模型），禁止静默丢图还假装双通道成立。  
7. **阅读序**：分格列表 `order` 为故事阅读唯一真相；页槽位可手填，但「按序填槽」助手须按 `order` 填充；`reading_order.json` 同时记录分格序与页序（layout 列表 + 每页槽位 panel id）。

### 数据形态（断裂空库直接建成下表；禁止「先将就再改名」）

- `dramas`：项目 + 故事总览（保留）  
- `episodes`：话 + 剧本结构（表名可保留 `episodes`，语义=话；用户文案禁止「集」）  
- `project_assets`：三类卡（保留）  
- **`panels`（分格表）**：由原 `storyboards` **本轮改名/重建**，禁止「表名仍叫 storyboards、只改注释」；字段含有序格、图片配方、底板指针、字层 JSON、音频指针、图片待复核、配方待重装；**无视频字段**  
- 页布局表/结构：template 注册表数据驱动（`template_id` + 槽位数 + 栅格几何）+ 每话 layout（slots 引用 panel id）；首期只注册 `single` / `grid_2x2` / `vertical_4`，**不加新模板不等于留路由债**——模板扩展是功能二期，路由本轮必须改完  
- `image_generations` 保留；删除 `video_generations` 与视频 compose  
- 音频记录：text/role/style/**本地路径**；来源 URL 仅审计，不得当业务指针

### freedub 对接

- `GET /api/freedub`：缓存 role/style 列表供 UI  
- `POST` JSON：`{ text, role?, style? }`  
- `code===200` → 下载 `data.audio_url` → 本地归档 → 绑定到格  
- **完成断言**：业务指针禁止 `http(s)://`；本地文件必须存在  
- 配置走现有 PearAPI 凭据/代理策略

### 供应商能力与画幅纪律（继承 2026-09-21 history，漫画后仍生效）

下列纪律来自 history（尤其 9-19 去掉伪造通用画幅、9-21 能力投影 / 协议校正 / 全手选门闸）与 `provider-api-contract` skill；断裂改漫画后**不得回退**。能力摘要只投影真事实：

| 投影项 | 含义（必须来自当前手选图片模型目录或已标 `adapter-override` 的核验映射） |
| :--- | :--- |
| **模式** | 文生图 / 图生图（或目录声明的等价 mode）；有无参考图决定走哪条，**不另造模式名** |
| **参考图上限** | 0 = 只能文生；≥1 = 可带参考图且张数不得超过上限；UI 显示「已选/上限」，达上限禁上传，服务端再拒 |
| **画幅列表** | 仅目录声明的 `aspect_ratio`；生成必须手选其中一项并原样提交 |
| **能力来源** | 目录原始 vs `adapter-override`，不得把补洞伪装成供应商原文 |

执行细则：

1. 资产页与分格台只投影**当前手选图片预设**的上表四项；无唯一预设 → 显示未选择并**禁用生成**；禁止自动选模；禁止把多模型能力合并成一张「假目录」。  
2. **文生 vs 图生以真实参考图为准**：无输入参考图（定妆）或无配方 references（分格，在模型要求参考图时）按目录模式校验；有参考图则张数 ≤ 上限，超限明确拒绝，禁止静默丢图还当成功。  
3. 生成必须手选画幅；请求携带用户选择的 `aspect_ratio`；**禁止**注入通用比例表、禁止默默用首项画幅兜底。目录未声明画幅时按既有合同显示未知并禁用或交供应商验证——**不得本地编造比例列表**。  
4. 适配器补洞必须标 `adapter-override`。  
5. 定妆标准图与分格底板均支持 **AI 生成或本地上传**；完成 = 本地归档 + 选用指针；上传图实际像素即拼页用的比例事实。  
6. AI 配置刷新按「图片 X」如实计数（漫画主路径以图片为准）。

### 页导出

- 首期模板：`single` | `grid_2x2` | `vertical_4`（注册表可扩展；首期不做自由画布）  
- **拼页几何只认真实像素**：读取各槽底板本地文件宽高；四格是「真实底板比例」的 2×2（例如四张均为目录 4:3 出图，则整页为 4:3 格拼成的 2×2），**禁止**按伪造 1:1 格硬拉。  
- **同页门闸（已定）**：同一四格/竖四页内，四张底板像素比例必须一致（允许 1px 误差）；不一致则**明确拒绝导出**并提示统一画幅或重出/重传。不在首期做 letterbox 混拼。  
- 导出一张本地图片；烧字可二期。缺槽或缺底板 → 明确失败。  

### UI 阶段

- 总览与剧本（话）  
- 项目资产库（定妆：生成或上传 + 手选画幅）  
- 分格台：底板生成/上传 + 字层 + TTS + 页模板与按真实比例导出  

删除：出视频、整集视频合成、工程 ZIP 导入导出。

## 实施任务

### A. 调查与影响地图

- [x] 通读 `AI漫画工作流.md`、本 plan、`AGENTS.md`、`spec.md`、命中 skill（project-dev / provider / media-archive / production-pipeline / validation-delivery）。  
- [ ] 列出短剧视频相关：路由、服务、类型、前端 API、分镜字段、compose、video_generations、测试名、README/spec/skill 段落。  
- [ ] 列出可保留：项目/话/资产/图片生成/任务/归档/显式组装。  
- [ ] 确认 PearAPI 现有客户端如何复用到 `freedub`（base URL、鉴权头、超时）。

### B. 断裂删除短剧视频主干

- [ ] 删除分镜视频提交、视频配方、视频待复核、整集视频 compose、视频主路径 UI。  
- [ ] Schema/类型去掉视频产品字段（空库新形状）。  
- [ ] AI 配置主路径以图片为准；删除视频相关测试文件（**不写负测**）。  
- [ ] **保留并延续**手选画幅门闸、目录能力投影、`adapter-override` 诚实标记（见上节纪律）。

### C. 分格底板合同改写

- [ ] 组装只出 `panelRecipe`；出场资产标准图按 ID 进 references；上限不足则失败可见。  
- [ ] 底板 **AI 生成 + 本地上传**；手选画幅只来自当前图片预设目录。  
- [ ] 文案分格/话；标脏按层；待重装/待复核仅图片。

### D. 字层 B

- [ ] 分格持久化 captions（文本、气泡类型、位置/缩放等）。  
- [ ] UI：2～3 种气泡 + 旁白；拖拽改位置；改字不调生图。  
- [ ] 导出话数包可带 `captions.json`。

### E. 页合成 C（含四格导出必做）

- [ ] 模板注册表；layout；按 order 填槽。  
- [ ] **按本地底板真实宽高拼页**；同比例门闸；成功导出可测（例如四张同 4:3 测出 2×2 页）。  
- [ ] `reading_order.json` 含分格序与页序。

### F. 声层 D（freedub）

- [ ] 适配器：GET 列表、POST 生成、错误映射。  
- [ ] 下载归档；测试断言本地音频文件存在且可读。  
- [ ] UI：按格对白 TTS；选 role/style。  
- [ ] 真实调用与 Agnes 生图验收分开报告。

### G. 交接与清理

- [ ] 话数包导出（整话/选中）。  
- [ ] 删除工程 ZIP 与视频 compose 成片路径（若还在）。

### H. 文档与 skill

- [ ] 重写 `spec.md`（含手选画幅、真实比例拼页、上传底板、标脏按层、freedub、命名「话」）。  
- [ ] 更新 `README.md`、`history.md`、相关 skills（含 provider-api-contract / media-archive）。  
- [ ] 品牌「土豆漫画」；路由 `/comic/`。

### I. 验证与收口

- [x] 全量正向 test / typecheck / build / lint。  
- [ ] **Agnes 真实全链**（免费图片）：刷新目录 → 手选预设与画幅 → 定妆（生成与上传路径都要覆盖到）→ 分格底板 → 同画幅四格导出成功打开；证据写入收口。  
- [ ] freedub TTS 真实调用分开报告。  
- [ ] 交付前自查短剧残骸已删（**不写负测**）。  
- [ ] 回填收口；未授权不 commit/push。

## 模块影响地图（实施时逐项勾销）

| 区域 | 动作 |
| :--- | :--- |
| `panelPromptAssembler`（原 storyboard*） | 本轮改名 + 仅 panel 配方 |
| 页图合成服务 / 原 composition 视频路径 | 删除视频 concat；页导出用新服务 |
| `backend/.../*video*` 生成服务/路由 | 从产品主路径删除 |
| `backend/.../projectArchiveService*` + import/export | 删除工程 ZIP |
| `backend/.../workspaceRoutes*` | 分格/字层/页导出/TTS |
| Pear 适配器 | 新增 freedub；视频适配器移出主路径 |
| schema | `panels` 重建；去视频；加字层/页布局/音频 |
| `App.tsx` 路由 | **`/comic/:id/...`**；删除 `/film/` 产品路由 |
| 分格台前端（原 StoryboardWorkspace） | 本轮改名 + 底板/字/页/TTS |
| `ProjectsPage` | 去 ZIP；导航进 `/comic/` |
| `frontend/src/api/*` | 去 video/compose/zip；加 panel/page/tts |
| `spec.md` / `README.md` / skills | 与工作流对齐；文档示例路由用 `/comic/` |
| Demo / `init_demo.py` | 漫画话/分格示例（无视频） |

## 验收标准（完成定义）

1. 空库走通：项目 → 话 → 定妆（生成与上传均可）→ 分格组装 → 手选目录画幅出底板或上传底板。  
2. 字层可存可重读；改字后底板仍可读。  
3. 同比例四格导出成功，页几何反映真实底板比例；缺底板或比例不一致时明确拒绝。  
4. TTS 本地音频可读。  
5. 话数包导出成功；`/comic/` 可进入；称「话」「土豆漫画」。  
6. 手选画幅/目录投影纪律保持（无预设或无画幅则不能生成）。  
7. 确定性正向测试通过；**Agnes 真实全链**与 freedub 真实调用分开写入收口。

## 验证计划

- 自动：全量正向 test、typecheck、build、lint。  
- 必过正向：组装、上传底板、同比例四格导出、比例不一致/缺底板明确拒绝、TTS 本地归档、话数包。  
- **真实外部（Agnes 图片全链必做）**：目录刷新 → 手选 → 定妆 → 底板 → 四格导出打开验图；证据（任务 id、本地路径、画幅）写入收口。  
- freedub TTS 真实调用另报。  
- 禁止负性测试；短剧残骸删除靠实现与自查。  
- 分开报告：自动 / Agnes / freedub / 人工 / 未验证。

## 风险

- 烧字进四格页可降级；**四格底板合成不可降级**。  
- freedub 临时链：只信本地归档。  
- 多参考图模型能力不足：要失败可见，勿静默丢 references。  
- **禁止**「显示名已改、路由/表名暂留」类技术债；`/comic/` 与 `panels` 本轮必须落地。  
- 禁止 I2V 回潮；禁止自由画布膨胀。  
- 复杂页模板（一大两小等）**首期明确不做**（功能分期，不是命名债）。

## 收口

- 完成事实：后端漫画 schema、panel 配方与上传归档、字层持久化、真实像素页导出、freedub 归档服务、话数包入口和 `/comic/:id/...` 前端入口已落地；旧视频服务与相关测试已从编译主干移除。  
- 实际命令：`backend/npm.cmd test`、`backend/npm.cmd run typecheck`、`backend/npm.cmd run build`、`frontend/npm.cmd test`、`frontend/npm.cmd run lint`、`frontend/npm.cmd run build`。  
- 未验证项：Agnes 图片真实全链、freedub 真实外部调用、浏览器人工验收尚未执行。  
- 剩余风险：话数包当前仍需补充已归档底板文件的完整流式写入；真实 Provider 能力仍需外部目录取证。  
- 外部操作（commit/push）：默认未执行。
