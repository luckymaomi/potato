# 当前任务：整站界面文案收口

## 需求

按 owner 最新界面文案规则审查整个前端，删除装饰性英文、标题附属宣传说明和面向开发者的技术宣传口吻。页面标题只保留短名称；必要操作提示缩短并放在对应字段、按钮或空状态动作附近，不用另一句宣传语替换。

## 当前事实

- 上一轮已完成资产卡和分镜配方的显式组装；资产与分镜表单中存在解释“何时组装、如何保存、不会覆盖”的长说明。
- 当前前端由首页、AI 配置、项目工作区四个阶段，以及抽屉、弹窗、表格、空状态和错误边界组成。
- 本轮开始时 Git 工作区仅有 4 个 owner 既有的生成计时相关文件改动；这些改动不属于本任务，不覆盖、不回滚、不暂存。
- 当前没有项目服务运行；本轮不调用真实供应商。

## 失败测试或失败证据

- `AssetWorkspace.tsx` 直接显示“预设只在点击组装时读取，不会自动覆盖下方文本”等实现机制说明。
- `StoryboardWorkspace.tsx` 直接显示“点击后才读取当前镜头规格与已选资产”等内部流程说明。
- 需要继续静态扫描并逐页阅读，确认标题区、卡片、表格、抽屉、弹窗、设置页及 CSS 中是否还有同类文案与装饰性类名。

## 目标

- 页面、工作区、卡片、表格、抽屉、弹窗、设置和空状态只保留任务名称、字段名称、操作反馈与必要短提示。
- 删除装饰性英文 kicker、eyebrow、slogan、宣传副标题，以及把数据模型、契约、流水线、节点图、异步任务等内部实现包装成用户卖点的文案。
- 保留产品专名、模型名、供应商名、文件扩展名、错误码和用户数据中的真实英文。
- 删除文案后同步清理失去用途的样式和组件结构，保持桌面与移动端布局完整。

## 不做范围

- 不改变资产提示词、分镜配方、生成、归档或供应商合同。
- 不改后端业务接口、数据库或数据模型。
- 不调用 Agnes、PearAPI 或其他真实供应商。
- 不修改 owner 既有的 4 个生成计时相关文件。
- owner 已在收口阶段明确授权完成后创建本地提交并 push。

## 设计

- 先按源码命中建立候选清单，再结合组件上下文逐项判断，避免机械删除合法英文和错误信息。
- 页面标题保留短名；标题附近的介绍性段落直接删除。
- 表单只保留影响填写结果的短提示；可从字段名、按钮名和交互本身理解的说明直接删除。
- 空状态保留明确动作，例如“暂无项目”与“新建项目”；删除理念说明和技术架构说明。
- 清理仅由被删文案使用的 CSS 类；通用 Ant Design `description` 属性按实际用途审查，不按名称盲删。

## 实施任务

- [x] 扫描并逐页确认装饰性、宣传性和技术宣传文案
- [x] 清理首页、列表、详情、工作区、卡片、表格、抽屉、弹窗、设置和空状态
- [x] 清理失去用途的 kicker、eyebrow、subtitle、description 等样式与结构
- [x] 运行前端定向检查、测试、lint、typecheck 和 build
- [x] 使用本地数据完成桌面与移动端浏览器检查，不调用真实供应商
- [x] 回填 `history.md` 与本计划的验证事实

## 验证计划

- 静态扫描装饰类名、标题副文案、内部技术术语和 owner 点名的两段说明，逐项复核剩余命中。
- 前端运行 `npm.cmd test`、`npm.cmd run lint`、`npx.cmd tsc -b --pretty false`、`npm.cmd run build`。
- 浏览器覆盖首页、AI 配置、项目四个工作区及相关弹窗/抽屉；分别检查桌面和 390px 移动视口，使用本地数据或请求拦截。
- 检查最终 Git 差异，确认提交不包含 owner 既有生成计时文件；按本轮授权 commit 并 push。

## 收口

- 完成事实：整站页面标题保留短名称；资产与分镜删除组装机制说明和配方技术口吻；生产页删除架构式说明；AI 配置、示例入口、空状态和错误提示改为短操作文案；清理对应样式，并修复 390px 下剧集操作行被裁切。
- 实际命令：前端 `npm.cmd test`（23/23）、`npm.cmd run lint`、`npx.cmd tsc -b --pretty false`、`npm.cmd run build` 均退出码 0；静态禁用词扫描为 0；Chrome 本地拦截数据覆盖 6 个页面的桌面与 390px 视口。
- 未验证项：没有调用真实供应商；随机媒体审美不属于本轮；浏览器数据来自本地接口拦截，不证明后端或供应商可用。
- 剩余风险：保留既有 5 条 effect lint warning 和 Vite 大包体提示；移动端阶段导航与 AI 配置表格按设计使用横向滚动。
- 交付状态：按 owner 授权由收口流程创建简体中文提交并 push；实际提交与远端结果以 Git 和交付报告为准。
