# 当前任务：资产生成交互收口

## 目标与边界

按 `C:\Users\Administrator\Desktop\AI短剧工作流.md` 收口资产标准图生成：删除批量出图入口，让最终资产提示词成为用户可见、可编辑、可持久化的事实，并在每张资产卡缩略图上展示单次生成的实时阶段。

- 不调用 Agnes、PearAPI 或其他真实供应商，不做付费验收。
- 完成确定性测试、静态检查和本地简体中文提交；不 push。
- 不清理 `backend/data`，当前旧数据库仍由 owner 明确重建。
- 保留工作区中与本任务无关的 5 个 `frontend/src/features/generation/*` 既有状态；若任务必须修改同一文件，只纳入可审查的目标行。
- schema 与项目 ZIP 继续采用断裂式升级，不保留旧字段或旧 ZIP 格式兼容层。

## 修改前事实

- 资产页仍有“生成未完成标准图”入口及前端顺序队列、停止控制。
- `project_assets` 只持久化 `output_type`；后端在点击生成时现场调用 `assembleAssetOutputPrompt`，用户看不到最终发送文本。
- 资产缩略图只显示“已有标准图/待生成”，生成阶段只出现在详情面板的计时组件。
- 后端任务已有排队、处理中、完成、失败状态；图片服务在供应商提交和本地归档前后已有阶段消息，可直接作为 UI 状态依据。
- 当前 ZIP `format: 5` 不包含最终资产提示词。

## 目标主干

### 可见提示词

- `project_assets.output_prompt` 是最终资产生成提示词的持久化事实，非空保存并随项目 ZIP 往返。
- 按 owner 最新纠正，产出层是独立提示词区域，删除布局下拉和模板覆盖交互。
- `output_type` 仅记录建卡时的初始模板类型；最终生成文本以独立的 `output_prompt` 为准。
- 卡片资料和提示词分别编辑；切换资产保留各卡草稿，刷新媒体状态保留正在编辑的文本。
- 新建资产卡由后端填充默认预设提示词；保存后，生成路由只读取 `output_prompt`，不再现场拼装。
- 用户上传参考图仍只进入 `referenceImages`，不混入提示词，也不传播到分镜引用。

### 单卡状态

资产缩略图按该卡当前跟踪任务显示：

- `pending`：排队中。
- `processing` 且处于供应商阶段：正在生成。
- `processing` 且处于本地保存阶段：归档中。
- `completed`：已完成。
- `failed`：失败。
- 用户主动停止的任务显示已停止；无本轮任务时读取最近历史结果，无历史时按标准图显示已完成或待生成。

### 删除批量入口

- 删除资产工作台及分镜台的批量出图按钮、顺序队列状态、循环提交和批量停止逻辑。
- 保留单卡生成与单卡停止。
- 产品事实与理念文档不再把资产批量出图列为主链能力。

## 完整影响面

- 后端：schema、领域类型、资产 repository、建卡初始提示词、生成消费、项目读取、ZIP 导入导出、Demo 初始化。
- 前端：领域类型、资产表单、独立提示词区、缩略图状态、样式和分镜台批量出图删除。
- 测试：提示词持久化、生成消费用户编辑文本、ZIP 往返、任务阶段到资产状态的正向映射。
- 文档：`spec.md`、`README.md`、`history.md`、外部《AI 短剧工作流.md》。

## 实施状态

- [x] 读取规约、Skill、理念文档并核验当前批量入口、提示词 owner、任务状态与归档阶段。
- [x] 建立本轮断裂式变更合同和影响地图。
- [x] 建立可见提示词、生成消费和单卡状态的正向失败契约（后端 6 处目标失败，前端状态模块缺失）。
- [x] 断裂升级 schema、repository、类型、项目读取和 ZIP 格式。
- [x] 生成路由消费已保存提示词，移除中途提出的模板预览接口。
- [x] 删除批量出图入口；实现独立可编辑提示词区和缩略图实时状态。
- [x] 同步 Demo（经建卡链生成初始提示词并增加验证）和产品事实文档。
- [x] 运行后端 test/lint/typecheck/build 与前端 test/lint/typecheck/build。
- [x] 静态检查、差异检查、敏感信息检查和提交范围复核；UTF-8 无 BOM，目标服务端口监听为 0。
- [x] 创建本地简体中文提交，不 push。

## 验证计划

- 后端：`npm.cmd test`、`npm.cmd run typecheck`、`npm.cmd run build`、`..\frontend\node_modules\.bin\oxlint.cmd src test scripts`。
- 前端：`npm.cmd test`、`npm.cmd run lint`、`npx.cmd tsc -b --pretty false`、`npm.cmd run build`。
- 静态检查：资产批量入口/队列调用为 0；资产生成路由直接消费 `output_prompt`；前端不存在资产模板正文；ZIP 仅接受新格式。
- 差异检查：`git diff --check`、未跟踪文件、敏感信息和暂存范围复核。

## 收口记录

- 完成事实：独立产出提示词区替代布局下拉；后端直接消费用户保存文本；资产/分镜批量出图入口删除；单卡缩略图显示阶段与终态；ZIP 6 保存提示词。
- 确定性验证：后端 64/64、前端 23/23，两端 lint/typecheck/build 均退出码 0。HTTP 保存至生成路由、生成阶段、本地落盘、Demo 初始文本和 ZIP 往返均有正向验证。
- 浏览器验证：Chrome + 本地模拟 API 检查独立编辑、资料修改保留文本、切卡草稿隔离、单次提交、五种状态、媒体刷新保留草稿和重开恢复结果，全部通过；不等于真实供应商验收。
- 静态扫描：资产/分镜图片批量入口与队列、模板覆盖接口归零；生成读取 `output_prompt`，前端没有模板正文。
- 未验证项：真实供应商生成效果、owner 人工审美验收；当前数据库重建未执行。
- 剩余风险：本地现有数据库已包含 `output_type`，仍缺 `output_prompt`，运行新版前需重建；旧 ZIP 不兼容。前端保留 5 条既有 effect lint warning 和 Vite 大包体提示；每秒轮询可能跳过短暂阶段。
- 提交状态：已创建本地简体中文提交“独立资产产出提示词并移除批量出图入口”；本记录随提交收口，未 push。
