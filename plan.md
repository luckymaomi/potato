# 画布进度反馈、节点精简与稳定交互收口

## 需求

- 保留“节点自治、直接连线只传数据”的现有生产合同，不增加隐式依赖或资产仓库暗取输入。
- 整图、分组和单节点运行显示真实总进度、当前节点、供应商任务进度与阶段消息；没有真实百分比时只显示真实阶段，不伪造进度。
- 修复检查器横向溢出：模型选择只显示模型标识，能力在独立区域换行展示；生成历史使用时间与简短版本号并显示媒体预览。
- 删除“通用文本”节点；《雨夜外卖》的镜头描述直接归入对应分镜图与镜头视频的面板提示词。
- 资产图仍是可复用的显式节点；用纯视觉的角色/场景/道具折叠组降低画布噪声，折叠不改变节点、连线、保存或执行语义。
- 供应商生成 HTTP 请求超时统一为 10 分钟；模型目录等短查询保留短超时，视频任务整体轮询时限继续由现有配置拥有。
- 修复选择或编辑节点时检查器被自动保存结果反复重新灌值、表现为界面刷新或编辑中断的问题。
- 不运行真实供应商测试，由 owner 完成真实验收。

## 当前证据

- 当前 `master` HEAD 为 `304907f`，工作区在本任务开始时干净；`backend/data` 不存在。
- 后端任务已经持久化 `progress` 与 `message`，图片、视频和合成服务也会上报“提交、供应商完成、本地保存”等真实阶段；前端轮询目前只消费 `status`，丢弃了已有进度事实。
- `CanvasInspector` 的模型 option 把模型名和全部能力拼成一行；能力卡片与两列字段缺少最小宽度和横向裁剪，截图证实检查器产生水平滚动。
- 生成历史当前显示 `#ID · 已落盘 · 完整日期时间`，没有缩略内容。
- Demo v13 有 10 个 `generic-text` 镜头描述节点；同一文本已同时存在于分镜图和视频节点提示词，属于重复画布事实。
- 检查器同步 effect 依赖完整 `node` 与 `project` 对象；每次字段修改和自动保存返回都会重新执行整表 `setFieldsValue`，会打断正在进行的交互。这是当前代码中能够直接证明的刷新来源。
- 前端 HTTP 已是 10 分钟，但 Provider transport 默认值、PearAPI 生成请求和 Agnes 轮询仍有 2 分钟超时。

## 设计与事实 owner

| 事实 | 唯一 owner |
| --- | --- |
| 节点任务进度与阶段 | 后端 `async_tasks.progress/message` |
| 当前画布运行总进度 | 前端 `runWorkflow` 的确定性运行范围与节点回调 |
| 节点执行进度投影 | `ProductionNodeData.execution` 与通用执行器 |
| 模型名与模型能力 | Provider model catalog；检查器只分别投影 |
| 镜头描述 | 分镜图、镜头视频各自的面板 `prompt` |
| 资产折叠状态 | 画布页面本地视觉状态，不进入生产快照 |
| 供应商生成请求超时 | Provider transport 常量 |

## 实施任务

- [x] 先补进度回调、Demo 节点数量/连线、模型 option 和 10 分钟超时的合同测试。
- [x] 接通任务百分比与阶段消息，增加顶部总进度和节点内进度反馈。
- [x] 重排检查器模型能力与历史 UI，消除横向滚动。
- [x] 删除 `generic-text` 插件和 Demo 镜头文本节点，升级 Demo 版本与初始化完整性检查。
- [x] 增加不改变生产语义的角色/场景/道具折叠控制。
- [x] 限制检查器表单只在切换节点时灌值，避免自动保存引发编辑刷新。
- [x] 将供应商生成请求超时统一为 10 分钟。
- [x] 更新 `spec.md`、`README.md`、`history.md`，执行定向与全量确定性验证。
- [x] 创建简体中文本地提交；不 push。

## 验证

- 前端定向测试：运行进度回调、节点任务阶段、Demo 无 `generic-text`、资产折叠不改变持久快照/执行范围、模型选项短标签与历史标签。
- 后端定向测试：默认和生成请求超时为 600000ms，现有 429 重试与任务阶段仍成立。
- 全量：前端 test/lint/build；后端 strict typecheck/test/build；`git diff --check`、UTF-8 BOM、敏感值和禁用单边强调样式扫描。
- 不创建 `backend/data`，不启动前后端，不调用 PearAPI/Agnes，不用自动测试冒充真实验收。

## 收口

- 完成事实：运行总进度与任务阶段已可见；检查器模型和历史 UI 已收口；资产可按类型视觉折叠；Demo v14 删除 10 个重复通用文本节点；表单不再因自动保存响应整表重置；Provider 生成 HTTP 超时统一为 10 分钟。
- 实际验证：定向合同先观察到旧实现失败；收口时实际执行 `frontend/npm.cmd test`（12 个测试文件、40/40）、`npm.cmd run lint`、`npm.cmd run build`，以及 `backend/npm.cmd run typecheck`、`npm.cmd test`（59/59）、`npm.cmd run build`，全部通过。前端构建仍有约 1.48 MB 单包警告。
- 静态扫描：`git diff --check` 无空白错误；仓库文件 UTF-8 BOM 为 0，常见密钥模式为 0，`border-left/right` 与 `::before/after` 为 0，旧运行时节点字段为 0；`generic-text` 只存在于删除记录和否定性测试。
- 未验证项：真实供应商全链与浏览器人工验收由 owner 执行；前端 build 仍报告约 1.48 MB 单包警告。
- 外部操作：未启动服务、未创建 `backend/data`、未调用真实供应商；收口检查确认项目 Node 进程为 0。已在 `master` 创建简体中文本地提交，push、部署、发布均未授权且未执行。
