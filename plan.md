# 节点插件化、自治上下文与资产仓库断裂式重构

## 需求

- 将“模板拓扑决定上下文”的画布重建为一个短剧生产核心：所有节点是可插拔插件，每个节点显式声明上下文，角色、场景、道具和分镜以稳定 ID 进入资产仓库，连线只表达数据流方向。
- 生产目录、工具栏、节点卡片、检查器、默认模板、预写 Demo 和后端生产入口必须是同一套定义的投影。
- 项目没有正式生产数据、用户文件或外部兼容承诺，数据库已由 owner 清空；采用断裂式变更，不提供迁移、兼容、双写或旧结构恢复。
- 默认模板和《雨夜外卖》预写 Demo 使用分层 DAG：连线直接表达数据依赖与资产复用，底层画布继续允许自由连线和节点内资产替换。
- 图片和视频只有在后端下载到项目本地存储并完成校验后才算生成完成；供应商 URL 只作为来源记录。所有生成记录形成可回看、可选择当前版本的历史，项目导出/导入携带本地媒体。
- 《雨夜外卖》Demo 的故事、四场剧本、4 个角色、3 个场景、3 个道具和 10 个分镜全部由仓库定义预写；初始化脚本只建立本地领域记录和画布，不调用文本模型。
- Demo 媒体统一预选 PearAPI：图片 `gpt-image-2`，视频 `grok-imagine-video`。owner 运行初始化脚本后，画布“运行未完成”跳过已完成文本，只生成资产图、分镜图、镜头视频和整集合成。
- owner 的一次实际测试失败后，曾要求代理修复并直接完成真实 PearAPI 全链，因此先建立覆盖 HTTP CRUD、画布保存、生产命令、任务、供应商、本地归档、初始化与验收的持久 Everything 日志。
- owner 最终又明确已删除 `backend/data`，要求代理完成实现后停止，由 owner 自己执行真实测试。当前已确认 `backend/data` 不存在；本轮不得由代理运行初始化脚本、启动服务或调用真实供应商。
- owner 最终明确节点自治合同：每个节点是可独立运行的小工具，面板参数是完整输入；连线只把已经存在的上游结果显式追加给下游，删除连线后节点仍能独立运行。运行单节点不得自动补跑祖先、不得因未完成上游阻止本节点、不得暗查父节点或资产仓库补输入。
- 《雨夜外卖》画布要显式呈现“逐镜头文本节点 + 实际入画的多个资产图节点 → 分镜图 → 镜头视频 → 整集合成”。资产图节点默认只用自己的提示词/模型/参考图，提取节点到资产图的连线可有可无；右侧面板只编辑本节点输入，不保留另一套隐藏依赖选择。

## 当前事实

- 分支为 `master`，任务开始时 HEAD 为 `667a69b`；工作区只有上一轮尚未实施的 `plan.md` 单链计划，该计划的单链目标已吸收到本合同，没有回滚 owner 其他改动。
- 前端基线为 25/25 测试、lint 和 build 通过；后端基线为 44/44 测试、strict typecheck 和 build 通过。前端已有约 1.47 MB 单包警告。
- `frontend/src/features/production/catalog.ts` 只集中部分角色元信息；检查器字段、执行命令和上下文规则仍分散。
- `prepareNodeForExecution` 只读取直接上游，并按材料/角色猜文本、图片和记录 ID。旧节点同时保存 `material`、`purpose`、`assetKind`、`textOperation` 和 `linkedRecordId(s/index)`，存在重复或矛盾事实。
- 起步模板为 11 节点/13 边/1 组，Demo 为 18 节点/27 边/5 组；当前执行器逐节点 `await`，模板分支不是并行能力的必要条件。
- 后端已有唯一 `ProductionWorkflowService.execute`，文本、图片、视频与合成都经过同一生产命令入口；此主干继续保留。
- 角色、场景、道具和分镜分散在多个 service。实体与分镜的 replace 先删除再重建，会破坏稳定引用和已生成媒体。
- 当前媒体结果只保存供应商 URL，实体上的最新 URL 会覆盖上一版，项目 ZIP 只包含 `project.json`；远端生成成功但本地未保存也会被标成 completed。这是本轮必须消除的失败事实。

## 失败测试或失败证据

- 新插件合同尚不存在：不能从一个定义同时得到创建入口、检查器参数、上下文需求、默认提示词、默认生成方式和执行命令。
- 现有执行器对直接上游以外的剧本、资产和分镜没有显式需求，只能依赖模板拓扑与角色分支猜测。
- 现有快照可同时持有相互冲突的角色派生字段；运行时才选择其中一套含义。
- 现有实体/分镜 replace 的 DELETE 证明确认重跑不能保留稳定资产 ID 与已有媒体。
- 曾实现的起步模板和 Demo 被压成同一水平线上的严格单链；owner 以实际截图否定了该形态，因为它隐藏一对多资产复用、多输入镜头和多输入合成关系。
- 现有图片/视频服务在供应商返回 URL 后直接完成任务，没有下载、文件签名/大小校验和原子落盘；业务实体只指向最新 URL，缺少可回看并选用的历史版本；归档没有媒体清单或文件。
- 2026-09-17 真实失败证据：项目 1 的 `demo_version=12`，但角色、场景、道具、分镜及其关系表全部为 0；前端“完整 Demo”入口只写了项目、剧集和空引用画布。初始化脚本又仅凭 Demo 版本提前返回，把这个半成品误判为已初始化。“运行未完成”因此错误执行 `production_extract-characters`，首个任务以“尚未同步可用的文本模型”失败，后两次被用户取消；尚未创建任何图片或视频 generation。
- 当前日志只有 SQLite 任务结果和可选 `LOG_FILE` 控制台副本，没有默认持久文件、请求完成状态、CRUD 输入摘要、节点/运行关联、任务全生命周期和媒体阶段事件，无法从一份日志还原整个工作流。

## 目标

- 插件目录为节点语义唯一 owner。每个插件至少声明稳定 `role`、标签/分类、材料、生产用途、上下文需求、产出、参数描述与默认值、默认提示词 key、默认生成方式和统一命令构造器。
- 节点快照只保存 `role`、插件参数/用户覆盖、结构化 `assetRefs`、状态和运行结果；角色派生事实不写入快照。
- 资产仓库按项目归属、资产类型与稳定 ID 维护角色、场景、道具、分镜关系与生成历史，但不向执行器提供隐式提示词或参考图。
- 通用上下文解析器读取插件声明，只从节点面板和已经完成的直接入边组成类型化上下文；执行器核心不按节点角色猜业务数据，也不查询仓库补输入。
- 通用上下文解析器只读取直接入边已经产出的文字、图片或视频；节点面板输入永远可独立成立，上游结果只做追加。资产仓库只负责持久化领域对象和历史，不是执行时的隐式输入源。
- 提取和分镜同步采用稳定 upsert：匹配项复用 ID 与已有媒体，新项创建；角色、场景和道具不因一次提取未返回而删除，以保留可复用资产与历史。分镜清单按当前集序号同步，并删除超出本次清单长度的旧分镜；同一次重跑不先删除全部记录。
- 起步模板和《雨夜外卖》Demo 使用分层 DAG。故事进入剧本，剧本分支到三类提取；资产图节点可独立运行并扇出到实际使用它的分镜图；逐镜头文本与实际入画资产图共同进入分镜图；镜头视频可消费对应分镜图或独立文生视频；整集合成汇聚本集全部镜头视频。
- 错误、取消、重试继续由现有统一模块拥有；画布组件不增加供应商状态码判断。
- 媒体服务保留每次生成记录，记录供应商来源 URL、本地相对路径、媒体类型/大小、失败阶段和是否为当前采用版本。下载或校验失败时生成与任务均失败，不更新资产当前版本。
- 资产当前媒体通过 generation ID 指针选择；用户可查询一个资产的全部图片/视频历史并切换采用版本，切换后画布与项目读取本地 `/static/...` 地址。
- 项目 ZIP 包含 `project.json`、媒体清单和项目中已落盘的媒体文件；导入时校验归档路径与文件，写入新项目目录并重建历史与当前指针，使换机后不依赖来源 URL。

## 不做范围

- 不增加配音、音乐、音效、字幕、多轨时间线或完整剪辑系统。
- 不承诺供应商远端物理撤销能力；本地媒体下载、历史和归档已进入本轮范围。
- 不把整个画布限制为单链，不删除自由连线、选区运行、下游运行、工作流组或整集合成。
- 不增加迁移脚本、兼容层、旧 API 分支、旧快照恢复、`migrate`、`dev:fresh`、`clean:data` 或 `pretest`。
- 代理不调用 PearAPI 做真实生成，不重新创建或删除 `backend/data`，不记录或提交任何 API Key；只提供由 owner 主动运行的真实验收入口。
- 不引入单边强调色、`border-left/right`、状态侧边栏或伪元素色条。

## 设计与事实 owner

| 事实 | 唯一 owner | 主要消费者 |
| --- | --- | --- |
| 节点角色、输入输出、参数、默认行为、命令构造 | 前端生产插件目录 | 工具栏、模板、Demo、节点卡片、检查器、执行器 |
| 节点持久化合同与资产引用结构 | 画布节点类型/快照模块 | store、保存、导入导出、检查器 |
| 上下文需求类型与解析规则 | 生产上下文解析模块 | 通用执行器、插件命令构造器 |
| 资产类型、稳定 ID、项目归属、分镜资产关系 | 后端资产仓库与当前 schema | 生产服务、提取、分镜、查询 API |
| 媒体下载、文件校验、本地路径与来源 URL | 后端媒体归档服务 | 图片/视频生成、静态文件、项目归档 |
| 生成历史与当前采用版本 | generation 记录 + 画布节点 result/history；有明确业务目标时再写资产指针 | 历史 API、检查器、导入导出 |
| 默认系统提示词 | 现有后端提示词目录 | 插件 prompt key、目录 API、Provider 请求 |
| 模型能力与默认模型 | 现有后端 Provider catalog/config | 前端模型目录、生产校验 |
| 执行状态、错误、取消与有界重试 | 现有错误合同、运行会话、任务和 provider runtime | 执行器与 UI 状态投影 |
| 默认模板与 Demo 布局 | 插件投影 + DAG 工作区定义 | 新项目初始化、Demo 入口、结构测试 |
| Everything 审计日志 | 后端结构化日志模块与 `backend/logs/everything.log` | HTTP 中间件、生产主干、任务、媒体归档、初始化与验收脚本 |

设计约束：

1. 插件以稳定角色为注册键，注册时校验唯一性和合同完整性；新增角色只注册插件，画布、执行器和检查器核心没有角色条件分支。
2. `ContextRequirement` 只表达节点可消费的显式连线数据类型和数量；所有连线输入均为可选增强，不存在执行时从项目/剧集/资产仓库暗取内容的 required 输入。
3. `assetRefs` 只用于明确选择持久化目标或记录输出归属，不能成为节点能否运行的前置条件；媒体节点没有目标 ID 时仍建立项目级 generation、返回本地文件并由节点历史持有。
4. 分镜 schema 持有场景及实际出现的角色/道具关系；这些关系可投影为画布连线，但生成命令只消费画布显式传入的数据。
5. 后端仍只有统一生产命令入口；提示词、参考图和待合成视频是命令的显式字段，具体文本、图片、视频、合成 service 继续承担各自生成职责。
6. 起步模板与 Demo 由通用工作区构造器生成节点、依赖边和单一工作流组；布局按生产阶段分列，资产与镜头按行展开。边必须能直接观察资产一对多、镜头多对一和合成汇聚关系。
7. 供应商成功只是下载阶段的输入。媒体归档服务以 `projects/<projectId>/<images|videos>/` 生成不可变文件名，限制协议、响应大小与媒体签名，临时文件下载并校验后原子改名；任何失败保留来源和失败阶段，但不产生 completed 或更新当前指针。
8. generation 记录是不可变历史，画布节点保留本节点 generation 历史并以 `result.generationId` 选择当前版本；切换版本不删除历史文件。有明确业务目标时可同步资产/分镜/剧集指针，通用生成不要求业务目标。
9. ZIP 内路径使用项目相对路径且导入时拒绝越界、缺失或签名不符；来源 URL 可以随元数据保留，但运行和展示只使用导入后的本地文件。
10. 《雨夜外卖》分镜关系写入资产仓库，同时由画布依赖边显式投影；镜头 6 精确连接小林、苏晴、股东甲、办公室、外卖箱，镜头 7/9 只连接苏晴和办公室，镜头 10 连接小林、苏晴、办公室、外卖箱。
11. 全图仍按 DAG 拓扑排序；整组运行会先运行范围内的上游再运行下游，但单节点运行只提交当前节点。直接入边未完成时没有可传结果，执行器忽略该输入并使用面板参数，不自动运行也不阻断当前节点。
12. Everything 日志采用逐行 JSON 追加写入并跨数据库清理保留；每条记录带时间、级别、事件名和结构化详情。所有键名命中 key/token/secret/authorization/password/cookie 的值统一脱敏，Data URL、Buffer、循环对象和超长内容只记录安全摘要。日志写入失败不能改变业务结果。
13. Demo 初始化的完成条件是项目、剧集、4 个角色、3 个场景、3 个道具、10 个分镜、逐镜关系和当前 DAG 画布同时存在；版本号只是标识，不能替代完整性检查。发现同版本半成品时原位补齐，不调用文本模型、不创建第二个项目。

## 实施任务

- [x] 建立持久 Everything 日志、统一脱敏、HTTP CRUD 完成记录、生产运行关联、任务和媒体阶段事件，并先用确定性测试保护不泄露凭据。
- [x] 建立 Demo 完整性检查与可恢复初始化，删除前端只创建半成品 Demo 的路径，为本次真实失败增加回归测试。
- [x] 增加可恢复的《雨夜外卖》真实验收入口；按当前版本指针跳过已落盘媒体、逐节点记录日志、失败保留 generation 并在安全边界内重试。
- [x] 将插件输入改成“面板完整输入 + 已完成直接入边追加”，删除单节点祖先补跑、未完成上游拦截、媒体节点必选资产记录和后端仓库隐式补图/补提示词。
- [x] 将 Demo 重画为逐镜头文本节点显式连接资产图和分镜图，镜头视频消费连线分镜图或独立文生视频，合成只消费十路连线视频；同步检查器、结构测试与初始化修复。
- [x] 核验当前仓库、基线、统一生产入口、旧节点合同、上下文执行、实体/分镜持久化与模板拓扑。
- [x] 调研 tldraw、ComfyUI frontend、AICON 与 Huobao Drama，沉淀已采用、不采用和未知边界。
- [x] 建立本次断裂式执行合同和事实 owner 地图。
- [x] 添加插件合同、快照合同、上下文解析、稳定资产同步与模板拓扑的回归测试。
- [x] 重建生产插件目录，并让工具栏、卡片、检查器、模板、Demo 和执行命令消费同一注册表。
- [x] 断裂式替换节点快照与保存合同，删除旧派生字段和旧关联字段。
- [x] 建立后端资产仓库、分镜资产关系与稳定同步，接入唯一生产主干。
- [x] 建立媒体下载归档、生成历史查询/选用、当前版本指针及前端节点历史选择。
- [x] 断裂式升级项目归档格式，导出本地媒体并在导入时校验、落盘和重建引用。
- [x] 建立声明式上下文解析器，删除按直接上游角色或祖先角色猜上下文的旧执行器逻辑。
- [x] 用分层 DAG 升级起步模板与《雨夜外卖》PearAPI Demo，并补齐资产扇出、逐镜精确入边、分镜到视频一对一、10 路视频汇入合成的结构测试。
- [x] 完成定向与全量确定性自动验证。
- [ ] 由 owner 执行空数据初始化、真实 PearAPI 全链和浏览器人工验收。
- [x] 同步 `spec.md`、`README.md`、`history.md`、`reference/README.md`，回填本计划并审查完整差异。
- [x] 在 `master` 创建一个简体中文本地提交；不 push、不部署、不发布。

## 验证计划

### 确定性自动验证

- 定向测试：插件注册唯一且合同完整；新快照不复制插件事实；上下文需求显式；资产按 ID/归属同步；分镜只接收已完成直接入边；同步重跑保留匹配资产 ID 与媒体；模板和 Demo 为无环分层图；单节点不补跑祖先且未完成上游不阻断。
- 媒体测试：供应商成功后文件下载、签名与大小校验、原子落盘才 completed；供应商失败与本地归档失败具有不同失败阶段；多次生成均保留；选用历史版本更新当前指针；ZIP 带媒体且换目录导入后只用本地地址。
- 前端：`npm.cmd test`、`npm.cmd run lint`、`npm.cmd run build`。
- 后端：`npm.cmd run typecheck`、`npm.cmd test`、`npm.cmd run build`。
- 静态扫描：旧节点字段、旧关联字段、旧分散入口、兼容/迁移/自动清库入口、单边强调色、伪元素色条、UTF-8 BOM、敏感信息均无活动代码命中；`git diff --check` 通过。

### 初始化与真实外部验收

- 当前凭据存在且 PearAPI 实时目录已确认 `gpt-image-2` 支持最多 16 张参考图及 9:16，`grok-imagine-video` 支持 1 张参考图及 9:16；不得把凭据值写入日志或报告。
- owner 运行可恢复验收，真实调用 PearAPI；通过条件只看预写文本存在、所有媒体节点返回本地文件和整集本地 MP4，不对随机生成内容做字面量、语义或精确内容断言。
- 每次真实尝试、跳过、失败、重试、供应商完成、本地归档与最终合成都必须进入 `backend/logs/everything.log`。供应商硬错误或重试耗尽时保留原始失败事实，不用 Mock 冒充成功。

### 人工验收

- 检查节点创建、插件参数投影、资产一对多连线、镜头多输入、合成汇聚、显式资产替换、自由拖拽/连线、错误/取消/重试反馈和桌面视觉。
- 若当前环境没有可用浏览器自动化或 owner 人工操作，明确列为未验证，不用 build 代替。

## 收口

- 完成事实：节点插件目录、自治节点输入、显式直接入边上下文、稳定资产仓库、本地媒体归档与历史选择、携带媒体的项目 ZIP、Everything 日志、可恢复 Demo 初始化和真实验收脚本均已落入唯一主干；Demo v13 为 46 个节点、66 条边和 1 个工作流组。
- 实际命令与结果：前端 `npm.cmd test` 为 9 个测试文件、35/35 通过，`npm.cmd run lint` 通过，`npm.cmd run build` 通过；后端 `npm.cmd run typecheck` 通过，`npm.cmd test` 为 58/58 通过，`npm.cmd run build` 通过；`git diff --check` 无空白错误，UTF-8 BOM、旧隐藏上下文函数、单边框和伪元素色条扫描均为零命中。
- 未验证项：按 owner 最终指令，代理没有创建 `backend/data`、没有运行 Demo 初始化、没有启动前后端、没有调用真实 PearAPI，也没有执行浏览器人工验收。真实供应商全链与页面交互由 owner 接手验证，不能宣称成功。
- 剩余风险：真实 PearAPI 的凭据、额度、实时模型可用性、限流与媒体返回格式尚未经本轮外部调用证明；前端构建仍报告约 1.48 MB 单包警告。供应商物理撤销不在本轮保证内；来源 URL 仅供审计且可能失效，只有真实运行并成功归档后的媒体才不依赖来源 URL。
- 数据与兼容：owner 已确认无正式生产数据和兼容承诺；不创建迁移或旧结构兼容。
- 外部操作：收口时已确认 `backend/data` 不存在且没有 tomato-ai-drama 的 Node 进程；真实调用脚本固定 PearAPI `gpt-image-2` / `grok-imagine-video`，但代理按 owner 最终指令未运行；本地提交在本次收口完成后创建，push、部署、发布均未授权且未执行。
