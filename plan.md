# 当前任务：断裂式变更为土豆漫画

> **⚠ 2026-09-24 owner 收窄**：页组装（拼页/气泡/配音/导出/话数包）与 TTS 配置已从产品删除。当前工作区只保留总览与剧本、资产图、分格台。下文仍含页组装验收项的段落视为**已作废合同残片**，以 `spec.md` 为准，禁止按旧条重建。

> ## 反阉割铁律（置于一切之前，不可违反）
>
> **没必要的可以删。留下的能力不许阉割。** 本条压过一切「少写一点也算完成」的判断。
>
> 允许删除、且删干净不算失败的，只有短剧里不再需要的东西：镜头视频、视频配方、视频待复核、整集视频合成、成片、镜头包、只服务这些能力的路由和测试。
>
> 不允许的是把还要留的能力压瘦后假装做完。生图、资产定妆、本地归档、手选画幅、参考图上限、配方组装、历史选用、待复核、分格台三栏、页组装（拼页、气泡、配音、导出、话数包），这些的**行为**必须和成熟短剧台里对应的图片能力一样完整。
>
> **不要求行数和旧版完全一致，也不要求必须比旧版更多。** 禁止用空注释、重复样板、无用抽象凑行数。也禁止把几百行的真实逻辑挤成几十行巨行或短桩来过关。正在发生的失败就是这种：`imageGenerationService` 从三百多行压成约 28 行，资产仓储和工作区路由各削掉三四百行，分格台继续变瘦。视频文件可以删；这些留下的服务被削瘦 = 阉割 = 未完成，必须按原有完整行为补回。
>
> 自查：删掉的每一块，要么是上面允许删除的视频主链，要么原有用户能做的事在新页面上仍然能做。少一件能做的事，就是没补齐。

> ## 测试铁律（置于一切之前，不可违反）
>
> **绝对禁止编写负性测试。**
> 禁止以「不存在 / 不应出现 / 不要 / 不得包含 / 已删除 / 404 证明没了 / 符号扫描为 0」等为断言目标的测试用例。此类测试毫无意义，不准写、不准留、不准用负例冒充验收。
> **只用正向测试保护正向行为**：断言用户与系统**应当成立**的结果——组装成功、生图/归档完成、字层可保存、四格页导出文件可读且尺寸符合模板、TTS 归档后本地文件可用、话数包导出成功、路由 `/comic/...` 可打开工作区、文案展示为「话」等。
> 缺前置条件时的**明确拒绝**（例如四格缺底板导出失败并返回可见错误）属于正向契约，允许写；但禁止写成「旧短剧视频接口不存在」这类追悼式负测。
> 短剧残骸靠实现里直接删掉 + 实现代理交付时自查；**不要**为此写自动化负测。

> ## 体验与代码量铁律（与测试铁律同级，不可违反）
>
> **本任务是「成熟 AI 短剧工作台 → 成熟 AI 漫画工作台」，不是 Demo、不是薄壳、不是先砍再补。**
> Owner 已在 `history.md`（2026-09-21「土豆漫画断裂改造两轮失败」）记录两轮偷懒：**+400 级新增 / -6000 级删除**、分镜台 820 行压到 25 行或压缩成约 42% 字符量的巨行 JSX——**一律判失败并作废**。再犯同构错误视为未执行本合同。
>
> ### 量尺（服从文首反阉割铁律，不追逐行数本身）
>
> | 量尺 | 基线 | 硬要求 |
> | :--- | :--- | :--- |
> | 允许的删除 | 视频主链文件 | 整文件删除视频生成、整集合成、成片/镜头包、旧视频路由，可以 |
> | 禁止的删除 | 生图、资产、归档、分格台、门闸 | 把这些压成短桩或巨行 = 阉割。当前 `imageGenerationService` 约 28 行就是未完成 |
> | 分格台 | 旧 `StoryboardWorkspace` 三栏成熟交互 | 轨道、预览、检查器、组装、画幅、历史、待复核都还在；不要求字符数必须 ≥ 44872，但少一整块交互 = 没做完 |
> | 页组装 | 新页面 | 拼页、气泡、配音、导出、话数包是完整页面，不是几十行壳 |
> | 注水 | — | 禁止空注释和重复样板凑行数 |
>
> ### 母版改写（唯一合法前端做法）
>
> 1. 必须用命令取出母版全文，禁止凭记忆从零写薄文件：
>    `git show 4ba454c:frontend/src/features/workspace/StoryboardWorkspace.tsx`
> 2. **在母版上改写分格台**：分镜→分格；删除视频区块。分格台只保留单格底板生产（规格、显式组装、画幅、生成/上传、历史选用、待复核）。字层、拼页、配音、话数包**不准**塞回这一页，它们属于后面的「页组装」。
> 3. **禁止**删除母版后另起 25～40 行壳；禁止在压缩壳上打补丁装厚；禁止在三栏上面再堆阅读序、导出检查、主路径步骤等无布局说明块。
> 4. 分格台三栏密度必须保留：轨道（缩略图+勾选+待复核/待重装）→ 预览（这一格底板）→ 检查器（规格 / 显式组装与配方快照 / 能力摘要与手选画幅 / 底板生成·上传·历史选用）。少一整块 = 阉割。页组装是**另一个路由上的成熟页面**，不是分格台里多出来的一截。
>
> ### 工序锁死（跳步无效）
>
> ① 留下的生图/资产/分格台不再是短桩，页组装可点通 → ② **漫画语义铁律落地**（分格台一看就是格子不是分镜）→ ③ 只删视频相关测、保留非视频测并补漫画正向测 → ④ **最后**才改 `spec` / `README` / skill 收口。
> 未过 ①② 时禁止写「交接摘要 / 已完成 / 可以收口」；禁止先改文档装完成。语义未断时勾选 I/J = 作假。
>
> ### 完成自检句（每次声称前端完成前必答）
>
> 「删掉的是不是只有视频主链？生图、定妆、分格台、页组装，用户原来能做的事是不是都还在？」少一件 = 没做完。
> 「打开分格台，像不像在填分镜表（景别/机位/运镜/环境声）？」像 = 没做完。
> 同时贴出 `git diff --shortstat`，并逐项说明每一大块删除是「视频退场」还是「留下的能力被削瘦」。后者必须先补回。

> ## 漫画语义铁律（2026-09-22 owner 当场否决后追加，不可违反）
>
> **删掉视频不等于做成了漫画。** 分格台检查器若仍是「景别 / 机位 / 镜头运动 / 环境声 / 对白进底板规格」——那是短剧分镜壳，**直接判未完成**，与薄壳同级失败。
>
> Owner 实机看到的失败形态（须消灭）：检查器标题「规格与画面描述」，字段「画面描述 / 动作 / 对白·旁白 / 景别=全景 / 机位=平视 / 镜头运动=固定镜头可轻微推进 / 构图 / 光线 / 氛围 / 环境声」。这是镜头说明书，不是格子说明书。
>
> 理念合同以 `AI漫画工作流.md` §3.1–3.3 为准。分格台只编 **A 层干净底板**；对白气泡是 **B 层（页组装）**；声音是 **D 层（页组装 TTS）**。
>
> ### 分格台 A 层允许的规格字段（用户可见文案必须像漫画）
>
> | 允许（漫画） | 用途 | 禁止用影视残骸冒充 |
> | :--- | :--- | :--- |
> | 标题 / 节拍说明 | 这一格落在哪个节拍；可作提示词回退 | 「画面描述」当电影场记 |
> | 动作定格 / 表情 | 定格姿态与脸 | 运镜式「动作」含推进/摇移语感 |
> | 构图 / 视角 | 静帧怎么取景、从哪看 | **机位**、**景别全景/特写**等镜头课术语默认选项 |
> | 光线 / 氛围 | 光影与情绪 | — |
> | 图像提示词主干 | 生图主干 | — |
> | 出场资产 ID + 额外参考图 | 双通道组装 | — |
>
> ### 分格台 A 层禁止出现（产品主路径）
>
> | 禁止 | 原因 | 正确落点 |
> | :--- | :--- | :--- |
> | 镜头运动 / `camera_movement` | 运镜属短剧；工作流写明不进漫画主路径 | **删除**字段、UI、组装输出 |
> | 环境声 / `sound` | 声音不是生图规格 | **D 层**；页组装 TTS，不进 A 表单与图片配方 |
> | 对白 / 旁白作为分格规格主字段 | 对白默认不进生图 | **B 层**；只在页组装编辑气泡；组装 prompt **跳过**对白 |
> | 机位 / `camera_angle` 文案 | 拍片用语 | 改为 **视角**（或等价漫画用词）；选项勿写「平视/俯拍」课表感也可接受，但禁止「机位」二字 |
> | 景别 / `shot_size` 文案与「全景」等选项 | Owner 否决：不像漫画 | 改为 **取景**（或并进「构图」）；选项改为漫画取景语（如近景人物、半身、全身、远景环境），禁止镜头课「景别」标题 |
>
> ### 组装提示词顺序（必须改写）
>
> 有值才输出：**图像提示词主干（空则回退节拍/标题）→ 资产卡文本 → 取景/构图/视角 → 动作定格/表情 → 光线/氛围**。
> **禁止**把对白、旁白、环境声、镜头运动写进 `image_recipe_prompt`。提示词宜强调干净画面、无字幕、无气泡、无水印。
>
> ### 完成自检句（语义）
>
> 「打开分格台检查器，一个不懂短剧的漫画作者会不会以为自己在填分镜表？」会 = 没做完。

> **变更类型：断裂式重建（Breaking Rebuild）**
> Owner 已清空旧短剧业务数据。本任务**不做**数据库前向迁移、**不做**旧视频字段兼容、**不做**旧工程 ZIP / format:8 往返、**不做**「短剧+漫画双模式并存」。
> 理念合同以仓库根目录 `AI漫画工作流.md` 为准；本 `plan.md` 是执行合同。实施中若与工作流冲突，先改工作流或本计划，再改代码。
> **写入纪律**：实施代理应分批改文件，禁止一次性巨量覆盖无关模块。
> **代码基线**：仓库已回退并推送文档合同；实现从当前 `master` 上的成熟短剧代码起步（含完整 `StoryboardWorkspace`），**不要**续做已作废的薄壳工作区。失败教训见 `history.md` 同日条目。

## 需求

1. 产品由「土豆短剧」断裂为「土豆漫画」：删除图生视频、视频配方、整集视频合成等短剧主链。
2. 保留并改写：项目 / 话（原集）/ 项目级角色·场景·道具定妆 / 分格（原分镜）底板生图 / 显式组装 / 本地归档 / 单格返工。
3. **体验零阉割**：旧成熟分镜台的交互密度、检查器、门闸、历史选用、待复核、空状态与错误可见性必须继承；只做语义替换与视频位→漫画位替换，不把工作台变瘦。
3b. **语义零短剧**：分格台规格/文案/schema/组装顺序必须按文首「漫画语义铁律」与 `AI漫画工作流.md` §3 改成漫画；禁止只改路由名、表名仍留分镜字段与镜头选项。
4. 编排层不进分格台，进独立的「页组装」页面（必须有完整 UI，禁止只接 API）：
   - **先拼页**：把已完成的多格底板选进槽位，组成单格 / 四格 2×2 / 竖四格；**必须能导出合成页图**；几何只认各槽真实底板像素比例
   - **再加字与声**：气泡贴字可拖（文案、类型、位置、缩放）；按格 TTS，下载后本地归档
5. **TTS 是独立全局配置**，与 AI 生图配置并列，不读图片预设，不把某一个供应商的 URL/密钥写死在业务服务里。首个适配器可以是 PearAPI `freedub`；换 TTS 只注册新适配器。
6. **定妆标准图与分格底板均支持 AI 生成或本地上传**（上传后同样走本地归档与选用指针）。
7. **画幅/比例/参考图上限**：只投影当前手选图片模型真实能力；缺手选画幅则禁用生成并说明原因；不合并多模型、不注入通用比例表。
8. 对用户可见文案统一称「话」；禁止「集」。代码可用 `episode`。
9. 交接在页组装完成：话数包是可下载文件包（含底板文件、字层、阅读序、页信息），不是只返回 JSON；另有合成页图导出。工程 ZIP 若仍在则删除。
10. **真实外部全链验收以 Agnes 图片模型为主**（免费）；确定性测试通过后跑通定妆→分格台底板（含上传抽查）→页组装四格导出；TTS 真实调用分开报告。
11. **代码量纪律见文首反阉割铁律**：没必要的视频可以删；留下的生图、资产、分格台、页组装不许阉割。不要求行数和旧版一致或更多，禁止注水，也禁止把真实逻辑压成短桩。

### 命名硬规则（验收必查）

| 表面 | 允许 | 禁止（产品主路径） |
| :--- | :--- | :--- |
| 用户可见中文 | 话、分格、分格台、页组装、底板、话数包、节拍、动作定格、表情、取景、构图、视角、光线、氛围 | 集、分镜（指镜头）、镜头视频、预览成片、**机位**、**景别**、**镜头运动**、**环境声**（作 A 层规格）、把「对白/旁白」当作分格台生图主字段 |
| 代码标识符 | `episode`（话）、`panels`（分格表）、`panel`；漫画规格字段名见下节 | 用户 `message` 写「第 N 集」；产品路由 `/film/`；产品主路径继续暴露 `camera_movement` / 分格表单绑 `dialogue`/`sound` 进配方 |

### 两个区位（不可并页）

侧栏顺序固定为四段：`总览与剧本` → `资产图` → `分格台` → `页组装`。

| 区位 | 路由 | 这一页只做什么 | 这一页禁止出现 |
| :--- | :--- | :--- | :--- |
| **分格台**（第一步组装） | `/comic/:id/panels` | 逐格生产**一张底板**：选格、编**漫画规格**、显式组装这一格的图片配方、手选画幅、生成或上传、历史选用/清除、图片待复核 | 拼页、导出合成页、气泡、拖字、配音、话数包、阅读序条、主路径步骤条；**禁止**景别/机位/镜头运动/环境声等分镜课表单 |
| **页组装**（第二步组装） | `/comic/:id/compose` | 把已有多格底板选进槽位拼成一页；然后贴气泡（可拖、可缩放）、按格配音；导出合成页；下载话数包 | 重新把「生成这一格底板」当成这一页的主按钮；不要退回视频成片 |

分格台主路径：选格 → 编漫画规格（节拍/定格/取景/视角/光影，无运镜无环境声）→ 组装这一格配方 → 保存快照 → 手选画幅 → 生成或上传这一张底板 → 历史选用。对白不在这一步主编辑。门闸不低于旧分镜台。

页组装主路径：按阅读序选格填槽 → 同比例拼成四格或竖四格（缺底板或比例不一致则明确拒绝）→ 拖动气泡并保存（对白/旁白在这里）→ 配音归档 → 导出合成页 → 下载话数包。

## 当前事实（2026-09-22 owner 复测后）

- **进行中脏工作区**：相对 HEAD 约 `+4300 / -4000` 量级；视频主链文件已删；`panels` + `/panels` + `/compose` + TTS 配置页已存在；自动测曾报绿。**未 commit / 未 push。**
- **已确认仍失败的产品语义（owner 实机否决）**：分格台检查器仍是短剧分镜字段——「规格与画面描述」「景别/机位/镜头运动/环境声」「对白/旁白」进 A 层规格；组装与文档仍按景别/机位写图片合同。**表名换了、语言没断 = 漫画未完成。**
- 语义补丁前 schema `panels` 曾含 `shot_size` / `camera_angle` / `camera_movement` / `sound` / `dialogue` 等短剧列；本轮已断裂为 `framing` / `viewpoint` / `expression` 等漫画字段，前端检查器同步改为节拍/定格/取景/视角/光影。
- 区位分页方向正确：分格台只出底板、页组装做拼页/气泡/TTS——**结构对，语义错。**
- Owner 数据已清空 → 允许空库改 schema，不必迁移旧列含义。
- 理念合同：`AI漫画工作流.md` §3.1 对照表仍是验收真相。
- **history 已钉死**两轮薄壳失败；本轮新增失败类型：**「视频删了、分镜壳还在」**。
- 文档曾被提前改过；因语义未断，**须在语义补丁后再对齐 `spec`/`README`，不得假装 I 已永久完成。**

## 正向测试合同（先红后绿）

只写保护下列**应当成立**行为的测试；名称与断言都说「做成了什么」，禁止「没有什么」：

1. 显式组装产出可用的 `panelRecipe`（含 prompt 与按 ID 带入的资产标准图 references），保存后生图入口消费该快照；**配方文本含取景/构图/定格等漫画块，不含对白、环境声、镜头运动。**
1b. 分格台保存/展示规格使用漫画字段与文案；`camera_movement` 与 A 层 `sound` 不再作为产品主路径写入；对白主编辑在字层 API。
2. 字层保存与重读成功；更新字层后底板指针与 generation 历史保持可用。
3. **四格导出成功**：`grid_2x2` 填满 4 个本地底板 → 本地可读图片；画布由真实宽高比拼出。
4. **四格导出明确拒绝**：缺底板或比例不一致 → 失败且错误可见。
5. **底板上传成功**：上传可作为当前底板选用。
6. **生图画幅手选**：仅当前图片预设目录画幅；无画幅则禁用并说明原因。
7. **TTS 成功归档**：业务指针指向本地可读音频（禁止业务指针留 `http(s)`）。
8. 话数包导出成功。
9. `/comic/:id/...` 可达；用户可见「话」。

**测试删除边界（防偷懒掏空回归）：**

- **允许删除**：明确只服务视频主链的测试文件（视频生成、compose、成片/镜头包、storyboard 视频配方/视频就绪等）。
- **禁止整文件删除**：provider / transport / registry / errorContract / audit / config / 资产组装 / mediaReference / taskProgress 等非视频测；应保留或迁到 `panels` 语义后仍保护同等行为。
- **禁止**用一份十几行压缩漫画测顶替被删的大片回归面。
- **不要**改写成「视频路由应 404」负测。

## 目标

- 自动测试与文档共同证明：土豆漫画主链可运行，且工作台体验与代码量不低于成熟短剧台。
- 主链按两个页面走：`剧本 → 定妆 → 分格台（单格底板）→ 页组装（拼页，再加气泡与配音，再导出）`。
- 短剧视频主链从代码、API、UI、spec、README、相关 skill 主路径消失。
- **消失的是视频能力，不是工作台厚度。两个区位不许并回一页。**

## 不做范围

- 不做旧数据迁移、双写、兼容层、旧 ZIP 导入。
- 不做 AI 写剧本 / 自动拆格 / AI 一次画四格 / I2V / 口型。
- 不做自由排版画布、出版级精排、逐气泡高亮听书、完整 DAW。
- 不做短剧模式开关或「同一产品双主链」。
- **不做「先薄壳能跑再补厚度」**；不做压缩巨行伪装；不做未过工作台量尺就文档收口。
- 默认不 commit/push，除非 owner 当次授权。

## 设计

### 断裂策略

1. **唯一主干**：漫画工作流；删除短剧视频主干，不留「废弃但可调用」的产品入口。
2. **空库重建**：schema 以漫画字段为准直接建表/改表；允许删除 `video_*` 列、视频 generation 表或停用其产品路径。
3. **体验不断裂**：领域与媒体主链可断；工作台成熟度不断。删除视频相关代码后，必须用漫画 UI/服务填回厚度，使整仓 diff 与工作台量尺符合文首铁律。
4. **事实 owner**：
   - 底板配方：assembler 本轮改为 panel 专用（如 `panelPromptAssembler`）
   - 资产生成提示词：保持显式组装 owner
   - 页合成：新建 page layout 服务（模板 + 槽位 + 真实像素拼图导出）
   - TTS：新建 freedub 适配器 + 音频归档
   - 字层：captions（文案+气泡类型+归一化几何）
5. **品牌与路由（本轮一次改完）**：显示名「土豆漫画」；`/comic/:id/script|assets|panels|compose`；删除产品主路径 `/film/`；用户可见一律「话」。`panels` 与 `compose` 是两个页面，不能共用一个工作台组件把两步叠在一起。
6. **标脏按层（禁止剧本一刀切脏底板）**：
   - 只改对白/旁白草稿或 B 字层 → 不自动脏 A
   - 改出场资产、底板规格、资产标准图/文本影响引用 → 脏 A
   - 只改 C 槽位/模板或只重跑 TTS → 不脏 A
   - 剧本大改情节不自动扫全话底板为脏
7. **定妆→底板一致性**：组装时出场资产标准图按 ID 写入 references；上限不足 → 明确失败（优先），禁止静默丢图。
8. **阅读序**：分格 `order` 为故事阅读唯一真相；`reading_order.json` 含分格序与页序。

### 旧分镜台 → 漫画分格台替换表（实施时逐项勾销）

| 旧成熟台能力位 | 漫画落点 | 禁止的偷懒形态 |
| :--- | :--- | :--- |
| 镜头轨道+缩略图+勾选 | 分格轨道+缩略图+勾选 | 纯文字列表无预览 |
| 预览区+空状态 | 底板预览+空状态 | 无预览仅表单 |
| 规格检查器（景别/机位/运镜/环境声/对白…） | **漫画规格检查器**（节拍、动作定格、表情、取景、构图、视角、光线、氛围）；对白/声不在此页主编辑 | 只改标签仍留镜头选项；「规格与画面描述」+「镜头运动」原样留下 |
| 显式组装+配方可编辑快照 | 同样保留组装按钮与可编辑最终提示词/参考图；**组装顺序按漫画语义铁律** | API 有组装、UI 无按钮；配方仍拼接对白/运镜/环境声 |
| 图片能力摘要+手选画幅门闸 | 仅图片模型四项投影；无画幅禁用 | 去掉门闸或自动兜底 |
| 生成/上传/历史选用/清除 | 底板同等 | 只能生成不能上传或无历史 |
| 图片待复核 / 配方待重装 | 保留（无视频复核） | 去掉标签与确认 |
| 视频配方 / 出视频 / 成片 / 镜头包 | **从分格台删除**。拼页、气泡、配音、话数包放到**页组装**，不填回分格台检查器 | 删视频后在分格台堆一堆说明块冒充新功能；或把两步挤在同一页 |
| 任务跟踪与错误可见 | 继续用现有 generation tracker / 错误合同 | 失败无提示 |

### 数据形态（空库直接建成；禁止「表名暂留」）

- `dramas` / `episodes`（语义=话）/ `project_assets` 保留
- **`panels`**：由 `storyboards` 本轮改名/重建；含配方、底板指针、图片待复核、配方待重装；**无视频字段**；规格列按漫画语义铁律重建（空库可直接改列名/删列）。**必须删或停用产品路径上的** `camera_movement`；**不得**把 `dialogue`/`sound` 当 A 层生图规格主列（对白在 `panel_captions`，音频在 `panel_audio`）。`shot_size`/`camera_angle` 若保留列，产品文案与选项必须改为取景/视角，禁止「景别/机位/全景/平视/固定镜头推进」课表。
- `panel_captions` / `panel_audio`：字层、声层
- `page_layouts`：`single` / `grid_2x2` / `vertical_4`
- `image_generations` 保留；删除 `video_generations` 与视频 compose
- 音频：本地路径为业务指针；来源 URL 仅审计

### TTS 配置（与 AI 生图配置分离）

- 全局「TTS 配置」：选择语音供应商、该供应商自己的地址与凭据、角色/风格目录。不放进 AI 生图页，不复用当前图片预设。
- 业务服务只调用「按当前 TTS 配置合成」。协议、鉴权、音频地址解析只存在于对应适配器。
- 首个适配器：PearAPI `GET/POST https://api.pearapi.ai/api/freedub`。
- 完成断言：下载到本地；分格业务指针禁止 `http(s)`。未知或未配置的 TTS 供应商直接失败，不猜测。

### 供应商能力与画幅纪律（继承 2026-09-21，不得回退）

| 投影项 | 含义 |
| :--- | :--- |
| **模式** | 文生图 / 图生图；由是否有参考图决定 |
| **参考图上限** | UI 显示已选/上限；超限拒绝，禁止静默丢图 |
| **画幅列表** | 仅目录声明；必须手选；禁止通用比例表与首项兜底 |
| **能力来源** | 目录原始或 `adapter-override` |

资产页与分格台只投影当前手选图片预设；无预设或无画幅 → 禁用生成。定妆与底板均支持生成或上传；上传像素即拼页比例事实。

### 页导出

- 模板：`single` | `grid_2x2` | `vertical_4`
- 只认本地底板真实宽高；同页比例必须一致（约 1px 误差），否则明确拒绝
- 缺槽/缺底板 → 明确失败；烧字可二期

### UI 区位

1. 总览与剧本（话）
2. 项目资产库（定妆：生成或上传 + 手选画幅；**保持现有成熟密度**）
3. **分格台**：母版三栏，只出单格底板
4. **页组装**：独立页面。上为已有底板的选格与页模板槽位，下为这一页的气泡与配音；导出合成页和话数包在这一页。样式写进 `app.css`，禁止无容器裸文本块

删除产品入口：出视频、整集视频合成、成片/镜头包（若仍在）、工程 ZIP。禁止把第 4 步的控件留在第 3 步页面上。

## 实施任务

### A. 调查与影响地图

- [x] 通读 `AI漫画工作流.md`、本 plan、`history.md` 两轮失败条目、`AGENTS.md`、`spec.md`、命中 skill。
- [x] 用 `git show 4ba454c:.../StoryboardWorkspace.tsx` 核对母版行数/字符量，写入影响地图。
- [x] 列出短剧视频相关：路由、服务、类型、前端 API、字段、compose、测试名、文档段落。
- [x] 列出可保留：项目/话/资产/图片生成/任务/归档/显式组装/成熟 UI 结构。
- [x] 列出视频位删除后，哪些留在分格台，哪些必须挪到页组装。
- [x] 确认 TTS 适配器如何注册，而不是把 freedub 写进生图配置。

### B. 断裂删除短剧视频主干（同时准备填回）

- [x] 删除分镜视频提交、视频配方、视频待复核、整集 compose、成片/镜头包主路径 UI。
- [x] Schema/类型去掉视频产品字段。
- [x] 删除**仅**视频相关测试；保留非视频测。
- [x] 保留手选画幅门闸与目录能力投影纪律。
- [x] 自查：本批删除对应的漫画填回项已列入 C–G，禁止「只删不补」。

### C. 分格底板合同改写

- [x] 组装只出 `panelRecipe`；标准图按 ID 进 references；上限不足失败可见。
- [x] 底板 AI 生成 + 本地上传；手选画幅。
- [x] 文案分格/话；标脏按层；待重装/待复核仅图片。
- [ ] **漫画语义**：组装顺序与字段按文首「漫画语义铁律」；配方不含对白/环境声/镜头运动。

### D. 字层 B（只在页组装）

- [x] captions 持久化（文本、气泡、位置/缩放）。
- [x] 页组装 UI：气泡+旁白；**可拖**；显式保存；改字不调生图。
- [x] 话数包可带字层 JSON。
- [ ] 确认分格台不再把「对白/旁白」当 A 层主编辑字段（对白只在本页）。

### E. 页合成 C（只在页组装）

- [x] 模板注册表；layout；按 order 填槽。
- [x] 真实宽高拼页；同比例门闸；成功与拒绝均可测。
- [x] `reading_order.json` 含分格序与页序。

### F. 声层 D（只在页组装；TTS 配置独立）

- [x] TTS registry + PearAPI freedub 适配器；业务不写死 URL/密钥。
- [x] 全局 TTS 配置页，不进 AI 生图配置。
- [x] 页组装按格 TTS；本地文件断言。
- [x] 真实调用与 Agnes 分开报告（本轮无凭据，明确列为未执行）。
- [ ] 确认分格台规格里不再出现「环境声」字段。

### G. 分格台前端（母版改写 · 量尺必过）

- [x] 从 `4ba454c` 母版展开落盘，再改成只出单格底板（禁止另起薄壳）。
- [x] 页面上不存在拼页、气泡、配音、话数包。三栏样式正常，不再往上堆说明块。
- [x] 结构密度大体回升（约旧台九成量级）；交互块仍在。
- [ ] **语义改写未完成（owner 否决）**：消灭景别/机位/镜头运动/环境声/「规格与画面描述」分镜壳；改为节拍/定格/取景/视角/光影等漫画文案与选项。
- [ ] Demo/默认值不再预填「全景 / 平视 / 固定镜头轻微推进」类镜头课文本。

### G2. 页组装前端（新页面）

- [x] 侧栏新增「页组装」，路由 `/comic/:id/compose`。
- [x] 选格填槽、拼页、拖气泡、配音、导出页、下载话数包都在这一页，并有正式 CSS 布局。

### H. 交接与清理

- [x] 话数包为可下载文件包（整话/选中），包内有底板文件。
- [x] 删除工程 ZIP 与视频成片路径（若还在）。

### I. 文档与 skill（仅当 C/G 语义 + 结构都过）

- [ ] 按漫画语义铁律重写/校正 `spec.md`（删除仍写景别/机位进配方的段落）。
- [ ] 更新 `README.md`、`history.md`（追加「视频删了分镜壳还在」失败记录；保留既有失败条目）。
- [x] 品牌「土豆漫画」；路由 `/comic/`（已有，语义补丁后勿回退）。

### J. 验证与收口

- [ ] 语义补丁后复跑全量正向 test / typecheck / build / lint。
- [ ] 人工打开分格台：检查器一看就是漫画格，不是分镜表（owner 否决项清零）。
- [ ] **Agnes 真实全链**证据写入收口（未授权/无凭据，未执行）。
- [ ] freedub 真实调用分开报告（未授权/无凭据，未执行）。
- [ ] 交付前自查：无镜头运动/环境声进 A 层；无对白进图片配方。
- [ ] 回填收口；默认不 commit/push。

### K. 漫画语义断裂（当前最高优先级 · 2026-09-22 重开）

- [x] 读 `AI漫画工作流.md` §3.1–3.3 + 本文首「漫画语义铁律」，列出 UI 标签、schema 列、assembler 拼接块、Demo 默认值四处命中表。
- [x] schema/类型：删除 `camera_movement`；`dialogue`/`sound` 退出 A 层产品写入；`shot_size`/`camera_angle` 改为 `framing`/`viewpoint`。
- [x] `storyboardPromptAssembler`（panel 组装 owner）按漫画顺序拼接，并以正向断言保护漫画块与干净画面约束。
- [x] `StoryboardWorkspace` 检查器文案与表单项改为节拍、动作定格、表情、取景、构图、视角、光线、氛围；三栏交互未删薄。
- [x] Demo / 初始化样本去掉影视默认句。
- [x] 正向测：组装出的 prompt 含漫画块、字层仍可存；未新增追悼负测。
- [x] 语义补丁后更新 `spec.md`、`README.md` 与 `history.md`；任务整体仍待全量与人工收口。

## 模块影响地图（实施时逐项勾销）

| 区域 | 动作 |
| :--- | :--- |
| `panelPromptAssembler` / `storyboardPromptAssembler` | 仅 panel 图片配方；**按漫画语义铁律拼接**；厚度不低于旧组装 |
| 页图合成 / 原 composition 视频 | 删视频 concat；新页导出服务 |
| `*video*` 生成服务/路由 | 产品主路径删除 |
| `episodeDeliveryService` 成片/镜头包 | 删除；改为话数包 + 页导出 |
| `workspaceRoutes*` | 分格/字层/页导出/TTS；入口齐全 |
| TTS 适配器 | 独立 registry；首个为 freedub；不进 AI 生图配置 |
| schema | `panels` 去视频；加字层/页/音频；**再去运镜/A 层对白·环境声产品路径** |
| `App.tsx` | `/comic/:id/script|assets|panels|compose`；删 `/film/` |
| **分格台前端** | **母版三栏 + 漫画规格文案**；禁止分镜课表单 |
| **页组装前端** | **新页面：拼格、气泡、配音、导出** |
| 资产页 / 剧本页 | 语义替换；**禁止借机阉薄** |
| `ProjectsPage` | 导航进 `/comic/` |
| `frontend/src/api/*` | 去 video/compose；加 panel/page/tts |
| 测试 | 只删视频测；补漫画正向测；禁止掏空 |
| `spec` / `README` / skills | **语义补丁后再收口** |
| Demo | 漫画话/分格示例（无影视默认句） |

## 验收标准（完成定义）

1. 空库分两页走通：分格台出单格底板；页组装拼页、气泡、配音、导出。分格台页面上看不到第二步控件。分格台密度不低于旧分镜台。
1b. **打开分格台检查器：文案与字段是漫画格（节拍/定格/取景/视角/光影），不是分镜表（景别/机位/镜头运动/环境声）；对白主编辑在页组装。**
2. 工作台不得借语义改写再次腰斩；禁止注水。
3. **整仓 diff 无 +几百/-几千腰斩；净变化与「删视频 + 补漫画」叙事一致。**
4. 字层可存可拖可重读；改字后底板仍可读；**组装 prompt 不含对白/运镜/环境声。**
5. 同比例四格导出成功；缺底板/比例不一致明确拒绝。
6. TTS 从独立配置合成，本地音频可读；话数包是含底板的文件包；`/comic/` 含 `panels` 与 `compose`；称「话」「土豆漫画」。
7. 手选画幅/目录投影纪律保持。
8. 非视频回归测未被整锅端掉。
9. 确定性正向测试通过；Agnes 与 freedub 分开写入收口。

## 验证计划

- 自动：全量正向 test、typecheck、build、lint。
- **语义人工**：分格台自检——无景别/机位/镜头运动/环境声作 A 层标签。
- **量尺**：汇报 shortstat；禁止借机削薄三栏。
- 必过正向：组装（漫画块）、上传、四格成功与拒绝、TTS 归档、话数包、字层保存。
- Agnes 图片全链必做；freedub 另报。
- 禁止负性测试；分开报告：自动 / 语义人工 / Agnes / freedub / 未验证。

## 风险

- 烧字进四格页可降级；**四格底板合成不可降级**。
- freedub 临时链：只信本地归档。
- 参考图上限不足：失败可见。
- **最大风险（已发生两次）**：把断裂理解成整台变瘦、压缩壳、掏空测试、未达标先收口。
- **区位风险（曾发生）**：把拼页、气泡、配音堆在分格台。两步必须分页。
- **语义风险（正在发生 · owner 已否决）**：删了视频却留下景别/机位/运镜/环境声分镜壳。只改表名不算漫画。
- 禁止 I2V 回潮；禁止自由画布膨胀；复杂页模板首期不做。

## 收口

- **状态：未收口。** 结构主链（分页、生图、页组装、TTS、话数包）大体落地；**漫画语义未断 = 任务未完成。**
- 完成事实（仅结构侧，不得当作总完成）：视频主链删除；`panels`/`compose`/TTS/字层/页导出存在；自动测曾绿。
- **阻塞完成项**：任务 **K**（漫画语义断裂）以及 C/G/D/F/I/J 中未勾项。
- 未验证项：Agnes/Freedub 真实外部调用、浏览器人工拖拽；语义人工否决项清零前不得写「可交付」。
- 外部操作（commit/push）：未执行；默认仍不授权。
