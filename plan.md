# 当前任务：建立唯一短剧生产核心、模块化错误主干并统一产品身份

## 需求

- 文本、图片、视频只是三种材料，不能继续与角色、场景、道具、分镜等生产用途混为同一层“节点类型”。
- 产品和代码统一表达“故事 → 剧本 → 资产图 → 分镜清单 → 分镜图 → 镜头视频 → 整集”的短剧生产链。
- 角色、场景、道具属于资产图分类；资产图是跨镜头复用的标准图，分镜图是单个镜头的一次性画面，两者必须明确区分。
- 所有 AI 文本操作必须公开默认系统提示词，节点创建后可见、可编辑、可恢复默认，并把编辑值真实传给后端。
- 运行中的节点、选区、下游分支或整个画布必须可停止；停止后不再提交后续节点，并调用现有任务取消接口停止当前任务状态。
- 任一节点运行失败后终止本次运行范围；404、429、500、502、503、504、超时、网络异常及业务错误由统一模块转换为友好原因和处理建议，未开始节点保持原状态，用户稍后手动重跑。
- 故事、剧本、资产提取、资产图、分镜清单、分镜图、镜头视频和整集合成必须属于唯一生产领域；画布、默认模板、Demo、检查器、API 和后端服务都是同一核心的投影，不能各自重写流程定义。
- 保留文本选配 API、四种媒体生成、自由连线、批量运行、工作流组和整集合成能力。
- 不删除 `backend/data`，不增加自动清库或迁移脚本。
- 产品身份统一为 `tomato-ai-drama`；磁盘根目录暂不改名，代码、配置、包名、界面、文档、导出文件和远端仓库不保留旧产品名。

## 实施前事实

- 旧节点联合类型把文本/图片/视频材料与角色、场景、道具、分镜用途混在一个维度。
- 旧节点使用多个重叠字段同时表达生产用途、输入方式和媒体生成方式，单个值会在不同上下文中产生不同含义。
- 旧关联字段同时承载角色、场景、道具和分镜记录，名称与真实含义不一致。
- 默认模板从“剧本”直接开始，缺少独立故事节点；页面工具栏按技术节点类型添加，不能直观看出短剧生产顺序。
- 剧本、角色提取、场景提取、道具提取、分镜拆分的系统提示词原先硬编码在单个旧生成服务中，前端看不到也不能覆盖。
- 后端已有 `POST /api/v1/tasks/:id/cancel`，但画布没有停止控制；当前取消只更新任务状态，不能保证第三方供应商已接收的任务物理终止。
- owner 在本轮实施前已删除 `backend/data`；空库启动验收重新创建当前 schema，临时验收项目随后删除，普通启动与验证没有自动清库。
- 当前工作区包含此前完整重构的未提交改动；本轮只能修改相关实现、测试和事实文档。

## 失败测试或失败证据

- 旧画布状态的联合类型直接证明材料与用途混用。
- 旧编排器通过多个重叠字段猜测节点究竟是文本、图片还是视频。
- `CanvasInspector.tsx` 把“分镜”“文生分镜图”“图生分镜视频”全部塞进同一个节点类型下拉框。
- 旧生成服务直接写死系统提示词，请求合同没有 `system_prompt` 可编辑输入。
- `CanvasPage` 只有布尔 `running` 和运行按钮，没有停止信号、当前任务 ID 管理或取消动作。

## 目标

- 稳定的生产角色 `role` 成为节点语义 owner；`material`、生产阶段、`assetKind`、文本动作和默认生成方式全部由角色目录派生，不写入节点形成重复事实。
- 文本输入方式与媒体生成方式使用独立字段；关联后端记录使用 `linkedRecordId(s/index)`。
- 默认提示词由独立后端模块拥有，前后端通过同一 API 公开默认值；节点快照保存用户覆盖值。
- 停止动作可中断前端轮询和后续编排，并取消已知的当前后端任务；界面明确显示“已停止”。
- 后端统一保留机器可读错误代码、HTTP 状态、供应商和可重试信息；前端统一负责友好文案与运行终止策略，画布页面不写状态码分支。
- 生产节点只保存稳定的 `role`；材料、用途、资产类别、文本操作、默认生成模式、标签与说明都由生产目录派生，消除可互相矛盾的重复字段。
- 前端执行器只把节点翻译为统一生产命令；后端 `ProductionWorkflowService` 统一接收命令并委派给文本、资产、图片、视频或合成模块。供应商适配、任务、错误、保存和数据库保持独立横切职责。
- 默认模板完整展示从故事到镜头视频的链路；Demo 使用同一语义结构。
- 工具栏、节点卡片、检查器、错误说明、README 和 spec 使用一致的用户语言。

## 不做范围

- 不修改数据库 schema 或 Provider 注册架构。
- 不承诺第三方供应商本身支持物理撤销；当前后端取消合同只能停止本地任务状态与前端编排。
- 不新增配音、音乐、音效、字幕或时间线剪辑能力。
- 不删除或手工改写当前数据库。
- 不执行真实供应商生成、部署或发布；commit 与 push 按 owner 本轮明确授权在闭环验证后执行。
- 不增加单边强调色或侧边状态条。

## 设计

- 新建画布节点语义模块，集中维护材料、用途、资产分类、文本操作和节点数据合同。
- 新建节点模板目录，统一定义可添加的生产节点、默认名称、默认材料、运行方式和默认提示词 key。
- 后端新增只读默认提示词目录 API；生成请求允许 `system_prompt` 覆盖，空值明确拒绝或回退由统一函数处理，不在各 handler 重复判断。
- 执行器从生产角色目录取得材料和动作，再翻译成统一生产命令，不通过多个可冲突字段猜测。
- 资产图只有在关联对应资产记录时调用角色/场景/道具专用接口，否则使用通用图片生成；分镜图和镜头视频只把关联分镜 ID 传给对应媒体接口。
- 前端运行会话使用独立控制器记录停止状态与活动 task ID。停止时调用任务取消 API；轮询收到本地停止信号后退出；批量循环在每个节点前检查停止状态。
- 默认模板版本和 Demo 版本递增，生成物只写当前结构，不维持旧字段双写。

## 实施任务

- [x] 调查旧节点合同、执行分派、模板、Demo、检查器、持久化、任务取消和提示词影响面。
- [x] 建立默认提示词 owner、只读目录 API 与请求覆盖测试。
- [x] 建立材料与生产用途分离的节点合同和模板目录。
- [x] 重写 store、执行器、模型选择、节点卡片与检查器消费者。
- [x] 实现可停止运行会话并接入后端任务取消接口。
- [x] 升级默认模板、复杂 Demo 和用户文案。
- [x] 更新并补齐语义、提示词与停止行为测试。
- [x] 同步 README、spec、history。
- [x] 完成前后端测试、类型检查、lint、build、样式扫描、编码与差异检查。
- [x] 建立模块化错误合同与友好错误展示，改为首次失败即终止本次运行并补齐回归测试。
- [x] 建立唯一生产角色目录、统一执行命令和后端生产编排入口，替换画布、模板、Demo 与检查器的重复流程定义。
- [x] 检查其余重复职责与反向依赖，按 SOLID 收口而不制造无行为空壳。
- [x] 重新完成前后端完整验证、编码、样式、敏感信息与差异检查。
- [x] 统一产品命名、切换远端仓库，闭环后提交并推送 `master`。

## 验证计划

- 默认模板明确包含故事、剧本、三类资产提取、三类资产图、分镜清单、分镜图和镜头视频。
- 每个节点同时有且只有一个材料与一个生产用途；角色/场景/道具通过 `assetKind` 区分。
- 分镜图优先取得分镜清单产生的记录 ID，并把上游资产图作为参考图；镜头视频取得分镜图作为参考。
- 六类文本默认提示词可以通过 API 获取，前端节点展示完整内容，编辑后的 `system_prompt` 到达 Provider 请求。
- 停止单节点或批量运行后不再运行后续节点，当前任务收到 cancel 请求，相关节点状态为已停止而不是伪装完成。
- 404、429、500、502、503、504、网络和超时错误显示明确原因及重跑建议；任一错误发生后，当前节点失败、运行范围终止且后续节点不再提交。
- 全仓活动代码不再包含废弃节点字段、旧结构判断、升级函数或双写逻辑。
- 前后端 test、typecheck/lint、build 全部通过；禁用单边强调色扫描保持 0。

## 收口

- 完成事实：画布节点只保存稳定生产角色；默认提示词公开可覆盖；文本和四种媒体模式经过唯一生产主干；节点、选区、下游、整画布和整集合成共享可停止运行会话；模板与 Demo 只生成当前合同，不保留旧字段识别、迁移或双写。
- 实际命令：前端 `npm.cmd test` 25/25、`npm.cmd run lint` 0 警告、`npm.cmd run build` 通过；后端 `npm.cmd run typecheck` 0 诊断、`npm.cmd test` 44/44、`npm.cmd run build` 通过；前后端依赖审计为 0 漏洞，`git diff --check`、旧产品名、废弃节点提示、禁止单边强调色扫描通过。
- 启动验收：空库后端、前端均实际启动；健康接口返回 `tomato-ai-drama API` 与 0.0.1，页面标题、六类提示词、项目 CRUD 和 Vite API 代理成功；新数据库文件为 `tomato_ai_drama.db`，临时项目删除，服务停止。
- 未验证项：本轮没有调用真实 Agnes/PearAPI，也没有做移动端人工视觉验收；浏览器自动截图只覆盖桌面默认画布。
- 剩余风险：第三方已经接收的异步任务若没有撤销协议，停止只能终止本地请求、轮询、状态写回和后续编排；前端生产包仍有约 1.48 MB 单块警告，需要后续按路由或依赖拆包。
- 外部操作：Git 远端已切换到 `https://github.com/luckymaomi/tomato-ai-drama.git`；主实现提交 `9f24838` 已推送 `origin/master`，未部署或发布。
