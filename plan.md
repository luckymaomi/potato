# 当前任务：资产卡与分镜配方显式组装

## 目标与边界

按 `C:\Users\Administrator\Desktop\AI短剧工作流.md` 和 owner 最新要求，把资产标准图提示词、分镜图片配方、分镜视频配方统一收口为“用户点击后才由后端组装，结果可见、可编辑、可保存，生成只消费已保存结果”的显式流程。

- 不调用 Agnes、PearAPI 或其他真实供应商，不做付费验收。
- 资产和分镜图片继续只允许单项生成，不恢复批量出图入口。
- 不恢复 AI 写剧本、拆镜、提取资产或生成镜头描述入口。
- 完成确定性测试、静态检查、浏览器本地验收和本地简体中文提交；不 push。
- 不清理 `backend/data`；owner 已明确数据库由其删除，本次按断裂式新 schema 实现，不写兼容迁移。
- 保留工作区中 4 个 `frontend/src/features/generation/*` 既有无内容差异文件，不纳入本次提交。

## 修改前事实

- 资产卡建卡时由 repository 自动调用 `assembleAssetOutputPrompt`，新卡立即得到非空 `output_prompt`；页面没有产出预设下拉和“组装提示词”按钮。
- 角色卡仍有 `identity_tags`；部分场景、道具字段使用 tags 选择器，部分普通资料字段使用多行输入。
- 分镜页面允许编辑 `image_prompt` 与 `video_prompt`，但生成图片或视频时，后端仍现场调用 `assembleStoryboardRecipes`，把镜头资料、资产文本和引用重新拼装；用户看不到实际最终发送内容。
- 分镜生成时引用当前资产标准图和本镜额外参考图，没有保存“点击组装时”的图片/视频引用快照。
- 当前 schema 强制资产 `output_prompt` 非空；项目 ZIP `format: 6`。

## 目标主干

### 统一交互规则

- 选择预设、修改字段、切换卡片或打开页面都不触发组装，也不覆盖已有结果。
- 只有点击明确的“组装提示词”或“组装两份配方”按钮，前端才请求后端 assembler。
- 后端是唯一组装 owner；前端只提交当前规格并展示返回结果，不复制模板正文或拼接规则。
- 组装结果先填入当前表单；用户可查看、编辑、完全重写，再保存或生成。
- 生成入口只消费数据库中已保存的最终文本和引用快照；不在生成时调用 assembler。

### 资产卡

- 角色卡删除 `identity_tags`；角色、场景、道具的所有非枚举资料字段统一使用普通单行输入框。
- `output_type` 恢复为按资产类型限制的预设下拉：角色 A-D、场景三类、道具两类。
- 新建卡的 `output_prompt` 默认为空；点击“组装提示词”时，后端根据当前名称、资料和 `output_type` 返回完整模板文本。
- 文本域保持可见可编辑；标准资产图生成读取保存后的 `output_prompt`，输入参考图继续走独立图片通道。

### 分镜台

- 点击“组装两份配方”时，后端一次返回 `imageRecipe` 和 `videoRecipe`。
- `image_prompt` 与 `video_prompt` 分别保存两份最终、可编辑的提示词；图片文本不含声音和对白，视频文本包含对应有值字段。
- 新增图片、视频两组已组装引用快照，分别持久化并在界面显示；来源仅为已选资产卡的标准图与本镜额外参考图。
- 分镜图片生成读取保存后的图片提示词和图片引用；镜头视频生成读取保存后的视频提示词和视频引用，并额外使用当前分镜图作为首帧。
- 镜头资料、资产选择或额外参考图发生变化后，已组装配方保持不变，直到用户再次点击组装。

### 数据与归档

- `project_assets.output_prompt` 改为允许空字符串；实际生成前做非空业务校验。
- `storyboards` 新增两组配方引用 JSON 字段；项目读取、Demo 和 ZIP 往返同步更新。
- 删除 `identity_tags` 的字段合同；项目 ZIP 断裂升级到 `format: 7`，不接收旧格式。

## 完整影响面

- 后端：schema、领域类型、资产/分镜 repository、两个 assembler、显式组装路由、图片/视频生成路由、项目读取、ZIP、Demo。
- 前端：领域类型、API、资产表单、分镜表单、组装 loading/防串写、配方引用展示、样式。
- 测试：新卡空提示词、显式组装、用户编辑结果原样生成、两份配方快照、普通字段字符串、ZIP 7 往返、Demo 初始化不触发组装。
- 文档：`spec.md`、`README.md`、`backend/README.md`、`history.md`、外部《AI短剧工作流.md》只同步已验证事实。

## 实施状态

- [x] 读取规约、命中 Skill 和上一轮交付记录，核验当前资产与分镜真实组装链路。
- [x] 建立本轮断裂式变更合同和完整影响地图。
- [x] 建立资产显式组装、分镜双配方快照及生成消费的正向失败契约。
- [x] 修改 schema、领域类型、repository、项目读取、ZIP 7 和 Demo 数据。
- [x] 实现后端资产提示词与分镜双配方显式组装接口，生成入口改为只消费已保存结果。
- [x] 实现资产普通输入框、预设下拉、组装按钮和可编辑文本域。
- [x] 实现分镜双配方组装按钮、两份可编辑文本与引用快照展示。
- [x] 同步当前事实文档和理念文档。
- [x] 完成定向测试、全量 test/lint/typecheck/build、静态扫描和浏览器本地验收。
- [x] 检查差异、敏感信息和提交范围，创建本地简体中文提交，不 push。

## 验证计划

- 后端：显式组装接口返回当前表单对应模板；新卡允许空提示词；生成保存用户编辑后的文本；图片/视频分别读取自己的引用快照；ZIP 7 往返。
- 前端：下拉不自动覆盖；点击才组装；组装结果可编辑；切卡/刷新不串写；非枚举字段为普通输入；已有实时状态反馈保留。
- 完整命令：后端 `npm.cmd test`、`npm.cmd run typecheck`、`npm.cmd run build`、oxlint；前端 `npm.cmd test`、`npm.cmd run lint`、`npx.cmd tsc -b --pretty false`、`npm.cmd run build`。
- 静态扫描：批量出图入口为 0；前端模板正文为 0；生成路由不调用 assembler；`identity_tags` 为 0（历史记录除外）。
- 浏览器：仅使用本地服务和模拟/本地数据验证交互，不提交真实供应商任务。

## 收口记录

- 完成事实：资产卡和分镜台均改为显式点击组装；结果可见、可编辑、可保存；三条媒体生成链只消费已保存结果。角色身份标签与 tags 输入器已移除，ZIP 升级为 7，批量出图入口保持删除。
- 确定性验证：后端 64/64、前端 23/23 测试通过；两端 lint、typecheck、build 均退出码 0。新增 HTTP 契约覆盖显式组装、编辑后生成消费与 ZIP 往返。
- 浏览器验证：本地 fixture 检查资产预设不自动覆盖、两类按钮点击后回填、结果继续可编辑，以及桌面/390px 手机布局；临时服务已关闭。
- 静态扫描：运行时 assembler 只由两个显式接口调用；生成路由只读取三份保存文本；`identity_tags`、tags 输入、批量出图、全局资产库和旧版本字段源码命中 0。
- 未验证项：真实供应商出图/视频效果、人工视觉审美；未读取或改动 `backend/data`。
- 剩余风险：schema 与 ZIP 7 是断裂式合同，旧数据库和旧 ZIP 不兼容；保留 5 条既有 effect lint warning 与 Vite 大包体提示。
- 外部状态：不调用真实供应商；创建本地简体中文提交，不 push；目标服务端口均已关闭。
