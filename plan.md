# 当前执行合同

> 本文件是本轮断裂式重建的执行合同。产品事实以 `spec.md` 为准，历史过程只追加到 `history.md`。本轮不保留旧数据结构、旧入口或旧布局兼容。

## 目标

把短剧制作收成一条可演示的领域工作台主链：

`剧本 → 人物 / 场景 / 道具 → 分镜台 → 分镜图 → 镜头视频 → 整集合成`

项目和剧集是容器；故事总览、剧本、人物、场景和道具是可复用的制作资料；导演台负责把一镜的内容、镜头语言和引用资料组装在一起；生产区按镜头执行出图、出视频和合成。

## 当前边界

- 页面使用项目壳和分板块工作台，不使用无限画布、节点编排、DAG 关系或节点连线表达主链。
- 打开项目默认进入剧本；工作台入口包含剧本、人物、场景、道具、分镜台和生产。
- 分镜台是一镜一包：剧情说明、景别、机位、运镜、动作、对白、场景、资产引用、图像提示词、视频提示词、构图和网格参数都可填写，也都可以留空。
- 九宫格、六宫格、十六宫格和自定义网格只影响单张分镜图的提示词，不把一张图拆成多个镜头。
- 资产最终沉淀为名称、标准图和图片描述。提示词、文生图、图生图和上传参考图只是生成过程，图片历史必须保留。

## 多选规则

- 人物、场景、道具和参考图控件都按可多选实现。
- 用户可以不选、只选一个或选择多个；单个选择只是列表长度为 1，不存在另一套单选逻辑。
- 每个镜头的三类资产彼此独立，分别落库为数组；生产组装按资产 ID 列表解析标准图 URL 列表。
- 资产依赖和参考图都按 ID/URL 列表去重；依赖关系禁止自环和循环。

## 生产并发合同

### 三层异步

1. 供应商调用始终异步提交并轮询供应商任务，直到明确成功、明确失败或用户取消；一次 HTTP 不阻塞到媒体生成完成。
2. 本地生成接口先返回本地任务 ID，后台执行；短文本可以等待文本结果，但图片和视频使用本地任务轮询。
3. 无依赖的图片任务按就绪层并行提交；图片不额外限制并发数量。视频同时最多一个。

### 依赖与互斥

- 资产批量生成先展开依赖闭包；只有所有依赖已有完成并锁定的标准图，当前资产才进入就绪层。
- 同一资产或同一镜头的图片/视频生成在 `pending` 或 `processing` 时拒绝第二次提交。
- 分镜图完成不会自动提交镜头视频。视频只能由用户点单镜视频或明确确认批量视频后提交。
- 批量中单项失败、阻塞或被拒绝只记录该项结果，其他就绪项继续处理。
- 429、408、5xx 和网络暂时性错误只在当前供应商任务层退避重试；首次提交不因网络抖动盲目重发。
- 整集合成是屏障：按分镜顺序收集已有视频，缺少任意镜头视频就明确失败，不生成伪成片。

## Provider 边界

阶段业务只传入文本、资产引用、参考图列表和用户选择；Agnes、PearAPI、模型能力、异步任务号、轮询和退避都属于 Provider 适配器与基础设施协议。显式模型优先，否则使用 AI 配置页全局预设，再否则自动选择。

## 已完成事实

- 已删除画布页面、画布导航、布局接口、布局字段、旧生产节点目录、DAG 执行器、React Flow 依赖和旧 Demo 节点入口。
- 已将工作区页面改为剧本、资产、导演分镜台和镜头生产区。
- 资产、分镜和生产接口按领域对象工作；图片、视频和本地任务仍由独立服务负责。
- `history.md` 已从上一版 Git 恢复，并在末尾追加本轮记录；后续只能追加，不删除旧记录。
- 后端图片/视频生成服务已保留历史记录、本地归档、当前版本指针和同目标进行中互斥。

## 本轮收口任务

- [x] 恢复 `history.md` 旧历史并追加本轮记录。
- [x] 补回本执行合同并写清多选、依赖和并发边界。
- [x] 把资产页收成以标准图为主角的制作间：浏览、锁定、升级、生成、参考图和历史在同一现场完成。
- [x] 扫描前端、后端和当前文档，确认旧画布入口、旧布局 API、节点主链和装饰性页面文案均已断开；计划中的禁止项文字只保留在本合同和历史记录中。
- [x] 运行前端构建、Lint、测试以及后端类型检查、测试和构建。
- [ ] 进行浏览器人工验收；真实供应商和部署不在本轮执行。本地提交已完成，推送因远端连接失败待网络恢复后执行。

## 验证记录

- 已验证：前端重构后构建通过。
- 已验证：后端类型检查通过。
- 已验证：后端测试 72/72 通过。
- 待验证：浏览器人工验收和真实供应商验收。

## 风险与不做事项

- 不恢复旧画布数据兼容，不清理或改写 `history.md` 既有历史。
- 不调用真实供应商，不执行部署或生产写入；本轮已按 owner 授权完成本地提交，推送因远端连接重置未完成。
- 当前未做真实浏览器截图和外部供应商验收；页面布局、真实媒体归档和模型能力仍需后续证据确认。
