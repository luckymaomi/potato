# 当前任务：总览与剧本首页、项目工作区及全局职责审查

## 需求

用户要求首页进入项目后的首屏就是“总览与剧本”，把核心钩子、世界观、主线、基调、本集目标、冲突、转折和结尾钩子等内容分清楚；同时重做项目工作区 UI，参考 `reference/` 中成熟项目的信息层级与阶段导航。用户已删除数据库，允许从空库直接替换 schema，不保留旧数据兼容层。

本轮追加全仓职责与语义审查：覆盖 `frontend/src`、`backend/src`、`backend/test`、`backend/scripts` 和根目录文档。审查按“变化原因”和真实调用链判断是否拆分；只有同时承担独立状态、业务规则、持久化、渲染或外部接线职责的文件才拆分，不按行数机械拆分。

产品边界仍遵循《AI 短剧工作流》与 `spec.md`：故事、剧本、镜头描述和资产资料由用户人工编辑；AI 只生成资产图、分镜图和镜头视频。任何 AI 写剧本、AI 拆镜、AI 提取资产、批量出图入口都不属于本轮，也不能因 UI 重做重新出现。

## 已确认事实

- 当前根路由 `/` 是项目列表；进入项目后默认跳转 `/film/:id/script`。
- `dramas` 只有一段 `description`，`episodes` 只有 `script_content` 和一段 `description`；总览与本集剧本目前是两张大文本卡，无法分别编辑故事钩子、世界观、主线、基调和本集叙事节拍。
- 项目壳已有三阶段：总览与剧本、资产图、分镜台；分镜台同时承载分镜图、镜头视频和整集合成。
- `workspaceRoutes` 是剧本读写接口 owner，`ProjectService` 是 dramas/episodes 持久化 owner，ZIP 归档和 Demo 初始化会读取完整项目对象，必须同步新字段。
- 工作区现有 CSS 同时包含旧的顶部项目布局和后置深色项目壳覆盖，页面层级与信息密度不稳定，是 UI 丑陋和移动端拥挤的主要证据。
- 工作区存在 4 个 owner 既有的生成计时相关未提交文件，本轮不覆盖、不回滚、不暂存。

## 参考调研结论

- `reference/moyin-creator`：剧集树、脚本输入和镜头属性按层分区，适合借鉴“当前集明确、内容分组、镜头编辑不混在故事字段里”；其 AI 生成、自动校准和变体树不适用。
- `reference/dramai`：项目卡、项目详情、素材/分镜/合成分段清楚，适合借鉴项目入口和阶段入口的视觉层级；其批量图片/视频和 AI 分镜生成不适用。
- `reference/huobao-drama`：项目列表、剧集页、资产页分开，适合借鉴项目—剧集关系和工具栏布局；其自动化生成链和供应商按钮不适用。
- `reference/open-ai-canvas`：故事、角色、分镜、生成按阶段表达，适合借鉴工作区导航和“当前对象”的稳定上下文；其 AI 故事生成、画布主路径不适用。
- `reference/ai-short-drama`、`open-ai-micro-drama` 等项目将文本 AI 作为主路径，和当前产品理念冲突，仅作边界反例。
- Kitty 仅用于观察“项目上下文和状态记录”边界，不复制其终端产品 UI；相邻仓库不存在本项目可直接复用的故事字段实现。

## 新的数据合同

`dramas` 增加结构化故事总览字段：`story_hook`（核心钩子）、`worldview`（世界观）、`storyline`（主线）、`tone`（基调）、`reference_setting`（参考设定）。

`episodes` 增加结构化本集叙事字段：`episode_goal`（本集目标）、`conflict`（主要冲突）、`turning_point`（转折）、`ending_hook`（结尾钩子）、`scene_notes`（场次与节拍）。

旧的 `description` 不再作为页面主输入；服务层只读写新字段，接口返回结构化对象。保留 `description` 列只用于空库初始化时兼容现有非剧本调用，新的剧本保存不会写回它。不存在正式生产数据，因此不设计迁移脚本。

前端领域类型新增 `StoryOverview` 和 `EpisodeStoryPlan`，剧本 API 从 `overview: string` 改为 `overview: StoryOverview`，保存接口一次提交故事总览和当前集叙事字段；`script_content` 仍保留为人工剧本文本。其他资产、分镜和生产 API 合同不变。

## 页面合同

- 根首页继续负责项目选择、创建、导入和示例入口；项目创建成功后直接进入 `/film/:id/script`。
- 项目首屏 `/film/:id/script` 只展示“总览与剧本”短标题、当前项目/当前集上下文、一个保存按钮和清晰分区：故事总览、本集结构、本集剧本。
- 每个字段使用普通输入框或文本域；不出现 AI 写作、润色、拆镜、提取资产按钮，不出现英文 kicker、宣传副标题或技术宣传术语。
- 页面采用单列主编辑区，字段分组卡片按阅读顺序堆叠；顶部显示保存状态和当前集选择，避免左右大卡高度错位。移动端保持单列，不产生横向溢出。
- 项目壳改为稳定的深色侧栏 + 浅色内容区：侧栏显示项目名称和四个阶段，内容头只保留面包屑、集数选择和集管理动作；不恢复旧顶部标签栏覆盖。
- 首页项目卡改为信息密度适中的网格：项目名、类型、更新时间、剧集数和“打开总览”动作；不增加营销文案。

## 实施任务

- [x] 调研 reference 项目并记录可借鉴边界
- [x] 调查当前前端入口、剧本接口、数据库、归档和 Demo 影响面
- [x] 重写本计划为本轮唯一任务合同
- [x] 以失败证据覆盖新字段读写、项目/剧集隔离和 ZIP 往返
- [x] 断裂重建 dramas/episodes 结构化字段、后端领域类型、ProjectService 和剧本路由
- [x] 同步前端类型、API、剧本页面和项目壳 UI；保持资产/分镜单列与显式组装不变
- [x] 同步 ZIP 归档、Demo、`spec.md`、`README.md`、`history.md`
- [x] 运行后端 test/typecheck/build 与前端 test/lint/typecheck/build；后端无 lint script
- [x] 静态扫描 AI 文本入口、批量入口、旧剧本字段主写入和装饰性文案
- [ ] 使用本地数据进行桌面与 390px 页面结构检查；不调用真实供应商（本轮未重复浏览器人工检查）
- [ ] 全仓职责审查：建立入口、路由、服务、repository、assembler、状态、UI、测试和文档职责地图
- [ ] 仅对审查确认跨越多个变化原因的文件实施职责拆分，保持行为和公共接口不变
- [ ] 为新增拆分补充或调整正向契约测试，并同步 `spec.md`、`README.md`、`history.md`
- [ ] 重新运行必要的前后端 test、lint、typecheck、build，并记录未验证项

## 验证计划

- 后端正向契约：新建项目返回空结构化总览和一集空结构；保存后读取字段稳定；不同项目/剧集互不串值；ZIP 导出导入保留结构化总览、本集字段和剧本文本。
- 前端正向契约：字段可编辑并提交；切换集数加载对应结构；保存状态可见；三阶段导航和项目动作可达；分镜台媒体双栏在桌面左右呈现、移动端单列堆叠且无横向溢出。
- 命令：后端 `npm.cmd test`、`npm.cmd run typecheck`、`npm.cmd run build`、`npm.cmd run lint`；前端 `npm.cmd test`、`npm.cmd run lint`、`npx.cmd tsc -b --pretty false`、`npm.cmd run build`。
- 静态检查：源码中不再以 `description` 作为剧本页面主输入，不新增 AI 文本生成/批量生成入口，不出现旧项目壳重复导航和装饰性英文文案。
- 未验证项单独报告：真实供应商、人工视觉审美和已有 owner 计时改动的来源。

## 收口

本计划只描述本轮总览与剧本首页及项目工作区重做。完成后在本节追加已验证事实、实际命令、未验证项和剩余风险；不复制其他任务的历史结论。

## 收口记录（2026-09-20）

- 已完成结构化故事总览、本集叙事字段、项目列表卡片、深色项目侧栏和单列“总览与剧本”页面；数据库为空库前提下直接采用新 schema，不保留兼容迁移。
- 后端 `npm.cmd test` 通过 65/65，`npm.cmd run typecheck` 和 `npm.cmd run build` 退出码 0；后端 `package.json` 没有 lint script，因此未执行后端 lint。
- 前端 `npm.cmd test` 通过 23/23，`npm.cmd run lint`、`npx.cmd tsc -b --pretty false`、`npm.cmd run build` 均退出码 0。lint 保留 5 条既有 `react(set-state-in-effect)` warning；build 保留 Vite 大包体 warning。
- 静态扫描未发现本轮 UI 中的 AI 文本生成入口、批量出图入口、装饰性英文文案或旧版本字段残留；文档中的边界说明属于事实记录。
- 未验证真实供应商调用、浏览器人工视觉审美、空库启动后的完整人工流程；owner 既有生成计时改动未覆盖。
- 本轮未执行 commit 或 push。

## 职责审查合同（2026-09-20）

- 前端页面文件可以负责页面状态编排和 JSX 组合，但不应同时成为跨页面复用的业务规则、API 组装器或媒体任务状态 owner。
- 后端路由只负责 HTTP 校验、调用和响应映射；领域规则、持久化、归档、供应商接线分别归服务、repository、archive 和 provider 模块。
- assembler 只负责一个稳定的文本/配方编译合同，不负责 HTTP、数据库写入或供应商调用。
- 测试按被保护的公共行为分组；共享测试工具和领域 fixture 不与单个服务实现混在一起。
- 文档分别承担当前事实（`spec.md`）、入口说明（`README.md`）、任务合同（`plan.md`）和经验记录（`history.md`），不重复维护互相冲突的事实。
