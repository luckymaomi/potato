# 当前任务：Agnes 真实 Demo 与动态供应商配置收口

## 需求

项目列表中的完整 Demo 必须直接走 Agnes，不保留虚拟成功逻辑；供应商和模型来自实时目录，密钥与默认模型统一读取根目录私有 `config.yaml`。真实短剧链路需要覆盖剧本、实体提取、参考图、分镜、分镜图、视频和整集合成，并把结果写回画布。

## 已确认事实

- 前后端分别为 `frontend/` 与 `backend/`，源码全部是严格 TypeScript；版本为 `0.0.1`。
- 根 `config.yaml` 被 Git 忽略，SQLite、前端状态和静态产物均不保存 API Key。
- Agnes 实时目录返回 12 个模型：文本 6、图片 3、视频 3；PearAPI 实时目录返回 80 个模型。
- Demo 模板 ID 为 `agnes-short-drama-demo`，v4 包含 11 个节点、13 条曲线连线和一个完整工作流组。
- 除手工梗概节点外，Demo 所有运行节点固定选择 Agnes；节点不写死模型，后端按根配置的可选默认模型或实时目录选择。
- Agnes 当前凭据可用 `agnes-video-2.5-flash`，而 `agnes-video-2.5` 实测提交返回 HTTP 403；因此根配置将 flash 设为视频默认模型。

## 失败证据与取舍

- 并发文本请求触发 Agnes HTTP 429；统一传输层保留有界退避，真实验收改为串行提交。
- 第一次道具图请求发生网络失败；同模型重试后成功，不把失败任务改写成成功。
- 第一次整集合成失败，因为 FFmpeg concat 默认拒绝 HTTPS；合成命令加入最小必需协议白名单，并新增 HTTP 视频回归测试。
- 浏览器 GUI 拖拽、框选和视觉质量仍需要人工验收；HTTP 200、单元测试或生成文件不能替代这部分判断。

## 目标

- Agnes Demo 可幂等创建或升级，不重复创建旧版本 Demo。
- 提取节点把首个实体 ID 传给角色、场景和道具节点；分镜图优先使用分镜 ID，不误用角色 ID。
- 模型目录动态发现；根配置可选 `default_models`，配置值必须存在于实时目录。
- 真实 Agnes 链路生成可读取的本地整集 MP4，所有结果回写 Demo 画布。
- 前后端完整测试、类型检查、构建、迁移、lint、依赖审计和样式禁令扫描通过。

## 不做范围

- 不新增供应商，不恢复旧 JavaScript/Vue 后端或兼容层。
- 不把 Mock、模板文字、页面状态或任务提交成功冒充真实媒体生成成功。
- 不在源码、文档、日志或回复中回显私有 API Key。
- 不执行 commit、push、部署或发布。

## 实施任务

- [x] 根配置接管供应商地址、密钥、设置和可选默认模型。
- [x] Agnes/PearAPI 实时模型发现与统一 Provider 注册中心。
- [x] Demo v4 固定 Agnes、移除假结果并支持旧 Agnes Demo 原位升级。
- [x] 修复实体 ID 和分镜 ID 的连线传播，补充前端回归测试。
- [x] 串行完成 Agnes 剧本、角色/场景/道具提取和分镜生成。
- [x] 完成 Agnes 文生图、图生图、文生视频和图生视频真实验收。
- [x] 修复远程 HTTPS 视频合成并完成真实整集合成。
- [x] 把真实任务状态、实体、媒体和整集结果写回 Demo 项目。
- [x] 清理前端 lint 诊断并完成全量自动验证。
- [x] 更新 `spec.md`、`README.md`、`history.md` 和本计划。

## 验证结果

- 后端：迁移、全局 strict typecheck、27/27 测试和 build 通过。
- 前端：11/11 测试、lint 0 诊断、build 和 moderate 依赖审计通过；审计为 0 漏洞。
- 样式扫描：`border-left`、`border-right`、伪元素色条、status rail 和 accent stripe 命中 0。
- 启动探针：后端 `/health` 返回 `0.0.1`；前端开发入口和 Vite API 代理返回 200。
- 真实 Agnes：文本、文生图、图生图、文生视频、图生视频均得到 completed；Demo 项目 ID 2、剧集 ID 3。
- 整集合成：输出本地 MP4，大小 4,546,747 字节；FFmpeg 解码检查退出码 0。
- 安全检查：`config.yaml` 被 Git 忽略；源码与文档未发现该实体配置中的密钥；源码目录没有 `.js/.jsx` 文件。

## 未验证项与风险

- 未做 PearAPI 真实付费生成，本次 owner 指定的是 Agnes Demo。
- 未完成浏览器 GUI 人工拖拽、框选和审美验收；仅完成前端启动与 HTTP 探针。
- 前端生产构建仍提示单个 JS 包超过 500 kB，属于性能风险，不影响当前功能验收。
- Agnes 存在限流和偶发网络失败，已支持明确失败、重试和再次运行，但不能保证第三方服务稳定性。

## 收口

- 已按 owner 明确授权创建初始提交并推送 `origin/master`；本地 `master` 跟踪远程同名分支。
- 未执行部署或发布。
- 前后端开发服务在最终交付前关闭。
