# 产品事实

## 主链

短剧项目的用户主链是：

`剧本 → 资产图 → 分镜台 → 分镜图 → 镜头视频 → 整集合成`

产品显示名为「番茄短剧」。项目和剧集是容器；首页按项目分组展示，分组下可折叠看到各集。故事总览和本集剧本是文本来源。人物、场景和道具由用户建立或从全局库加入，形成可复用资产图。导演分镜把一镜所需信息组合在一起；分镜图在分镜台直接生成，镜头视频与整集合成在生产区完成。

## 领域对象

- `dramas`：项目容器和故事总览。
- `episodes`：剧集容器、本集剧本和整集视频。
- `asset_library_items`：跨项目资产库，保存名称、图片描述、当前图片指针和历史。
- `project_assets`：项目中使用的资产，保存名称、图片描述、当前图片和自定义标签。
- `storyboards`：有序镜头规格以及当前分镜图、镜头视频指针。
- `image_generations` / `video_generations`：每一次生成的独立历史。
- `tasks`：本地任务状态和供应商任务轮询状态。

资产图工作台统一管理人物、场景和道具。类型是筛选维度，每项资产还可以维护自定义标签。标准图可由 AI 文生图/图生图生成，也可本地上传；当前图片通过生成历史里的「选用这张」切换；没有锁定标准图、版本升级或资产依赖语义。故事、剧本和角色/场景/道具信息由用户人工填写。

项目下可新建、重命名、删除剧集（首页分组与项目壳均可；至少保留一集）；每集独立剧本与分镜。

资产本体与生成过程分开。提示词、参考图、供应商、模型和本次参数属于 generation；资产沉淀的是名称、当前图片、对图片的描述和自定义标签。

## 分镜台

每个镜头都可以组合以下区域，全部可选：剧情说明、景别、机位、运镜、构图、动作、对白、光线、氛围、声音、人物/场景/道具资产托盘、图像提示词、负面提示词和视频提示词。

一个镜头只产生一张分镜图，并在分镜台直接提交。后端 `storyboardPromptAssembler` 是唯一镜头规格编译器：按稳定语义顺序组装镜头语言、资产文本和用户提示词，并把资产图作为独立参考图数组；图片 prompt 可表达九宫格，视频 prompt 必须表达单一连续画面并禁止九宫格/分屏/网格/拼图/水印。分镜图可来自 AI 生成，也可本地上传；可清除当前分镜图指针（历史记录保留）。画幅选项来自当前图片模型的 `capabilities.aspectRatios`，模型有则可选，没有就不出现。分镜预览框为正方形，按舞台剩余空间 flex 自适应放大（不写死宽高）；1:1 图铺满，9:16 / 16:9 等比缩放入框。分镜图和镜头视频是两个独立结果，分镜图完成不会自动提交视频。

## 生成与并发

AI 只用于资产图、分镜图和镜头视频媒体生成；故事、剧本、角色/场景/道具提取和分镜清单均由用户人工编辑。

供应商图片和视频都异步提交，取得供应商任务号后轮询到明确成功、明确失败或用户取消。本地生成接口立即返回本地任务 ID。

每个进行中的生图/生视频任务单独显示状态与已用秒数，计时起点取后端持久化的 generation/task 创建时间；刷新页面后仍可根据 `pending/processing` 记录恢复显示并继续轮询。有供应商真实百分比才显示百分比，否则只显示阶段与已用秒数。用户可停止单项生成任务。

没有前置任务的图片可以并发提交；分镜图会先等待本镜引用的资产图完成；视频同时最多一个。同一资产或同一镜头同时只允许一个进行中的 generation。

整集合成按分镜顺序收集视频，缺视频就明确失败，不生成不完整成片。生产区只提供单镜视频与整集合成；出图在分镜台完成。生产页展示当前图片/视频模型预设及其目录声明的画幅与时长能力；每个镜头各自选择视频时长与视频画幅（互不统一），选项只来自当前视频模型的 `supportedDurations` / `aspectRatios`，不注入通用比例表。分镜台的画幅选择只来自当前图片模型目录，并明确标注为「图片画幅」。

## 接口入口

前端路由：

- `/`
- `/ai-config`
- `/film/:id/script`
- `/film/:id/assets`
- `/film/:id/assets/library`
- `/film/:id/storyboard`
- `/film/:id/produce`

后端工作区接口集中在 `backend/src/routes/workspaceRoutes.ts`。供应商适配器、任务、媒体归档和项目 ZIP 是独立基础设施；对象之间的关系通过项目资产、分镜托盘和有序镜头保存。AI 配置页默认模型预设在选择后自动保存。

## 验收

- 空数据库可创建项目并进入剧本；首页可按项目折叠查看各集，并可新建、重命名、删除集（至少保留一集）。
- 项目壳四个制作阶段可以互相切换，左侧底栏返回项目列表；顶栏与侧栏为深色壳；当前剧集可切换、重命名、删除与新建。
- 资产图可按人物、场景、道具和自定义标签筛选；可上传标准图或 AI 逐张生成（可停止）；标准图、生成历史和「选用这张」行为可见；无「从剧本提取」、无批量出图。
- 分镜台可通过资产缩略图多选本镜人物、场景和道具；镜头可加入、保存、删除（删除后重排镜号）；分镜图可上传、AI 生成或清除当前图；预览框为正方形。
- 生成中的图/视频显示「已生成 X 秒」，刷新后不丢，可停止。
- 生产页支持单镜「生成视频」与整集合成；无批量出图/出视频，无「去分镜台」干扰按钮。
- 生产页注明当前分镜图/视频模型预设与各自画幅能力；每个镜头单独选视频时长与视频画幅（来自视频模型目录）；分镜台注明图片模型与「图片画幅」。
- 前端不伪造通用画幅列表；能力未知时最多保留当前已选值。
- 前端 build、lint 和测试通过；后端 typecheck、build 和测试通过。
