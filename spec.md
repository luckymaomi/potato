# 产品事实

## 主链与边界

土豆漫画的当前生产主链是：

`人工故事/剧本 → 项目资产卡与标准资产图 → 分镜台组装图片配方 → 分镜底板`

- 项目是最外层容器；话是项目内的生产单元。
- 项目资产库只属于一个项目，同项目各话按资产 ID 复用，不跨项目引用。
- 故事、剧本、资产卡文本和分镜规格由用户人工编辑。产品没有 AI 写剧本、AI 提取资产或 AI 自动拆镜入口。
- AI 只用于资产标准图和分镜底板图。AI 配置、实时模型目录和 Provider 合同只公开当前图片能力，不提供独立文本生成服务，也不提供配音 / TTS。真实供应商能力以实时模型目录和适配器合同为准。
- 产品不包含页组装、字层气泡、拼页导出、话数包或配音配置。

## 数据模型

- `dramas`：项目、故事总览和项目设置。
- `episodes`：话、本话剧本和漫画工作区上下文。
- `project_assets`：唯一资产表，类型只取 `character | scene | prop`。
- `panels`：本话有序分镜规格、显式组装并可编辑的图片配方、资产引用、额外参考图、当前底板指针、图片历史；内部仍可保留配方过期标记供生成自动重装。
- `panel_project_assets`：分镜与本项目资产卡的 ID 关系。
- `image_generations`：每次图片生成或本地导入的历史和本地归档信息。
- `async_tasks`：本地异步任务状态。

故事源由项目级画风锁与本话剧本文本组成。产品页面「总览与剧本」只编辑：`tone`（基调）、`reference_setting`（参考设定）和 `script_content`（故事与剧本合一正文）。画风锁与剧本文本给人读与写作，**不**自动注入分镜图片配方。不再在页面上维护核心钩子/世界观/主线分栏，也不维护本话目标/冲突/转折等结构分栏与场次组装；schema 仍可保留旧列，空库下保存时写为空串。故事源与分镜台彻底分离：不建镜头列表，不与分镜一对一挂钩，不自动建镜。总览与剧本、资产卡、分镜规格在编辑停顿约 2 秒后防抖自动保存；不再依赖手动「保存」按钮，切换对象或离开前会先 flush。Ctrl/Cmd+S 可立即落盘。

数据库按当前 schema 从空库直接创建，不读取旧资产表或旧字段。项目资产卡保存：

- `kind`、`name`。
- 按类型校验的 `text_profile`，当前唯一键为 `brief`：一段合并的视觉描述（placeholder 提示可写年龄/性别/外形/服装/表情，或场景地点光影，或道具外形细节状态）；不再拆多栏。
- 按资产类型校验的 `output_type`，作为用户显式组装时选择的产出预设。
- 独立保存的 `output_prompt`，即用户可见、可编辑的完整资产生成提示词。
- 只供标准资产图生成使用的 `input_reference_images`。
- 当前标准资产图、本地路径和当前 generation ID。

资产详情可「复制给其他 AI」：剪贴板含卡类型、资产名称、需覆盖项、分条「标签：内容」输出样例与当前 `brief` 草稿，便于粘贴到外部模型写好后再贴回。也可单独复制最终 `output_prompt`。所有非枚举资料字段都是普通文本输入并保存为字符串；字符串会去除首尾空白，空字段不持久化到结构化文本中。参考图数组会去空和去重。

每张资产卡只表达一个造型、一种场景光影设定或一种道具状态，并拥有独立 ID。场景光影变体和道具状态变体分别建立独立资产卡。

标准资产图是资产卡产出物。用户上传的资产参考图是标准资产图的生成输入，不会作为分镜底板图继续传播。标准图可由 AI 生成或本地上传；generation 历史只用于选定当前标准图，不形成资产版本树。

## 资产产出层

资产详情提供独立的“产出层提示词”区域。角色卡可选择 A-D 布局，场景卡可选择空间全景、局部特写或光影变体，道具卡可选择多角度或状态变体；另有「图片参考锁定」下拉（角色：锁脸；场景：锁景；道具：锁物）与共用「画面禁字」下拉（禁止出字），均可清空。下拉只选择选项，不自动覆盖文本。只有用户点击“组装提示词”时，前端才把当前卡片规格交给后端，后端返回完整 `output_prompt` 并填入可见、可编辑文本域。组装顺序为：若已选图片参考锁定则先写入对应锁定文案（含「以我上传的参考图为唯一…锚点，img2img 图生图」与一致性）→ 若已选画面禁字则写入禁止出字段 → 卡面文本 → 布局/产出预设说明；未选对应项则跳过该段。用户可以继续修改、完全重写，也可以不点击按钮直接填写。修改资料、切换下拉、切换资产和刷新媒体状态都不会暗中重组或覆盖文本，切换资产时保留当前页面内各卡草稿。锁定与禁字选项仅用于组装请求与前端草稿，不单独持久化；生成仍只读已保存的 `output_prompt`。

`backend/src/services/assetOutputPromptAssembler.ts` 是资产产出模板的唯一 owner，只由显式组装接口调用。新建卡只保存默认 `output_type`，`output_prompt` 为空，等待用户组装或直接填写。支持的预设为：

- 角色卡：布局 A 三栏三视图、布局 B 左脸右身、布局 C 4+3 双层、布局 D 7 图锚点组。
- 场景卡：空间全景图、局部特写图、光影变体卡。
- 道具卡：多角度图、状态变体卡。

图片参考锁定、画面禁字与预设模板同为显式下拉：不选则组装不含对应段；选中锁脸/锁景/锁物或禁止出字则写入对应整段。这是可编辑提示词模板，不是独立锁脸模型。资料变化后需要用户再次点击组装或人工同步文本。光影变体卡和状态变体卡本身就是独立项目资产 ID，各自只表达一种设定。

单卡“保存并生成标准图”先校验并保存当前表单。资产生成入口只接收供应商、模型和画幅参数；后端原样读取该卡 `output_prompt`，并把 `input_reference_images` 作为独立图片参数。提示词要求非空，保留用户的换行及首尾空白；生成时不追加隐藏模板或重新拼接资产资料。生成成功或人工上传后，选定结果写入资产卡 `image_url`；分镜台只引用该标准图。

## 分镜台

分镜台与剧本分离：分镜由用户在分镜台新建与排序。规格只保留标题、本镜画面（写入 `action`，可含场景/站位/景别/氛围）、本项目出场资产 ID、本镜其他参考图。不再提供取景/视角/构图/表情/光线/氛围六栏，也不自动带入上一镜底板；需要连续性时自行把上一镜上传为其他参考图。旧列 `description` / `image_prompt` 仅作组装回退。对白、旁白、配音和页拼版不在当前产品范围。

`backend/src/services/storyboardPromptAssembler.ts` 是图片配方 owner。用户可点「组装图片配方」预览并保存；点「生成底板」时，若规格已变或尚无配方，后端会按当前分镜规格自动组装后再提交生成。已保存且未过期的人工改写配方，在规格未变时仍直接用于生成。组装**不**读取总览画风锁、资产卡 `text_profile` 或资产 `output_prompt`。

```ts
{
  imageRecipe: {
    imagePrompt: string,
    imageReferences: string[],
  },
}
```

图片提示词顺序：本镜画面（空时回退 description / image_prompt / 标题）；有值才输出，并附带干净画面、无字幕、无气泡、无水印约束。身份与造型一致性靠出场资产标准图（参考图），不靠把资产说明书抄进提示词。

参考图数组：勾选资产当前标准图 + 本镜其他参考图。资产卡的生成输入参考图不进入分镜配方。组装结果填入可见、可编辑的最终提示词，并以缩略图展示配方参考图（可从快照删除）。修改分镜规格、资产选择或其他参考图会使已保存配方过期；下次点「生成底板」或「组装图片配方」会按最新规格重装。产品页面不再展示「待复核 / 待重装」状态机，也不要求用户确认复核。

## 生成消费

- 资产标准图入口只接收供应商、模型和画幅选项；后端直接消费资产卡已保存的 `output_prompt` 和该卡输入参考图。
- 分镜图片入口接收供应商、模型、图片画幅，以及当前分镜规格；后端在配方过期或为空时按最新规格自动组装，再消费得到的 `image_recipe_prompt` 与 `image_recipe_references`。规格未变且已有人工保存配方时，直接消费该配方。
- 前端可先用显式组装接口预览配方；生成请求携带当前规格，不在前端拼接提示词规则。
- 资产页和分镜台只投影当前图片预设的目录能力摘要；摘要包含模式、参考图上限、画幅和能力来源。没有唯一预设时必须显示未选择并阻止生成，不能自动选模或合并多个模型能力。
- 资产页与分镜台在缺少手选图片画幅时禁用生成入口并显示原因；切镜保留仍合法的手选画幅，仅清除已不在新模型能力内的值。
- 资产页生成请求会传递用户选择的图片画幅；没有目录画幅时显示未知并交由后端拒绝或供应商验证，不注入通用比例表。适配器覆盖目录字段必须标记为 `adapter-override`。
- AI 配置刷新模型目录成功后按返回结果报告当前图片模型数量，不得用全量合计冒充当前列表数。
- 前端样式唯一入口为 `frontend/src/theme/app.css`；剧本/资产/分镜工作区必须自带可滚动子区域，外层 `project-workspace-body` 可 `overflow: hidden`。

供应商图片异步提交，本地接口立即返回任务 ID。任务轮询到明确成功、失败或用户取消；有真实百分比才显示百分比，否则显示阶段和已用时间。供应商返回图片地址后，generation 先进入 `remote`：业务指针与界面主图指向该远程 URL（无魔法：远程就是远程）。后台再有界重试本地归档（下载超时约 45s、最多 5 次）；归档成功后 status 变为 `completed`，指针切到 `/static/...`。`available` 仅表示本地文件真实存在。服务启动后持续轮询待归档记录。服务重启或任务已终态但 `image_generations` 仍停在 pending/processing 时，列表与再次提交前会回收这些孤儿记录，避免「已有进行中任务」死锁；分镜台与资产台在进行中均可停止生成。

图片任务可并发；同一项目资产或同一分镜同时只允许一项进行中的媒体生成。资产图与分镜图均由用户单次提交，页面没有批量出图入口。

每张资产缩略图和详情标准图显示文字状态：正在提交、排队中、正在生成、正在保存到本地、远程预览·待归档、远程预览·本地保存失败、已完成、失败或已停止。任务按秒轮询，阶段取真实后端消息；短于轮询间隔的阶段可能直接跳到下一状态。页面重开时从历史恢复最近结果和进行中任务，失败原因可在提示及历史中查看；有旧标准图时，重生成中的任务状态仍显示在旧图上。

上游资产变更或分镜规格变更会使已保存配方过期；点生成时自动按最新规格重装。产品不要求用户确认底板复核。

分镜图片生成在后端要求最终可用的图片配方（自动组装或已保存）、至少一项出场资产，以及每个出场角色/场景已有标准图。图片画幅只取当前模型目录声明的能力。

## 页面与接口

前端路由：

- `/`
- `/ai-config`
- `/comic/:id/script`
- `/comic/:id/assets`
- `/comic/:id/panels`

项目工作区固定为总览与剧本、资产图、分镜台三个区位。总览与剧本只维护画风锁与故事/剧本文本；分镜台独立生产并归档单镜底板。话可新建、重命名和删除，但项目至少保留一话。

后端工作区业务接口集中在 `backend/src/routes/workspaceRoutes.ts`。供应商适配器、任务、媒体归档和 AI 配置分别由独立模块负责。没有通用生产命令入口，也没有页组装 / TTS 路由。

## 验收合同

- 空数据库可创建项目；项目之间资产隔离，同项目不同话可复用同一资产 ID。
- 三类资产卡的结构化文本、独立产出提示词、产出预设类型和标准图生成输入参考图可保存、更新和读取。
- 新卡提示词为空；选择预设不会改写文本，点击组装后后端返回可见模板；用户编辑并保存的完整提示词是资产生成唯一文本输入。
- 资产缩略图随任务阶段更新文字状态；切卡与媒体刷新保留当前页面内的编辑草稿。
- 分镜只能保存当前项目资产 ID，并可保存本镜额外参考图。
- 分镜台可显式组装图片配方供预览编辑；点生成时若配方过期或为空则按最新规格自动组装。
- 分镜图片消费自动组装或已保存的图片配方；规格未变时保留人工改写的配方。
- 生成入口不再因「待重装」拒绝；缺模型或画幅时前端禁用并说明原因。
- 标准资产图、分镜底板及其 generation 历史可选择当前使用结果。
- 确定性自动验证包括后端 test/typecheck/build 和前端 test/lint/build；真实供应商验收与人工视觉验收单独报告。
