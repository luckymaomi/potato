# 产品事实

## 主链与边界

番茄短剧的当前生产主链是：

`人工故事/剧本 → 项目资产卡与标准资产图 → 分镜台编译图片/视频配方 → 分镜图 → 镜头视频 → 整集合成`

- 项目是最外层容器；剧集是项目内的生产单元。
- 项目资产库只属于一个项目，同项目各集按资产 ID 复用，不跨项目引用。
- 故事、剧本、资产卡文本和分镜规格由用户人工编辑。产品没有 AI 写剧本、AI 提取资产或 AI 拆镜入口。
- AI 只用于资产标准图、分镜图和镜头视频。真实供应商能力以实时模型目录和适配器合同为准。

## 数据模型

- `dramas`：项目、故事总览和项目设置。
- `episodes`：剧集、本集剧本和整集成片指针。
- `project_assets`：唯一资产表，类型只取 `character | scene | prop`。
- `storyboards`：本集有序镜头规格、额外参考图和当前图片/视频指针。
- `storyboard_project_assets`：分镜与本项目资产卡的 ID 关系。
- `image_generations` / `video_generations`：每次媒体生成或本地导入的历史和本地归档信息。
- `async_tasks`：本地异步任务状态。

数据库按当前 schema 从空库直接创建，不读取旧资产表或旧字段。项目资产卡保存：

- `kind`、`name`。
- 按类型校验的 `text_profile`。
- 只供标准资产图生成使用的 `input_reference_images`。
- 当前标准资产图、本地路径和当前 generation ID。

角色卡文本覆盖身份、物理、服装、神态与声音；场景卡文本覆盖空间、光影、陈设与氛围；道具卡文本覆盖物理、细节与状态。字符串会去除首尾空白，数组会去空和去重，空字段不持久化到结构化文本中。

标准资产图是资产卡产出物。用户上传的资产参考图是标准资产图的生成输入，不会作为分镜资产图继续传播。标准图可由 AI 生成或本地上传；generation 历史可切换当前版本。

## 分镜台

分镜规格包括标题、剧情说明、图像/视频主干提示词、景别、机位、运镜、构图、动作、对白、光线、氛围、声音、本项目资产 ID 和本镜额外参考图。

`backend/src/services/storyboardPromptAssembler.ts` 是唯一配方 owner。它读取一次已持久化分镜规格与已验证的本项目资产卡，输出：

```ts
{
  imageRecipe: {
    imagePrompt: string,
    imageReferences: string[],
  },
  videoRecipe: {
    videoPrompt: string,
    videoReferences: string[],
  },
}
```

图片提示词顺序：图片主干（空时回退剧情/标题）、资产卡文本、景别、机位、构图、动作、光线、氛围。

视频提示词顺序：视频主干（空时回退剧情/标题）、资产卡文本、景别、机位、运镜、构图、动作、光线、氛围、声音、对白。

所有文本块有值才输出。资产卡文本进入 prompt；所选资产卡当前标准图和本镜额外参考图进入独立 references 数组。资产卡的生成输入参考图不进入分镜配方。

## 生成消费

- 资产标准图入口只接收供应商、模型和画幅选项；后端从资产卡编译文本并读取该卡输入参考图。
- 分镜图片入口只接收供应商、模型和图片画幅选项；图片服务消费 `imageRecipe`。
- 分镜视频入口只接收供应商、模型、时长和视频画幅选项；视频服务消费 `videoRecipe`，并把当前已选分镜图作为独立首帧。
- 前端不提交最终分镜 prompt 或 reference 数组，也不复制后端拼接规则。
- 视频辅助参考图按模型真实参考图上限校验；不支持时明确失败，不静默丢弃或伪造能力。

供应商图片和视频异步提交，本地接口立即返回任务 ID。任务轮询到明确成功、失败或用户取消；有真实百分比才显示百分比，否则显示阶段和已用时间。生成媒体成功后必须归档到本地，业务对象当前指针只指向已完成且本地可用的 generation。

图片任务可并发；同一项目资产或同一分镜同时只允许一项进行中的媒体生成。视频任务统一串行。资产图、分镜图和镜头视频的“生成未完成”入口按顺序逐项处理，可停止当前任务并终止后续提交。

整集合成按分镜顺序消费本集镜头视频；缺少任一镜头视频时明确失败。图片画幅、视频画幅和视频时长只取当前模型目录声明的能力。

## 页面与接口

前端路由：

- `/`
- `/ai-config`
- `/film/:id/script`
- `/film/:id/assets`
- `/film/:id/storyboard`
- `/film/:id/produce`

项目工作区只有总览与剧本、项目资产库、分镜台和生产四个阶段。资产库页面只显示当前项目的角色卡、场景卡和道具卡。剧集可新建、重命名和删除，但项目至少保留一集。

后端工作区业务接口集中在 `backend/src/routes/workspaceRoutes.ts`。供应商适配器、任务、媒体归档、AI 配置和项目 ZIP 分别由独立模块负责。没有通用生产命令入口。

项目 ZIP 当前格式为 `format: 4`，保存项目、剧集、项目资产卡、分镜、generation 历史以及本地媒体和本地参考图。导入会重建 ID 与本地路径；不读取旧 ZIP 格式。

## 验收合同

- 空数据库可创建项目；项目之间资产隔离，同项目不同剧集可复用同一资产 ID。
- 三类资产卡的结构化文本和标准图生成输入参考图可保存、更新和读取。
- 分镜只能保存当前项目资产 ID，并可保存本镜额外参考图。
- 分镜台一次编译两份独立配方；文本顺序稳定，空字段跳过，图片保持独立数组。
- 分镜图片只消费图片配方；分镜视频只消费视频配方和独立首帧。
- 标准资产图、分镜图、镜头视频及其 generation 历史可选择当前版本。
- 项目 ZIP 往返后，资产规格、分镜关系、参考图、本地媒体和当前版本仍能对应。
- 确定性自动验证包括后端 test/typecheck/build 和前端 test/lint/build；真实供应商验收与人工视觉验收单独报告。
