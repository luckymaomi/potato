# 产品事实

## 主链

短剧项目的用户主链是：

`剧本 → 资产图 → 分镜台 → 分镜图 → 镜头视频 → 整集合成`

项目和剧集是容器。故事总览和本集剧本是文本来源。人物、场景和道具从文本提取或由用户建立，形成可复用资产。导演分镜把一镜所需信息组合在一起，生产区再按镜头执行媒体任务。

## 领域对象

- `dramas`：项目容器和故事总览。
- `episodes`：剧集容器、本集剧本和整集视频。
- `asset_library_items`：跨项目资产库，保存名称、图片描述、当前图片指针和历史。
- `project_assets`：项目中使用的资产，保存名称、图片描述、当前图片和自定义标签。
- `storyboards`：有序镜头规格以及当前分镜图、镜头视频指针。
- `image_generations` / `video_generations`：每一次生成的独立历史。
- `tasks`：本地任务状态和供应商任务轮询状态。

资产图工作台统一管理人物、场景和道具。类型是固有维度，每项资产还可以维护零个或多个自定义分类标签，并按类型和标签筛选。全局资产库是资产图工作台的子视图。

资产本体与生成过程分开。提示词、参考图、供应商、模型和本次参数属于 generation；资产沉淀的是名称、当前图片、对图片的描述和自定义标签。

## 分镜台

每个镜头都可以组合以下区域，全部可选：剧情说明、景别、机位、运镜、构图、动作、对白、光线、氛围、声音、时长、人物/场景/道具资产托盘、图像提示词、负面提示词、视频提示词和网格提示。

一个镜头只产生一张分镜图。网格参数仅影响这一张图的提示词构图，不切图、不增加镜头。分镜图和镜头视频是两个独立结果，分镜图完成不会自动提交视频。

## 生成与并发

供应商图片和视频都异步提交，取得供应商任务号后轮询到明确成功、明确失败或用户取消。本地生成接口立即返回本地任务 ID，前端轮询任务状态。

没有前置任务的图片可以并发提交，不设置额外的图片数量上限；分镜图会先等待本镜引用的资产图完成；视频同时最多一个。同一资产或同一镜头同时只允许一个进行中的 generation。批量中的一项失败只标记该项，其他项目继续。429 和暂时性错误只退避重试当前项目，首次提交不盲目重发。

分镜图必须拿到资产托盘中已完成的当前图片才提交。整集合成按分镜顺序收集视频，缺视频就明确失败，不生成不完整成片。

## 接口入口

前端路由：

- `/`
- `/ai-config`
- `/film/:id/script`
- `/film/:id/assets`
- `/film/:id/assets/library`
- `/film/:id/storyboard`
- `/film/:id/produce`

后端工作区接口集中在 `backend/src/routes/workspaceRoutes.ts`。供应商适配器、任务、媒体归档和项目 ZIP 是独立基础设施；对象之间的关系通过项目资产、分镜托盘和有序镜头保存。

## 验收

- 空数据库可创建项目并进入剧本。
- 项目壳四个制作阶段可以互相切换，当前剧集明确可见。
- 资产图可按人物、场景、道具和自定义标签筛选；标准图、生成历史和“选用这张”行为可见。
- 分镜台通过资产缩略图直接选择本镜人物、场景和道具。
- 分镜台可以逐镜保存完整但可空的规格。
- 生产页可以单镜或批量出图，视频需要明确确认，整集合成拒绝缺镜头视频。
- 前端 build、lint 和测试通过；后端 typecheck、build 和测试通过。
