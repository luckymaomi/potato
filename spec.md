# 产品事实

## 主链与边界

土豆短剧的当前生产主链是：

`人工故事/剧本 → 项目资产卡与标准资产图 → 分镜台编译图片/视频配方 → 分镜图 → 镜头视频 → 整集合成`

- 项目是最外层容器；剧集是项目内的生产单元。
- 项目资产库只属于一个项目，同项目各集按资产 ID 复用，不跨项目引用。
- 故事、剧本、资产卡文本和分镜规格由用户人工编辑。产品没有 AI 写剧本、AI 提取资产或 AI 拆镜入口。
- AI 只用于资产标准图、分镜图和镜头视频。AI 配置、实时模型目录和 Provider 合同只公开图片与视频服务，不提供独立文本生成服务。真实供应商能力以实时模型目录和适配器合同为准。

## 数据模型

- `dramas`：项目、故事总览和项目设置。
- `episodes`：剧集、本集剧本和整集成片指针。
- `project_assets`：唯一资产表，类型只取 `character | scene | prop`。
- `storyboards`：本集有序镜头规格、显式组装并可编辑的图片/视频配方、两组配方引用快照、额外参考图、当前图片/视频指针，以及图片待复核、视频待复核和配方待重装状态。
- `storyboard_project_assets`：分镜与本项目资产卡的 ID 关系。
- `image_generations` / `video_generations`：每次媒体生成或本地导入的历史和本地归档信息。
- `async_tasks`：本地异步任务状态。

故事总览由项目字段分别保存：`story_hook`（核心钩子）、`worldview`（世界观）、`storyline`（主线）、`tone`（基调）和 `reference_setting`（参考设定）。本集结构由剧集字段分别保存：`episode_goal`（本集目标）、`conflict`（主要冲突）、`turning_point`（转折）、`ending_hook`（结尾钩子）和 `scene_notes`（场次与节拍）。`script_content` 仍是用户人工编辑的本集剧本文本。总览与本集结构在项目、剧集之间独立保存，读取和归档时保持各自对应关系。

数据库按当前 schema 从空库直接创建，不读取旧资产表或旧字段。项目资产卡保存：

- `kind`、`name`。
- 按类型校验的 `text_profile`。
- 按资产类型校验的 `output_type`，作为用户显式组装时选择的产出预设。
- 独立保存的 `output_prompt`，即用户可见、可编辑的完整资产生成提示词。
- 只供标准资产图生成使用的 `input_reference_images`。
- 当前标准资产图、本地路径和当前 generation ID。

角色卡文本覆盖身份、物理、服装、神态与声音，但不设“身份标签”；场景卡文本覆盖空间、光影、陈设与氛围；道具卡文本覆盖物理、细节与状态。所有非枚举资料字段都是普通文本输入并保存为字符串；字符串会去除首尾空白，空字段不持久化到结构化文本中。参考图数组会去空和去重。

每张资产卡只表达一个造型、一种场景光影设定或一种道具状态，并拥有独立 ID。角色卡只用 `default_outfit` 描述本卡造型；场景光影变体和道具状态变体分别建立独立资产卡。

标准资产图是资产卡产出物。用户上传的资产参考图是标准资产图的生成输入，不会作为分镜资产图继续传播。标准图可由 AI 生成或本地上传；generation 历史只用于选定当前标准图，不形成资产版本树。

## 资产产出层

资产详情提供独立的“产出层提示词”区域。角色卡可选择 A-D 布局，场景卡可选择空间全景、局部特写或光影变体，道具卡可选择多角度或状态变体；下拉只选择预设，不自动覆盖文本。只有用户点击“组装提示词”时，前端才把当前卡片规格交给后端，后端返回完整 `output_prompt` 并填入可见、可编辑文本域。用户可以继续修改、完全重写，也可以不点击按钮直接填写。修改资料、切换预设、切换资产和刷新媒体状态都不会暗中重组或覆盖文本，切换资产时保留当前页面内各卡草稿。

`backend/src/services/assetOutputPromptAssembler.ts` 是资产产出模板的唯一 owner，只由显式组装接口调用。新建卡只保存默认 `output_type`，`output_prompt` 为空，等待用户组装或直接填写。支持的预设为：

- 角色卡：布局 A 三栏三视图、布局 B 左脸右身、布局 C 4+3 双层、布局 D 7 图锚点组。
- 场景卡：空间全景图、局部特写图、光影变体卡。
- 道具卡：多角度图、状态变体卡。

四种角色模板都显式约束同一张脸、同一发型、同一服装，身高比例和五官完全一致，并使用纯色或中性背景。场景和道具模板也把各自的一致性标准写进结果，用户能看到并修改。资料变化后需要用户再次点击组装或人工同步文本。光影变体卡和状态变体卡本身就是独立项目资产 ID，各自只表达一种设定。

单卡“保存并生成标准图”先校验并保存当前表单。资产生成入口只接收供应商、模型和画幅参数；后端原样读取该卡 `output_prompt`，并把 `input_reference_images` 作为独立图片参数。提示词要求非空，保留用户的换行及首尾空白；生成时不追加隐藏模板或重新拼接资产资料。生成成功或人工上传后，选定结果写入资产卡 `image_url`；分镜台只引用该标准图。

## 分镜台

分镜规格包括标题、剧情说明、图像/视频主干提示词、景别、机位、运镜、构图、动作、对白、光线、氛围、声音、本项目资产 ID 和本镜额外参考图。

`backend/src/services/storyboardPromptAssembler.ts` 是唯一配方 owner。只有用户点击“组装两份配方”时，显式接口才读取前端当前镜头规格与已验证的本项目资产卡，一次输出：

```ts
{
  imageRecipe: {
    imagePrompt: string,
    imageReferences: string[],
  },
  videoRecipe: {
    videoPrompt: string,
    videoReferences: string[],
  },
}
```

图片提示词顺序：图片主干（空时回退剧情/标题）、资产卡文本、景别、机位、构图、动作、光线、氛围。

视频提示词顺序：视频主干（空时回退剧情/标题）、资产卡文本、景别、机位、运镜、构图、动作、光线、氛围、声音、对白。

所有文本块有值才输出。资产卡文本进入 prompt；所选资产卡当前标准图和本镜额外参考图进入独立 references 数组。资产卡的生成输入参考图不进入分镜配方。组装结果分别填入可见、可编辑的图片最终提示词和视频最终提示词，并显示各自引用；用户可继续修改或重写。保存时写入 `image_recipe_prompt`、`video_recipe_prompt`、`image_recipe_references` 和 `video_recipe_references`。修改镜头规格、资产选择或额外参考图不会自动覆盖已组装结果，而是将本镜标记为配方待重装，并将已有媒体标为待复核；必须再次显式组装并保存后才可清除配方待重装。

## 生成消费

- 资产标准图入口只接收供应商、模型和画幅选项；后端直接消费资产卡已保存的 `output_prompt` 和该卡输入参考图。
- 分镜图片入口只接收供应商、模型和图片画幅选项；后端消费已保存的 `image_recipe_prompt` 与 `image_recipe_references`，提交前不再组装。
- 分镜视频入口只接收供应商、模型、时长和视频画幅选项；后端消费已保存的 `video_recipe_prompt` 与 `video_recipe_references`，并把当前已选分镜图作为独立首帧，提交前不再组装。
- 前端只在显式组装接口中传当前规格，不复制后端拼接规则；媒体生成请求不提交 prompt 或 reference 数组。
- 视频辅助参考图按模型真实参考图上限校验；不支持时明确失败，不静默丢弃或伪造能力。
- 资产页和分镜台只投影当前图片/视频预设的目录能力摘要；摘要包含模式、参考图上限、画幅、视频时长/计费和能力来源。没有唯一预设时必须显示未选择并阻止生成，不能自动选模或合并多个模型能力。
- 资产页与分镜台在缺少手选图片画幅、视频画幅或（目录声明档位时的）视频时长时禁用对应生成入口并显示原因；切镜保留仍合法的手选画幅，仅清除已不在新模型能力内的值。
- 资产页生成请求会传递用户选择的图片画幅；没有目录画幅时显示未知并交由后端拒绝或供应商验证，不注入通用比例表。适配器覆盖目录字段必须标记为 `adapter-override`。
- AI 配置刷新模型目录成功后按返回结果报「图片 X / 视频 Y」，不得用全量合计冒充当前 Tab 列表数。
- 前端样式唯一入口为 `frontend/src/theme/app.css`；剧本/资产/分镜工作区必须自带可滚动子区域，外层 `project-workspace-body` 可 `overflow: hidden`。

供应商图片和视频异步提交，本地接口立即返回任务 ID。任务轮询到明确成功、失败或用户取消；有真实百分比才显示百分比，否则显示阶段和已用时间。生成媒体成功后必须归档到本地，业务对象当前指针只指向已完成且本地可用的 generation。

图片任务可并发；同一项目资产或同一分镜同时只允许一项进行中的媒体生成。资产图与分镜图均由用户单次提交，页面没有批量出图入口。视频任务统一串行；分镜台的“生成未完成视频”按镜头顺序逐项处理。

每张资产缩略图和详情标准图显示文字状态：正在提交、排队中、正在生成、归档中、已完成、失败或已停止。任务按秒轮询，阶段取真实后端消息；短于轮询间隔的阶段可能直接跳到下一状态。页面重开时从历史恢复最近结果和进行中任务，失败原因可在提示及历史中查看；有旧标准图时，重生成中的任务状态仍显示在旧图上。

上游资产的 `text_profile` 或 `output_prompt` 变化、标准图切换，以及分镜图切换或清除，都会保留历史 generation 并标记受影响镜头的图片或视频待复核；成功选用新分镜图清除图片待复核并标记视频待复核，成功选用新视频清除视频待复核。用户可按镜确认图片或视频通过，不要求整集重跑。

分镜图片生成在后端要求已保存且未过期的图片配方、至少一项出场资产，以及每个出场角色/场景已有标准图；分镜视频要求已完成分镜图、未过期的视频配方，且拒绝含九宫格、分屏、多宫格等连续首帧污染词。视频主干、运镜和动作均为空时只显示风险提示。整集合成按分镜顺序消费本集镜头视频；缺少任一镜头视频或仍有视频待复核时明确失败。图片画幅、视频画幅和视频时长只取当前模型目录声明的能力。

Agnes Video 2.5 未声明固定视频时长档位时，不由适配器静默截断用户时长或伪造 `seconds`；未传时长交由供应商协议处理。PearAPI 已核验模型的目录覆盖能力来源标记为 `adapter-override`，便于审计目录原始字段与适配器补洞。当前 PearAPI 文档已核验的图片族包括 GPT Image 1.5/2/2K/4K 与 Nano Banana Pro/Pro-4K/2/2-4K/2-Lite/基础版；它们的参考图上限和画幅必须按 `provider-api-contract` 的标准化证据映射，不能外推到未列出的模型。

## 页面与接口

前端路由：

- `/`
- `/ai-config`
- `/film/:id/script`
- `/film/:id/assets`
- `/film/:id/storyboard`

项目工作区只有总览与剧本、项目资产库和分镜台三个阶段。分镜台同时承载分镜图、镜头视频和整集合成；资产库页面只显示当前项目的角色卡、场景卡和道具卡。剧集可新建、重命名和删除，但项目至少保留一集。

后端工作区业务接口集中在 `backend/src/routes/workspaceRoutes.ts`。供应商适配器、任务、媒体归档、AI 配置和项目 ZIP 分别由独立模块负责。没有通用生产命令入口。

项目 ZIP 当前格式为 `format: 8`，保存项目、剧集、项目资产卡产出提示词与预设类型、分镜规格、两份最终配方及引用快照、三项复核状态、generation 历史以及本地媒体和本地参考图。导入会重建 ID 与本地路径；不读取旧 ZIP 格式。

## 验收合同

- 空数据库可创建项目；项目之间资产隔离，同项目不同剧集可复用同一资产 ID。
- 三类资产卡的结构化文本、独立产出提示词、产出预设类型和标准图生成输入参考图可保存、更新和读取。
- 新卡提示词为空；选择预设不会改写文本，点击组装后后端返回可见模板；用户编辑并保存的完整提示词是资产生成唯一文本输入。
- 资产缩略图随任务阶段更新文字状态；切卡与媒体刷新保留当前页面内的编辑草稿。
- 分镜只能保存当前项目资产 ID，并可保存本镜额外参考图。
- 分镜台只在显式点击时一次编译两份独立配方；结果可见、可编辑、可持久化，文本顺序稳定，空字段跳过，图片保持独立数组。
- 分镜图片只消费已保存图片配方；分镜视频只消费已保存视频配方和独立首帧，生成动作不现场重组。
- 上游变更必须让受影响镜头可见地进入待复核或配方待重装；生成与合成入口必须在后端拒绝不满足就绪条件的请求。
- 标准资产图、分镜图、镜头视频及其 generation 历史可选择当前使用结果。
- 项目 ZIP 往返后，资产规格、分镜关系、参考图、本地媒体和当前选用的 generation 仍能对应。
- 确定性自动验证包括后端 test/typecheck/build 和前端 test/lint/build；真实供应商验收与人工视觉验收单独报告。
